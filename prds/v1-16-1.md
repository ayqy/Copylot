# V1-16-1 拆分：反馈入口增强（Issue 模板自动附带 Settings + GrowthStats + TelemetryEvents）

## 范围（MVP）
在不新增权限、不新增任何联网发送、且不包含任何网页/复制内容的前提下，升级 Popup/Options 的“反馈与建议”入口：GitHub `issues/new` 预填内容在原有环境信息与 `copilot_settings` 关键开关快照后，追加本地诊断信息：
- `copilot_growth_stats`（增长统计规范化结果）
- `copilot_telemetry_events`（匿名事件日志过滤结果，最近最多 N 条，URL 过长时仅截断该段落）

## 子需求清单（按 MVP）
- [x] Shared：扩展 `src/shared/word-of-mouth.ts`
  - [x] `FeedbackSettingsSnapshot` 补齐标量字段：`isAnonymousUsageDataEnabled`、onboarding 版本、默认 Chat 服务、语言等（严禁包含用户输入文本）
  - [x] `buildFeedbackIssueUrl(...)` 支持注入 `growthStatsSnapshot` 与 `telemetryEventsSnapshot`，读取失败兜底 `{}` / `[]`
  - [x] `copilot_telemetry_events` 仅保留最近 N 条并按 `ts` 倒序；URL 过长时仅截断 telemetry 段落
- [x] 接入：Popup + Options 两处反馈入口注入诊断快照（不新增事件名、不改动既有链路）
  - [x] Popup：`src/popup/popup.ts` 的 `#feedback-link`
  - [x] Options：`src/options/options.ts` 的 `#wom-feedback-open`
- [x] i18n：更新 `feedbackIssueBodyTemplate`（en/zh），追加 `copilot_growth_stats` 与 `copilot_telemetry_events` 两段 JSON code block；`npm run check-i18n` 通过
- [x] 单测：更新 `scripts/unit-tests.ts`
  - [x] `buildFeedbackSettingsSnapshot(...)` 输出不包含用户输入文本字段（无 `userPrompts/template/title` 等 key）
  - [x] `buildFeedbackIssueUrl(...)` body 中包含 `copilot_growth_stats` 与 `copilot_telemetry_events` 关键片段
- [x] 用例与交付：新增 `docs/test-cases/v1-16.md`、输出 `docs/reports/v1-16-report.md`；`bash scripts/test.sh` 全量通过

## 当前状态
- 状态：done
- 最近更新：2026-03-19

