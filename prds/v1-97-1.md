# v1-97-1 真实发布回填最小闭环（收入优先）

## 子目标
完成至少 1 轮真实发布回填，确保官网/CWS/Pro 候补三入口具备可审计的帖子来源与参数一致性。

## 具体任务
- 按 `docs/growth/checklists/manual-posting-xhs-v1-96.md` 执行手动发布并回填帖子 URL。
- 更新 `docs/growth/assets/social/xhs/v1-96/conversion-evidence-index.json`：
  - `conversionEntries[*].postUrl` 由 `manual_post_pending` 改为真实帖子 URL；
  - `auditChainSamples[*].postUrl` 同步改为同一帖子 URL；
  - `evidencePath` 指向本轮证据文件。
- 新增执行记录：`docs/growth/executions/v1-97-growth-backfill.md`。

## 验收标准
- 三个入口的 `postUrl` 均非 `manual_post_pending`。
- `campaign/source/medium` 与 `targetUrl` UTM 参数一致。
- 执行记录可追溯到证据文件与量化结果。
