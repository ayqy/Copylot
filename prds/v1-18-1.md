# V1-18-1 拆分：本地漏斗指标补齐（GrowthStats 关键里程碑时间戳）

## 范围（MVP）
在不新增权限、不新增任何联网发送、且不记录任何网页/复制内容的前提下，补齐本地 `chrome.storage.local` 的 `copilot_growth_stats` 漏斗里程碑时间戳记录（安装 -> 首次打开 Popup -> 首次成功复制 -> 首次 Prompt 使用 -> 7 日内复用），并确保可通过 Options 隐私页导出、通过反馈模板带出，具备可回归的单测与用例文档，达到可发布状态。

## 子需求清单（按 MVP）
- [x] 扩展 `copilot_growth_stats` 结构 + 规范化（可回归）
  - [x] `src/shared/growth-stats.ts`：`GrowthStats` 新增可选时间戳字段（number, ms）
  - [x] 新增 `normalizeGrowthStatsValue(...)`：非法值（非 number / <=0 / NaN / Infinity）清理为 `undefined`
  - [x] 兼容旧数据：必要时自动回写“规范化后的结构”
- [x] Popup 首次打开里程碑写入
  - [x] `src/shared/growth-stats.ts`：新增 `markFirstPopupOpened(...)`
  - [x] `src/popup/popup.ts`：在 `initialize()` 的 `popup_opened` 同一路径写入 `firstPopupOpenedAt`（仅首次）
- [x] 成功复制里程碑写入（成功路径写入；失败不写；不影响主流程）
  - [x] `src/shared/growth-stats.ts`：抽出纯函数 `applySuccessfulCopyToGrowthStats(stats, { now, isPromptUsed })`
  - [x] `incrementSuccessfulCopyCount(...)` 同时维护：
    - [x] `firstSuccessfulCopyAt`（仅首次）
    - [x] `lastSuccessfulCopyAt`（每次更新）
    - [x] `reusedWithin7DaysAt`（count 1->2 且 7d 内，仅写一次）
    - [x] `firstPromptUsedAt`（Prompt 复制成功时写入，仅一次）
  - [x] `src/content/content.ts`：`reportSuccessfulCopy({ isPromptUsed })` 透传到后台
  - [x] `src/background.ts`：接收 `isPromptUsed` 并在同一次统计更新写入
  - [x] 容错：storage 读写失败 try/catch，静默降级（仅 `console.warn`）
- [x] 单测 + 用例文档 + 可发布交付
  - [x] `scripts/unit-tests.ts`：覆盖时间戳写入规则、Prompt 标记、7 日复用规则、normalize 清理非法值
  - [x] `docs/test-cases/v1-18.md`：用例 A-F + `bash scripts/test.sh` 执行记录
  - [x] `docs/reports/v1-18-report.md`：发布简报
  - [x] `bash scripts/test.sh` 全量通过

## 当前状态
- 状态：done
- 最近更新：2026-03-19

