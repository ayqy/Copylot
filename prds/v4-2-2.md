# v4-2-2 自动化回归与验收用例

## 子需求

- 更新 `e2e/code-block-flow.spec.ts` 与 `e2e/fixtures/code.html`，覆盖代码块悬停复制与整页转换核心回归。
- 更新 `scripts/unit-tests.ts`、`scripts/content-interaction-tests.ts` 与 `test/cases/code-block-reuse.html`，形成无需真人参与的稳定回归链路。
- 更新 `docs/test-cases/v4-2.md`，把自动化入口与手工复核步骤统一到同一份文档。

## 验收要点

- `bash scripts/test.sh` 为统一测试入口，退出码为 `0`。
- 自动化至少覆盖悬停复制、整页 fenced code、缩进空行保留、行号与复制按钮噪声清理。
- 手工用例与自动化用例共享同一套样例页与判定标准。
