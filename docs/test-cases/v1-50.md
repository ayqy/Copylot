# V1-50 Pro 意向验证节奏固化：每周“意向证据摘要”一键生成 + 问卷模板渠道化 测试用例

## 目标
验证子 PRD `prds/v1-50.md` 的验收点：在不新增权限、不联网发送数据、不采集用户复制内容的前提下，把本地匿名事件口径固化为“过去 7 天（7d window）的 Pro 意向证据摘要”一键复制能力，并形成可复用的问卷/复盘模板与可审计证据落盘资产。

本轮验收对象（固定路径）：
- 用例文档（本文件）：`docs/test-cases/v1-50.md`
- 模板：`docs/monetization/pro-waitlist-survey-template.md`、`docs/growth/pro-intent-weekly-digest-template.md`
- 证据目录与索引：`docs/evidence/v1-50/` + `docs/evidence/v1-50/index.md`

---

## 用例 A：入口与 DOM 稳定性（隐私页一键复制按钮存在）

前置：
- 使用仓库内最新 `plugin-*.zip` 解压后以 unpacked 方式加载（禁止用 `src/` 开发态代替验收）。

步骤：
1. 打开 Options -> 隐私与可观测性。
2. 展开「Pro 意向漏斗摘要」面板。
3. 观察并定位按钮：`复制本周 Pro 意向证据摘要`（稳定 DOM：`#copy-pro-intent-weekly-digest`）。

期望：
- 按钮存在且可点击；DOM id 稳定可用于自动化/截图索引定位。

---

## 用例 B：匿名使用数据 OFF（不读取/不推断历史，仅输出环境信息与说明）

步骤：
1. Options -> 隐私与可观测性：确保“匿名使用数据”开关为 OFF（默认）。
2. 点击 `复制本周 Pro 意向证据摘要`。
3. 将剪贴板内容粘贴到任意纯文本处。

期望（必须全部满足）：
- 摘要明确提示：`匿名使用数据关闭（无可用事件）`，并说明“不读取/不推断历史”。
- 仅包含环境信息（extension version / exportedAt / 匿名开关状态）与隐私声明。
- 不包含事件统计明细（不应出现 `pro_entry_opened/pro_waitlist_opened/pro_waitlist_copied/pro_waitlist_survey_copied` 的计数字段）。

---

## 用例 C：匿名使用数据 ON 且 7d window 内无事件（计数为 0，转化率为 N/A）

步骤：
1. Options -> 隐私与可观测性：先点击“清空”本地匿名事件日志（Telemetry Events 面板）。
2. 开启“匿名使用数据”开关。
3. 立即点击 `复制本周 Pro 意向证据摘要` 并粘贴剪贴板内容。

期望：
- 摘要为 Markdown，包含：
  - 时间窗（本地时间 `from/to`，过去 7 天）
  - 关键事件计数（overall + bySource），均为 `0`
  - 关键转化率（overall）：分母为 0 时输出 `N/A`
  - 环境信息（extension version / exportedAt / 匿名开关状态）
  - 隐私声明（不包含网页内容/复制内容/URL/标题）

---

## 用例 D：匿名使用数据 ON 且 7d window 内有事件（计数/转化率可复核）

步骤：
1. Options -> 隐私与可观测性：开启“匿名使用数据”开关（若已开启可跳过）。
2. Options -> Pro Tab（触发 `pro_entry_opened`，source=options）。
3. 在 Pro Tab 依次触发：
   - 点击 `加入候补名单`（触发 `pro_waitlist_opened`）
   - 点击 `复制候补文案`（触发 `pro_waitlist_copied`）
   - 点击 `复制问卷（含环境信息）`（触发 `pro_waitlist_survey_copied`，若该入口存在）
4. 回到 Options -> 隐私与可观测性，点击 `复制本周 Pro 意向证据摘要` 并粘贴。

期望：
- 事件计数（overall）与 bySource（options）均至少包含你刚触发的次数（>=1）。
- 转化率字段可用“手算”复核：
  - `waitlist_opened_per_entry_opened = pro_waitlist_opened / pro_entry_opened`
  - `waitlist_copied_per_waitlist_opened = pro_waitlist_copied / pro_waitlist_opened`
  - `survey_copied_per_entry_opened = pro_waitlist_survey_copied / pro_entry_opened`
- 摘要不包含网页内容/复制内容/URL/标题；仅包含本地匿名事件统计与扩展环境信息。

---

## 用例 E：7d window 边界（事件过滤：<from 排除，=from/=to 包含）

说明：本用例通过“手动写入本地 telemetry（仅本地）”验证时间窗边界；适用于回归与审计。

步骤：
1. Options -> 隐私与可观测性：开启“匿名使用数据”开关。
2. 在扩展的 Service Worker（或任意能执行 `chrome.storage.local` 的扩展上下文）运行以下脚本写入本地事件：
   - 写入 1 条 `ts = Date.now() - 8d` 的事件（应被排除）
   - 写入 1 条 `ts = Date.now() - 7d` 的事件（=from，应被包含）
   - 写入 1 条 `ts = Date.now()` 的事件（=to，应被包含）

参考脚本（示意）：
```js
const now = Date.now();
await chrome.storage.local.set({
  copilot_telemetry_events: [
    { name: 'pro_entry_opened', ts: now - 8*24*60*60*1000, props: { source: 'options' } },
    { name: 'pro_entry_opened', ts: now - 7*24*60*60*1000, props: { source: 'options' } },
    { name: 'pro_entry_opened', ts: now, props: { source: 'options' } }
  ]
});
```

3. 回到 Options -> 隐私与可观测性：点击 `复制本周 Pro 意向证据摘要` 并粘贴。

期望：
- `pro_entry_opened` 的计数应为 `2`（8d 的被排除，7d 边界与 now 边界包含）。
- 时间窗 `from/to` 与写入 ts 的关系可人工核对（可审计）。

---

## 自动化回归记录（可发布审计证据）

- 统一入口：`bash scripts/test.sh`
- 最近执行日期：2026-03-20
- 结论：PASS

