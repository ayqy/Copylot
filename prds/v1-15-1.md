# V1-15-1 拆分：Options “重新查看新手引导”入口（MVP）

## 范围
在不新增权限、不新增任何联网埋点、且不影响现有复制体验的前提下，在 Options 的 Prompt 管理页补齐“重新查看新手引导”入口；点击后在新 Tab 打开 `src/popup/popup.html#onboarding` 并强制展示 3 步引导，用于回看。

## 子需求清单（按 MVP）
- [x] UI：在 `src/options/options.html` 的 `#prompts-tab` 顶部新增轻量提示区块，稳定 DOM：
  - [x] 容器：`#options-onboarding-panel`
  - [x] 打开按钮：`#options-onboarding-open`
- [x] i18n：新增/补齐相关 key，并同步更新 `_locales/en/messages.json` 与 `_locales/zh/messages.json`；确保 `npm run check-i18n` 通过
- [x] Options 逻辑：在 `src/options/options.ts` 监听点击，使用 `chrome.tabs.create({ url })` 打开 `${chrome.runtime.getURL('src/popup/popup.html')}#onboarding`（失败降级不影响其它功能）
- [x] Popup deeplink：在 `src/popup/popup.ts` 支持 `#onboarding` hash 强制打开引导 `openOnboardingModal('manual')`，忽略 completed 状态；未携带 hash 仍按 `shouldAutoShowOnboarding` 自动展示
- [x] 可发布交付：补齐用例文档 `docs/test-cases/v1-15.md` 与简报 `docs/reports/v1-15-report.md`；`bash scripts/test.sh` 全量通过

## 当前状态
- 状态：done
- 最近更新：2026-03-19

