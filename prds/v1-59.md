# 问题修复和功能优化（V1-59 渠道分发/投放周报再降摩擦：一键复制本周获客效率摘要（Markdown，按 campaign））

## 目标
基于 `docs/roadmap_status.md` 的收入优先 Top3：Top1/Top2 仍受“需要可用代理/VPN + 商店可达”的网络可达性阻塞，本轮顺延选择 Top3「渠道分发/投放跑数与复盘固化 v4」作为 1~3 天最小可交付增量主题。

收入第一：在不引入支付/订阅、不新增权限、不联网发送数据、不采集用户复制内容的前提下，把 v1-58 已打通的「按 campaign 合并导出获客效率 CSV（leads + distCopies + leadsPerDistCopy）」进一步变成**可直接粘贴到周报/复盘文档**的 Markdown 资产——新增「复制本周获客效率复盘摘要（Markdown，按 campaign）」入口，降低渠道分发跑数后的复盘/审计摩擦，让“真实收入回报路径（可转化获客增长 -> 留资 -> 后续 Pro 转化验证）”推进具备可验证证据。

## ULTRA IMPORTANT 第一原则：任务开始后，不要停下来，直到完成目标
**所有细节，都由你来定，判断依据是根据核心目标决定应该如何做，而不要停下来问我**

## 具体任务
1、新增「复制本周获客效率复盘摘要（Markdown，按 campaign）」入口（让渠道周报可直接粘贴）
  - 入口位置：Options -> 隐私与可观测性 -> Pro 面板（与 v1-58 导出入口同区域）
  - 稳定 DOM（必须固定，便于用例/断言/回归）：`#copy-pro-acquisition-efficiency-by-campaign-weekly-report`
  - 匿名开关约束（必须与 v1-50/v1-56 保持一致，避免误读）：
    - 匿名使用数据 OFF：允许生成 Markdown，但**不得读取/不推断**本地事件；Markdown 必须包含明确的 OFF 提示与环境信息（用于可审计）
    - 匿名使用数据 ON：读取本地匿名事件日志并生成按 campaign 汇总的获客效率摘要
  - 时间窗：过去 7 天（lookbackDays=7；window 口径必须与 v1-51/v1-54/v1-57/v1-58 保持一致，便于互证）
  - 指标口径（必须可复核，且与 v1-58 一致）：
    - `leads`：`pro_waitlist_copied + pro_waitlist_survey_copied`（按 campaign 聚合）
    - `distCopies`：`pro_distribution_asset_copied`（按 campaign 聚合，action 聚合求和）
    - `leadsPerDistCopy`：`leads / distCopies`（`distCopies=0 -> N/A`；数值格式建议保留 4 位小数，与 v1-58 CSV 一致）
    - 空 campaign：复用 i18n 文案 `proAcqEffByCampaign7dCsvEmptyBucket`（用于发现分发漏填导致不可归因）
  - Markdown 内容要求（字段/顺序必须稳定，便于审计与对比）：
    - 标题：例如「Pro 获客效率复盘摘要（过去 7 天，按 campaign）」（需 i18n）
    - Env 区块：`extensionVersion/exportedAt/lookbackDays/windowFrom/windowTo/anonymousUsageData`
    - 表格区块（按 `leadsPerDistCopy desc -> leads desc -> distCopies desc -> campaign asc` 排序，与 v1-58 rows 一致）：
      - `campaign/leads/distCopies/leadsPerDistCopy`
    - Insights 区块（最少 3 条，且可复核）：
      - Top1 campaign（按 `leadsPerDistCopy`）
      - Top1 campaign 的 `leads/distCopies/leadsPerDistCopy`
      - 空 campaign leads 占比（用于发现“获客不可归因”的商业化风险）
    - Privacy 区块：明确“不包含 URL/标题/网页内容/复制内容；仅基于本地匿名事件派生”
  - 实现建议（必须可单测的纯函数，避免在 UI 层拼口径）：
    - 新增 `src/shared/pro-acquisition-efficiency-by-campaign-weekly-report.ts`
      - `buildProAcquisitionEfficiencyByCampaignWeeklyReportSummary(...)`（复用 `buildProAcquisitionEfficiencyByCampaignCsv` 的 rows/window 口径）
      - `formatProAcquisitionEfficiencyByCampaignWeeklyReportMarkdown(...)`（输出稳定 Markdown）
    - Options 侧新增 `copyProAcquisitionEfficiencyByCampaignWeeklyReportToClipboard()`，并接入 i18n 成功/失败提示

2、补齐自动化测试 + 可执行用例（保证 1~3 天可交付、可回归）
  - 自动化测试加固（优先纯函数 + `scripts/unit-tests.ts`）：
    - 7d window 边界一致性（与 v1-58 导出互证：同一 now 下 windowFrom/windowTo 必须一致）
    - OFF 行为：不得读取 events；Markdown 必须包含 OFF 提示 + Env 区块
    - ON 行为：至少覆盖 2 个 campaign + 空 campaign；排序稳定；`distCopies=0 -> N/A`；小数格式稳定（4 位）
    - 互证断言：从同一组 telemetry events 生成的 weekly report 表格数据，必须可与 v1-58 的 rows 对齐复算一致
  - 新增测试用例文档（固定路径，必须在 PRD 中明确）：`docs/test-cases/v1-59.md`
    - 覆盖：匿名 OFF/ON；点击 `#copy-pro-acquisition-efficiency-by-campaign-weekly-report` 复制成功；把 Markdown 表格数据与 `#export-pro-acquisition-efficiency-by-campaign-7d-csv` 导出结果互证；并包含一次回归记录（日期 + PASS）
    - 必须要求 `bash scripts/test.sh` 全量通过

3、证据落盘与进度看板更新（让商业化推进“做完可证明”）
  - 证据目录（固定路径）：`docs/evidence/v1-59/` + `docs/evidence/v1-59/index.md`
    - 至少落盘（脱敏、可审计、可复核）：
      - `pro-acq-eff-by-campaign-weekly-report.off.md`：匿名 OFF 示例（仅 Env + OFF 提示）
      - `pro-acq-eff-by-campaign-weekly-report.on.md`：匿名 ON 示例（含至少 2 个 campaign + 空 campaign 行）
      - 截图索引：按钮入口 + 复制成功通知（可选补齐匿名 OFF/ON 两态）
      - 互证步骤：如何用 v1-58 导出的 CSV 复算并核对 weekly report 表格（避免“做完不可证明”）
  - 更新 `docs/roadmap_status.md`：勾选 v1-59 条目，并刷新“当前进度/Top3/阻塞”描述（断点续孵化不断档）

## 验收标准
* 修复上述所有问题，并测试通过，达到可发布状态
* 实现以上所有需求，并测试通过，达到可发布状态
* 可验证的商业化推进证据（必须可审计/可复核/可复用）：
  - Options -> 隐私与可观测性 -> Pro 面板存在入口，且稳定 DOM 为 `#copy-pro-acquisition-efficiency-by-campaign-weekly-report`
  - 匿名开关 OFF：可复制 Markdown 且明确提示“匿名使用数据关闭（不读取/不推断事件）”，并包含 Env 区块（window/版本/时间戳）
  - 匿名开关 ON：可复制 Markdown，表格包含 `campaign/leads/distCopies/leadsPerDistCopy`，且与 v1-58 的 7d 合并 CSV 口径一致、可互证复算（`distCopies=0 -> N/A`）
  - 证据落盘可审计：`docs/evidence/v1-59/index.md` 至少包含（1）OFF/ON 示例 Markdown（2）截图索引（3）与 v1-58 CSV 的互证步骤，可支撑“按 campaign 的获客效率周报”复盘与审计
* 测试与用例可执行：
  - `docs/test-cases/v1-59.md` 可逐条执行并包含一次回归记录（日期 + PASS），且 `bash scripts/test.sh` 全量通过

## 注意
1、以上任务为“最小可交付增量（1-3 天）”，收入第一：本轮只做“获客效率周报 Markdown 一键复制 + 用例/证据/测试闭环”，不引入支付/订阅、不新增权限、不做与商业化取证无关的体验微调
2、保持口径兼容：不得修改 v1-51/v1-54/v1-57/v1-58 的既有导出字段/文件名/统计口径；本轮只新增 weekly report 作为复盘加速器
3、根据需求创建测试用例文档，通过单元测试和自动化测试来确保代码的正确性（本次测试用例文档路径固定为 `docs/test-cases/v1-59.md`）
4、目标任务成功完成之后，写一份简要的汇报材料文档（固定路径：`docs/reports/v1-59-report.md`），说清楚每一个具体任务现在是什么状态（是否按要求完成了），已经实现了什么效果，以及本次对代码的修改范围（涉及哪些目录和文件）
5、隐私与合规红线：不得新增扩展权限；不得上传/采集用户复制内容；OFF 时不读取/不推断本地事件；ON 时仅基于本地匿名事件日志派生（字段白名单）；campaign 仅来源于用户手动输入/本地设置且需通过既有校验（不得包含敏感信息）

