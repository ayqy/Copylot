# V1-31-1 拆分：生产构建自检脚本 + 全量 Smoke 用例闭环 + 发版简报

## 范围（MVP）
在不新增权限、不新增任何联网发送、且不影响现有复制主流程的前提下，补齐 V1 发布收尾的“测试用例与发布准备”可执行交付：

- 生产构建产物自检脚本：可自动验证 `dist/` 为“干净产物”，失败即阻断发布
- 全量 Smoke 手工回归用例：覆盖 V1 核心链路（A-H），可在 30 分钟内跑完，并记录结论
- 发布简报：可用于口头验收与发版记录

## 子需求清单（按 MVP）
- [x] 生产构建“干净产物”自检脚本
  - [x] 新增 `scripts/verify-prod-build.sh`（`set -euo pipefail`；失败退出非 0；不新增 npm 依赖）
  - [x] 覆盖自检项：
    - [x] `dist/` 必须存在
    - [x] `dist/` 不得包含测试资源（`*test*` / `*runner*`）
    - [x] `dist/manifest.json` 不得包含 `test` 字样引用
    - [x] `dist/src/popup/popup.js` 不得包含 `clickCount|easter|test/index.html`
    - [x] `dist/manifest.json` 权限白名单校验（严格等于 `["storage","clipboardWrite","contextMenus"]`，顺序不敏感）
- [x] 接入统一回归入口
  - [x] 更新 `scripts/test.sh`：在 `npm run build:prod` 后追加执行 `bash scripts/verify-prod-build.sh`
- [x] V1 全量 Smoke 手工回归用例（A-H）
  - [x] 新增 `docs/test-cases/v1-31.md`
  - [x] 覆盖用例 A-H（权限/隐私、引导、复制主流程、追加模式、表格转换、Prompt 工作流、口碑闭环、商业化入口）
  - [x] 记录一次 `bash scripts/test.sh` 回归结论（包含生产包自检脚本）
- [x] 发布简报交付
  - [x] 新增 `docs/reports/v1-31-report.md`

## 当前状态
- 状态：done
- 最近更新：2026-03-20

