# V1-23 ASO 素材规范最小闭环：截图顺序 / GIF 脚本 / 更新日志模板 测试用例

## 目标
验证 V1-23 达到可发布（不新增权限、不引入任何联网分析、不改动核心复制/Prompt/引导主流程的前提下）：
- 已沉淀可复用 ASO 资产文档：
  - `docs/aso/store-assets.md`（CWS 截图顺序 + 每张截图的拍摄步骤 + 开关前置 + EN/ZH 文案 + 统一隐私提示）
  - `docs/aso/gif-script.md`（可执行 20-30s GIF/短视频脚本，EN/ZH 双语，口径与 `docs/aso/value-prop.md` 一致）
  - `docs/aso/cws-release-notes-template.md`（可复制粘贴到 CWS 的更新日志模板，含 Privacy/Verification，且禁止夸大/禁止写未实现能力）
- 口径一致性抽检通过：不出现“联网分析/上传复制内容”等违背隐私原则表述
- `bash scripts/test.sh` 全量通过

## 预置条件
- 仓库已包含上述文档文件
- 可执行 `bash scripts/test.sh`

## 用例 A（素材规范完整性）
检查项（`docs/aso/store-assets.md`）：
1. 包含截图顺序，且截图数量 >= 6（建议 6-8）。
2. 每张截图均包含：
   - 截图标题（EN/ZH 各 1 条）
   - 要展示的真实功能点（能在仓库中通过 `README.md` / `docs/roadmap.md` / `manifest.json` 等验证）
   - 拍摄步骤（可复现）
   - 需要的开关前置（如 Format/Table/Append/Hover/Extra 等）
3. 包含统一口径的隐私提示（EN/ZH），且与 `docs/aso/value-prop.md` / `docs/privacy-policy.md` 不冲突。
4. 不包含“未来能力/设想”文案；不包含“强制弹窗/强制评价”等高打扰引导描述。

执行记录：
- 结果：通过
- 备注：截图顺序为 7 张；逐张包含 EN/ZH 标题、功能点、开关前置与拍摄步骤；隐私提示为 EN/ZH 双语。

## 用例 B（GIF 脚本可执行）
检查项（`docs/aso/gif-script.md`）：
1. 至少包含 1 个 20-30 秒脚本，含分镜/字幕/镜头切换。
2. 选择并固定一个主线场景（A 或 B），且脚本描述的每个 UI/开关/结果均可在当前版本复现：
   - Popup 中存在 `Table: CSV` 等相关开关（`src/popup/popup.html`）。
   - 表格复制能力在 `README.md` 与实现中存在。
3. 字幕/旁白文案提供 EN/ZH 两套，且与 `docs/aso/value-prop.md` 口径一致（不夸大）。

执行记录：
- 结果：通过
- 备注：脚本固定为“场景 A：网页表格一键转 CSV 并粘贴到表格工具”；已核对 Popup 开关与功能点均为现有能力，文案与 `docs/aso/value-prop.md` 一致。

## 用例 C（更新日志模板可复用）
检查项（`docs/aso/cws-release-notes-template.md`）：
1. 可直接复制粘贴到 CWS 更新日志。
2. 包含固定结构：
   - Highlights（1-3 条）
   - Fixes
   - Privacy（无变更也声明“无新增权限/无新增数据收集”）
   - Verification（内部用：必须要求 `bash scripts/test.sh` 通过）
3. 模板明确“禁止夸大/禁止写未实现能力”。

执行记录：
- 结果：通过
- 备注：模板包含 Highlights/Fixes/Privacy/Verification 固定段落，并在文档顶部强调不写未实现能力。

## 用例 D（口径一致性抽检）
抽检方法：
1. 从 V1-23 新增文档中随机抽检 3 条对外文案（截图标题/字幕/隐私声明/Release notes 文案均可）。
2. 对照 `README.md` / `docs/aso/value-prop.md` / `docs/privacy-policy.md`：
   - 不冲突、不夸大
   - 不出现“联网分析/上传复制内容/上传 URL/上传页面标题”等违背隐私原则表述

本次抽检样本（3 条）：
1. `docs/aso/store-assets.md`：`“网页表格一键转 CSV（可直接粘贴到表格工具）”`
2. `docs/aso/gif-script.md`：`“Web table → CSV / Markdown. Clean. Ready to use.”`
3. `docs/aso/cws-release-notes-template.md`：`“Anonymous usage data: OFF by default; ... not sent over the network.”`

执行记录：
- 结果：通过
- 备注：三条均可在 `README.md` / `docs/aso/value-prop.md` / `docs/privacy-policy.md` 中找到对应口径或实现佐证；未发现联网分析/上传复制内容等表述。

## 自动化回归：统一入口（验收前必须填写）
步骤：
- 执行：`bash scripts/test.sh`

期望：
- lint/type-check/check-i18n/unit-tests/build:prod 全量通过。

执行记录：
- 命令：`bash scripts/test.sh`
- 结果：通过
- 最近执行日期：2026-03-19
