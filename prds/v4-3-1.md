# v4-3-1 追加会话状态与隐私口径

## 目标
先把 Shift 追加模式的会话状态、完成判定、清理计数与隐私边界固化到共享层。

## 具体任务
1、扩展 `src/shared/append-session.ts` 的状态枚举、完成计数、清理计数与最大段数统计。
2、保证导出只包含段数、枚举、时间戳与聚合计数，不记录正文或 URL。
3、补齐 `scripts/unit-tests.ts` 的纯函数回归。
