# v4-3-4 跨 campaign 复核共享逻辑与导出脚本

## 目标

把 `advanced_cleaning` 的跨 campaign 样本复核收敛到共享层与脚本层，形成可复制、可下载、可回归的审计包。

## 状态

- [x] 复核包输出 `overallLeaderTrackId`、`supportingCampaigns`、`conflictingCampaigns`、`thinCampaigns`、`prioritizedCampaigns`、`conclusion`、`nextStep` 与证据来源。
- [x] 每个 campaign 都输出 `leader`、`signalGap`、`reviewStatus`、`reviewAction`、`conclusion`、`nextStep` 与匿名聚合证据。
- [x] 复核包显式区分 `acquisition_bias_unresolved`、`sample_still_thin` 与 `no_campaign_signals`。
- [x] `scripts/build-pro-route-validation-campaign-review-pack.ts` 可稳定生成 `docs/evidence/v4-13/campaign-review-pack/*`。

## 具体任务

1. 收敛 `src/shared/pro-route-validation-campaign-review.ts` 的 pack 结构、blocker 分类、Markdown 输出与证据来源。
2. 更新 `scripts/build-pro-route-validation-campaign-review-pack.ts`，把 `stability` / `verdict` 样例收口成确定性产物。
3. 保证全程只使用本地匿名聚合字段，不引入页面正文、复制内容、URL、标题或联系方式。
