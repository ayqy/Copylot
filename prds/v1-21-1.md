# V1-21-1 拆分：口碑入口可观测性对齐（wom_* 埋点补齐 source + 商店 UTM campaign 统一）

## 范围（MVP）
在不新增权限、不新增任何联网发送、且不改变既有 `wom_*` 事件名的前提下，补齐 Popup/Options 的 WOM（分享/反馈/评价）入口可审计来源（`source=popup|options`），并统一打开商店详情页的 UTM 参数（`utm_campaign=v1-21`），同时补齐单测、用例文档与可发布交付物。

## 子需求清单（按 MVP）
- [x] `wom_*` 事件补齐 `source` props 白名单过滤（隐私合规、向后兼容）
  - [x] `src/shared/telemetry.ts`：`wom_feedback_opened`/`wom_share_opened`/`wom_share_copied`/`wom_rate_opened` allowlist 增加 `source`
  - [x] `src/shared/telemetry.ts`：`source` 仅允许 `'popup' | 'options'`；非法值在 `sanitizeTelemetryEvent` 中丢弃（props 变 `undefined`）
  - [x] 兼容性：无 props 的旧数据仍可通过 sanitize；开关关闭时不记录且关闭即清空（不发送、不缓存、不补发）
- [x] Popup/Options 口碑入口接入 `source` + 统一商店 UTM campaign（不改 DOM、不新增依赖）
  - [x] `src/popup/popup.ts`：WOM 入口埋点补齐 `{ source:'popup' }`（feedback/share/rate/share_copied）
  - [x] `src/options/options.ts`：WOM 入口埋点补齐 `{ source:'options' }`（feedback/share/rate/share_copied）
  - [x] `src/shared/word-of-mouth.ts`：默认商店详情页 UTM 统一为 `utm_campaign=v1-21`
  - [x] `src/options/options.ts`：Options 商店详情页 UTM 统一为 `utm_campaign=v1-21`（`utm_medium=options-entry`）
- [x] 文档与测试闭环（单测 + 用例文档 + 可发布简报）
  - [x] `docs/growth/telemetry-events.md`：WOM 事件补充 props：`{ source: 'popup' | 'options' }`
  - [x] `scripts/unit-tests.ts`：覆盖 WOM source 过滤、非白名单过滤、非法 source 丢弃、UTM campaign=v1-21
  - [x] `docs/test-cases/v1-21.md`：补齐用例 A-D + `bash scripts/test.sh` 执行记录
  - [x] `docs/reports/v1-21-report.md`：发布简报
  - [x] `bash scripts/test.sh` 全量通过

## 当前状态
- 状态：done
- 最近更新：2026-03-19

