# V1-22-1 拆分：匿名本地埋点枚举值收敛（source/action 严格过滤 + 单测/用例补齐）

## 范围（MVP）
在不新增权限、不新增任何联网发送、且不改变既有事件名的前提下，收敛本地匿名事件（`copilot_telemetry_events`）中枚举型 props（`source/action`）的取值范围：非法值在 `sanitizeTelemetryEvent` 阶段丢弃（事件保留，props 可能变 `undefined`），避免误记录潜在敏感信息；并补齐单测、用例文档与可发布简报，保证 `bash scripts/test.sh` 全量通过。

## 子需求清单（按 MVP）
- [x] Telemetry：枚举型 props 严格过滤（仅本地、向后兼容、隐私合规）
  - [x] `src/shared/telemetry.ts`：在 `sanitizeTelemetryEvent/sanitizeProps` 增强枚举值域校验（不新增事件名、不新增联网发送）
  - [x] `source`：
    - [x] `pro_entry_opened` / `pro_waitlist_opened`：仅允许 `'popup' | 'options'`
    - [x] `pro_waitlist_copied`：仅允许 `'options'`
    - [x] `onboarding_shown` / `onboarding_completed`：仅允许 `'auto' | 'manual'`
    - [x] `wom_*`：保持既有 `'popup' | 'options'` 校验逻辑不变
  - [x] `action`：
    - [x] `onboarding_completed`：仅允许 `'finish' | 'skip'`
    - [x] `rating_prompt_action`：仅允许 `'rate' | 'later' | 'never'`
  - [x] 兼容性：无 props 的旧数据仍可通过 sanitize；开关关闭时不记录且关闭即清空（不发送、不缓存、不补发）
- [x] 调用方对齐（不改 UI，仅确保只产出合法枚举值）
  - [x] `src/popup/popup.ts`：校验 `onboarding_*` / `rating_prompt_action` / `pro_*` / `wom_*` 均为固定枚举值（无动态拼接）
  - [x] `src/options/options.ts`：校验 `pro_*` / `wom_*` 均为固定枚举值（其中 `pro_waitlist_copied` 仅 `source='options'`）
- [x] 文档与测试闭环（单测 + 用例文档 + 可发布简报）
  - [x] `docs/growth/telemetry-events.md`：补充说明 `source/action` 枚举值域 + 非法值在 sanitize 阶段丢弃
  - [x] `scripts/unit-tests.ts`：覆盖 `pro_*`/`onboarding_*`/`rating_prompt_action` 的 enum 严格过滤断言
  - [x] `docs/test-cases/v1-22.md`：补齐用例 A-D + `bash scripts/test.sh` 执行记录
  - [x] `docs/reports/v1-22-report.md`：发布简报
  - [x] `bash scripts/test.sh` 全量通过

## 当前状态
- 状态：done
- 最近更新：2026-03-19

