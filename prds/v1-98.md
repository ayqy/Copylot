# 问题修复和功能优化（V1-98 xhs 可转化样本扩展与收入证据批量回填）

## 目标
实现具体任务中要求的所有功能和问题修复（在 Top1「CWS 权限 + `pxy` 代理」阻塞未解除时，顺延执行 `docs/roadmap_status.md` Top2 第 1 项），使“分发 -> 转化 -> Pro 意向”链路在 1-3 天内新增可审计样本并形成可导出证据，达到可验证商业化推进的验收标准。

## ULTRA IMPORTANT 第一原则：任务开始后，不要停下来，直到完成目标
**所有细节，都由你来定，判断依据是根据核心目标决定应该如何做，而不要停下来问我**

## 具体任务
1、xhs 可转化样本扩展（收入优先，1-3 天内完成）
  - 基于 `docs/growth/checklists/manual-posting-xhs-v1-96.md` 与既有发布入口，新增至少 3 条真实可追溯帖子样本，优先覆盖官网、CWS、Pro 候补三类转化入口。
  - 更新 `docs/growth/assets/social/xhs/v1-96/conversion-evidence-index.json`：批量回填 `conversionEntries[*].postUrl`、`auditChainSamples[*].postUrl`，并补齐 `clicks/installs/proIntentSignals` 与证据文件路径。
  - 保持 `campaign/source/medium/content` 参数口径一致，避免出现无法归因的新增样本。

2、收入证据包与执行记录闭环
  - 新增执行记录 `docs/growth/executions/v1-98-growth-backfill.md`，记录样本来源、入口映射、关键指标与风险处置结果。
  - 在 `docs/evidence/v1-98/` 落盘可导出证据（建议包含 `conversion-funnel-v1-98.csv`、`sample-audit-v1-98.json`、截图索引），确保可直接用于复盘和口头汇报。
  - 更新 `docs/growth/metrics.md` 对应条目，使新增样本可体现在 `clicks -> installs -> proIntentSignals` 的同口径对比中。

3、测试用例与门禁（必须可执行）
  - 创建测试用例文档 `docs/test-cases/v1-98.md`，至少包含：
    - 用例 A：样本回填完整性（新增样本数、`postUrl` 非占位、字段完整性）。
    - 用例 B：归因一致性（`campaign/source/medium/content` 与目标入口映射一致）。
    - 用例 C：商业化证据可导出（可从索引定位到证据文件并复核 `clicks/installs/proIntentSignals`）。
    - 用例 D：阻塞分流（若账号/风控阻塞，`docs/growth/blocked.md`、`docs/growth/todo.md`、执行记录三方一致）。
  - 执行 `bash scripts/test.sh`，将结果落盘到 `docs/evidence/v1-98/test.sh.log`，并在 `docs/test-cases/v1-98.md` 记录 PASS/BLOCKED 与证据文件名。

## 验收标准
* 修复上述所有问题，并测试通过，达到可发布状态
* 实现以上所有需求，并测试通过，达到可发布状态
* 可验证的商业化推进证据（必须可审计/可复盘）：
  - `docs/growth/assets/social/xhs/v1-96/conversion-evidence-index.json` 至少新增 3 条真实帖子样本，且每条都可追溯到官网/CWS/Pro 候补入口之一。
  - 至少 2 条新增样本包含非空的 `clicks`、`installs`、`proIntentSignals` 三项量化字段，并与执行记录一致。
  - `docs/evidence/v1-98/` 产出至少 1 份可导出的漏斗证据文件（CSV 或 JSON），可直接用于复盘会议。
* 顺延机制与阻塞记录符合规则：
  - `docs/roadmap_status.md` 明确记录 Top1 阻塞为“CWS 权限 + `source ~/.bash_profile && pxy` 未就绪”，并标注本轮顺延执行 Top2（v1-98）。
* 测试与用例可执行：
  - `docs/test-cases/v1-98.md` 提供可复现步骤、预期结果、回归记录（PASS/BLOCKED + 证据文件名）。
  - `bash scripts/test.sh` 执行完成且日志落盘，不引入与收入主链路无关的大范围功能改动。

## 注意
1、以上任务规模非常大，每一个都很有挑战性，弄清楚需求后你应该创建新的需求文档来记录每个子需求，例如 `v1-98-1.md`、`v1-98-2.md`、`v1-98-3.md`（按需拆分，保持 1-3 天可交付）
2、一步一步来，对于每个阶段，边执行边更新对应文档中的进度（至少同步 `docs/roadmap.md`、`docs/roadmap_status.md`、`docs/growth/executions/v1-98-growth-backfill.md`）
3、根据需求创建测试用例文档，通过单元测试和自动化测试来确保代码的正确性（固定路径：`docs/test-cases/v1-98.md`）
4、目标任务成功完成之后，写一份简要的汇报材料文档（用于口头汇报，所以要简明扼要抓住重点），说清楚每一个具体任务现在是什么状态（是否按要求完成了），已经实现了什么效果，以及本次对代码的修改范围（涉及哪些目录和文件）（固定路径：`docs/reports/v1-98-report.md`）
5、收入第一：除阻塞上架/收款/转化漏斗的关键问题外，不处理纯体验细节；若真实发布遇登录/验证码/风控阻塞，必须先更新 `docs/growth/blocked.md` 并执行 `make todo`，随后继续完成可离线复核的收入证据补齐
