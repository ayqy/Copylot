# V1-38-1 拆分：Options 隐私页「Pro 意向漏斗摘要」+ 证据包导出（本地可审计）

## 范围（MVP）
在**不新增权限**、**不联网发送**、**不收集用户内容（URL/标题/网页内容/复制内容）**的前提下：

- 在 Options -> 隐私与可观测性新增「Pro 意向漏斗摘要」面板
- 支持一键复制「商业化证据包（Pro Funnel Evidence Pack）」并固化可审计 JSON 格式

## 子需求清单（按 MVP）
- [x] Pro 意向漏斗摘要（仅本地）
  - [x] 数据源仅来自 `copilot_telemetry_events`
  - [x] 按 `props.source=popup|options` 分别统计 `pro_entry_opened/pro_waitlist_opened/pro_waitlist_copied` 的次数与最近时间戳
  - [x] 派生转化率仅展示/导出（不落盘）：`waitlist_opened / entry_opened`、`waitlist_copied / waitlist_opened`
  - [x] 明确边界：事件窗口最多 100 条（FIFO），仅基于当前窗口统计
  - [x] 交互：支持刷新/复制（JSON）
  - [x] 开关关闭时：面板提示“未开启，不记录/不导出”，摘要导出为空/不可用

- [x] 一键导出证据包（可审计 JSON）
  - [x] 顶层字段：`meta/settings/proFunnel/events`
  - [x] `meta`：导出时间（ms）、扩展版本号（manifest）、导出来源（options）
  - [x] `settings`：字段白名单（当前仅 `isAnonymousUsageDataEnabled`）
  - [x] `proFunnel`：摘要对象
  - [x] `events`：仅 `pro_*` 事件 + 再次 `sanitizeTelemetryEvents`
  - [x] 交互：复制证据包按钮 + 明确反馈

## 当前状态
- 状态：done
- 最近更新：2026-03-20

