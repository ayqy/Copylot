# V1-9-2 DOM 回归测试补齐（空白文本节点分隔）

## 目标
补齐可执行、可回归的 DOM 用例，覆盖“内联元素之间空白不丢失 / 块级元素之间不引入行内空格”，并确保 `bash scripts/test.sh` 全量通过。

## 具体任务
1. 新增最小测试用例 HTML：`test/cases/whitespace-between-inline.html`
   - 内联元素之间的换行缩进：期望输出包含单个空格分隔（`Hello world`）
   - 块级元素之间的换行缩进：期望保持块级分隔（不应变为同行 `Hello world`）
2. 新增期望快照：`test/snapshots/whitespace-between-inline.expected.md`
3. 更新测试清单：执行 `npm run build:tests` 生成最新 `test/test-manifest.json`
4. 验收前确保 `bash scripts/test.sh` 全量通过（lint/type-check/check-i18n/unit-tests/build:prod）

## 验收标准
- 新用例可在 `public/test/index.html` 通过 test runner 一键运行
- `test/test-manifest.json` 已包含新用例与期望值
- `bash scripts/test.sh` 全量通过

## 进度记录
- 2026-03-19：已完成（新增用例与快照；已更新 `test/test-manifest.json`；`bash scripts/test.sh` 已通过）

