# V1-35-1 拆分：全页转换正文优先（语义 main/article root）+ DOM 回归用例 + 用例/回归闭环

## 范围（MVP）
在不新增任何扩展权限、不新增任何联网发送、不改变既有“选区转换/智能区块复制/表格转换/Prompt 选区优先”等主流程行为的前提下：

- 仅当整页处理根为 `document.body` 时，尝试选择语义明确的正文容器（`main/article/[role="main"]`）作为默认根节点
- 使用“最大正文文本长度”策略，并设置最小阈值避免误选小容器
- 补齐 DOM 回归用例与用例文档闭环，确保可发布

## 子需求清单（按 MVP）
- [x] 整页处理：语义正文根节点选择（仅 root=BODY 生效）
  - [x] `src/shared/content-processor.ts`：`processContent(root, settings)` 在 `root.tagName === 'BODY'` 时选择 `main/article/[role="main"]`
  - [x] 策略：取候选中 `innerText.trim().length` 最大者；仅当 `>= 200` 才替换 root；否则回退 `document.body`
  - [x] 行为约束：不新增配置项/不新增依赖/不新增落盘字段；选区/表格/块级路径不受影响
- [x] DOM 回归用例（可执行）
  - [x] 新增：`test/cases/semantic-main-content.html`（含 header/nav/aside 噪音 + main 正文）
  - [x] 更新：`test/test-manifest.json`（期望输出仅含正文，排除噪音文本）
- [x] 用例/回归闭环
  - [x] 新增：`docs/test-cases/v1-35.md`（用例 A-D + 自动化回归记录）
  - [x] 产出：`docs/reports/v1-35-report.md`（状态/效果/修改范围/测试结论）
  - [x] 执行：`bash scripts/test.sh`：PASS（2026-03-20）

## 当前状态
- 状态：done
- 最近更新：2026-03-20

