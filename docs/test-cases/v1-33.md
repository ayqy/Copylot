# V1-33 CWS 发布流程防呆：publish:cws 强制生产回归 + 支持 dry-run 测试用例

## 目标
验证子 PRD `prds/v1-33.md` 达到可发布（不新增权限、不改变既有核心功能逻辑）的验收口径：

- `npm run publish:cws` 成为安全发布入口：任何网络调用前，强制执行 `bash scripts/test.sh` 并完成产物一致性校验与重新打包
- `npm run publish:cws -- --dry-run` 可在无 `.env` 凭据环境中完成发布前置演练：无上传/发布网络副作用；仍执行回归门禁并生成/覆盖 zip；打印缺失凭据项
- 产物一致性可审计：`dist/manifest.json` 的 `version === manifest.json.version`，且 zip 文件名版本号与之完全一致

## 用例 A（dry-run 可演练且无网络副作用）
目标：在未配置 `.env` 的环境中执行 dry-run，验证不会发生上传/发布网络调用，但会完成门禁与产物准备，并提示缺失凭据项。

步骤：
1. 运行：`npm run publish:cws -- --dry-run`

说明（本地模拟无 `.env`）：
- 若本仓库存在 `.env`，可通过如下方式模拟“无 `.env` 凭据”：
  - `env -u CWS_EXTENSION_ID -u CWS_CLIENT_ID -u CWS_CLIENT_SECRET -u CWS_REFRESH_TOKEN DOTENV_CONFIG_PATH=/tmp/does-not-exist npm run publish:cws -- --dry-run`

期望：
- 不会尝试上传/发布（无任何网络调用副作用）
- 会强制执行 `bash scripts/test.sh`
- 会生成/覆盖 `plugin-${version}.zip`
- 会打印缺失的 CWS 凭据项列表（如 `CWS_EXTENSION_ID` 等）

执行记录：
- 结果：通过
- 最近执行日期：2026-03-20
- 备注：使用 `DOTENV_CONFIG_PATH=/tmp/does-not-exist` + `env -u ...` 模拟无 `.env` 场景，脚本仍完整跑完门禁并输出缺失项提示。

## 用例 B（非 dry-run 缺少凭据时失败可审计）
目标：执行 `npm run publish:cws`（非 dry-run）但不提供任何凭据，验证会在任何网络调用前失败退出非 0，并输出缺失项列表；不得出现“上传成功/发布成功”等误导信息。

步骤：
1. 运行：`npm run publish:cws`

说明（本地模拟缺少凭据）：
- `env -u CWS_EXTENSION_ID -u CWS_CLIENT_ID -u CWS_CLIENT_SECRET -u CWS_REFRESH_TOKEN DOTENV_CONFIG_PATH=/tmp/does-not-exist npm run publish:cws`

期望：
- 在任何网络调用前退出非 0
- 输出清晰的缺失环境变量列表：`CWS_EXTENSION_ID, CWS_CLIENT_ID, CWS_CLIENT_SECRET, CWS_REFRESH_TOKEN`
- 不出现“上传成功/发布成功”等误导信息

执行记录：
- 结果：通过
- 最近执行日期：2026-03-20
- 备注：脚本在完成发布前置门禁后输出缺失项并退出非 0；未进入上传/发布逻辑。

## 用例 C（产物一致性）
目标：验证产物一致性校验与 zip 命名一致性。

检查项：
1. `dist/manifest.json` 的 `version === manifest.json.version`
2. zip 文件名为 `plugin-${version}.zip`，且 `${version}` 与上述 manifest 版本号完全一致

执行记录：
- 结果：通过
- 最近执行日期：2026-03-20
- 备注：`npm run publish:cws`/`--dry-run` 均会在打包前强校验版本一致，并按该版本号生成/覆盖 zip。

## 自动化回归记录（发布门禁审计证据）
- 统一入口：`bash scripts/test.sh`
- 最近执行日期：2026-03-20
- 结论：PASS

