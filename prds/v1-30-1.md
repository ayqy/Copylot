# V1-30-1 拆分：Prompt 工作流一致性修复（右键 Prompt 选区优先 + 选区 Prompt 自动打开 Chat + 教程/用例闭环）

## 范围（MVP）
在不新增权限、不新增任何联网发送、且不影响现有复制主流程的前提下，补齐 `prds/v1.md` 中“首次使用 Prompt -> 形成复用习惯”的关键体验一致性，使之达到可发布：

- 右键菜单 Prompt 支持“选区优先”（Selection > Page）：有选区处理选区，无选区处理整页
- 选区 Prompt 场景补齐“自动打开 Chat”（仅复制成功后触发；失败不触发；关闭开关或 Chat 服务不可用时不打开）
- 修正文档口径并补齐可执行的测试/用例闭环；自动化测试全量通过；输出可发布简报

## 子需求清单（按 MVP）
- [x] 右键菜单 Prompt 支持“选区优先”（Selection > Page）
  - [x] 更新 `src/background.ts`：ContextMenu `contexts` 同时包含 `selection` 与 `page`
  - [x] 更新 `src/background.ts`：右键点击 Prompt 时，`info.selectionText.trim()` 非空走 `PROCESS_SELECTION_WITH_PROMPT`；否则保持既有页级链路
  - [x] 保持既有 Prompt `usageCount/lastUsedAt` 更新与 `saveSettings` 逻辑不变
  - [x] 不新增权限/依赖/telemetry 事件名；不新增任何联网发送
- [x] 选区 Prompt 补齐“自动打开 Chat”（仅复制成功后触发）
  - [x] 更新 `src/content/content.ts`：扩展 `PROCESS_SELECTION_WITH_PROMPT` 分支，复制成功后在携带 `chatServiceUrl/chatServiceName` 时提示并延迟 `window.open`
  - [x] 延迟与 `PROCESS_PAGE_WITH_PROMPT_AND_CHAT` 保持一致（1500ms）
  - [x] 任意异常仅影响本次提示，不影响后续复制/Prompt 使用
- [x] 文档口径与测试/用例闭环
  - [x] 更新 `docs/tutorials/prompt-workflow.md`：将右键 Prompt 口径更新为“选区优先”，并补齐两种路径验证步骤
  - [x] 新增 `docs/test-cases/v1-30.md`：覆盖用例 A-E + 自动化回归记录
  - [x] 输出简报 `docs/reports/v1-30-report.md`
- [x] 自动化回归
  - [x] 执行 `bash scripts/test.sh` 全量通过（lint/type-check/check-i18n/unit-tests/build:prod）

## 当前状态
- 状态：done
- 最近更新：2026-03-19

