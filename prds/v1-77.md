# 问题修复和功能优化（V1-77 合规 ASO 最小迭代：关键词矩阵强化 + Listing 文案微调 + 可审计 diff 证据包）

## 目标
基于 `docs/roadmap_status.md` 的收入优先 Top3，本轮选择 Top1「（v1-77）合规 ASO 小步迭代（离线先落盘）」作为 1~3 天“最小可交付增量”主题；Top2「（v1-70）发布后回归与商店端取证补齐」与 Top3「（v1-71）Listing 同步落地 + 商店端取证」仍需要人工账号/权限/网络环境执行，保持阻塞但不影响本轮离线推进。

收入第一：通过“关键词矩阵 + 首屏转化文案”小步迭代，提升 Chrome Web Store 搜索覆盖与 Listing 首屏转化效率，并把变更固化为**可粘贴字段包 + diff 证据包 + 合规门禁 PASS**，让后续恢复网络/权限后可以直接同步上架并可复盘（避免“仓库改了但无法证明改了什么/是否合规/是否能上架”）。

## ULTRA IMPORTANT 第一原则：任务开始后，不要停下来，直到完成目标
**所有细节，都由你来定，判断依据是根据核心目标决定应该如何做，而不要停下来问我**

## 具体任务
1、ASO 关键词矩阵强化（不引入支付/订阅承诺）
  - 迭代 `docs/aso/keywords.md`：
    - 覆盖核心搜索意图：copy to markdown / table to csv / prompt template / clean copy / web page to text 等
    - EN/ZH 分组保持可维护（后续可直接复制到 CWS Keywords）
    - 严禁出现 v1-76 红线策略中的 blocked 词（付费/订阅/subscribe now 等）

2、CWS Listing EN/ZH 长描述“首屏转化”微调（对齐增长与商业化口径）
  - 迭代：
    - `docs/ChromeWebStore-Description-EN.md`
    - `docs/ChromeWebStore-Description-ZH.md`
  - 必须保持并可被证据脚本断言的关键信息存在：
    - QUICK START（3 步）可见（降低新装理解成本）
    - PRIVACY 口径可见（默认本地处理/不上传复制内容/匿名开关默认 OFF）
    - Pro 候补 CTA 可见且“仅候补/未上线”表述明确，并包含稳定链接：`docs/monetization/pro-scope.md`

3、证据与测试闭环（可审计/可复核/可执行）
  - 生成并落盘 v1-77 证据目录（文件名固定、可复算、可 diff）：
    - `docs/evidence/v1-77/`
    - 使用 `scripts/build-cws-listing-diff-evidence-pack.ts` 生成：
      - `cws-listing-evidence-pack-<extensionVersion>-<utcCompact>.json`
      - `cws-listing-diff-evidence-pack-<extensionVersion>-<utcCompact>.json`
      - `docs/evidence/v1-77/index.md`（引用 baseline/current/diff + sha256 + 关键变更摘要 + 红线断言结果）
    - 基线选择：以 `docs/evidence/v1-68/` 的 current pack 作为 baseline（反映当前已固化的 Listing 素材口径）
  - 生成“可粘贴字段包”（用于解除 v1-71 的操作摩擦）：
    - `docs/evidence/v1-77/listing-paste-pack.md`（EN 长描述 / ZH 长描述 / keywords + 3 个来源文件 sha256）
  - 新增测试用例文档（必须可逐条执行）：`docs/test-cases/v1-77.md`
    - 覆盖：更新 keywords/长描述 -> `bash scripts/test.sh`（含 v1-76 扫描门禁）-> 生成 diff 证据包 -> 复核索引与 sha256
    - 必须包含一次回归记录（日期 + PASS/BLOCKED + 证据文件名）
  - 更新 `docs/roadmap.md` 与 `docs/roadmap_status.md`：同步 v1-77 进度与 Top3（收入优先，断点续孵化不断档）

## 验收标准
* 修复上述所有问题，并测试通过，达到可发布状态
* 实现以上所有需求，并测试通过，达到可发布状态
* 可验证的商业化推进证据（必须可审计/可复核/可复盘）：
  - `docs/evidence/v1-77/index.md` 存在，且包含：
    - baseline/current/diff pack 文件名与 sha256（可追溯）
    - keywords 变更摘要（added/removed）
    - assertions/redlines 结果（例如：hasProWaitlistCta/hasPrivacyClaims/noOverclaimKeywords）
  - `docs/evidence/v1-77/listing-paste-pack.md` 存在，可直接用于 CWS Developer Dashboard 粘贴同步（不允许二次编辑）
  - `bash scripts/test.sh` 全量通过，且包含 v1-76 红线扫描门禁（确保“ASO 迭代不会把合规风险带入上架/获客入口”）
* 测试与用例可执行：
  - `docs/test-cases/v1-77.md` 可逐条执行并包含一次回归记录（日期 + PASS/BLOCKED + 证据文件名）

## 注意
1、以上任务为“最小可交付增量（1-3 天）”，收入第一：本轮只做“ASO 关键词 + Listing 首屏转化文案 + 可审计证据/门禁闭环”，不引入支付/订阅、不新增权限、不做与获客/转化路径无关的体验微调
2、合规红线：不得在文案/关键词中暗示“Pro 已上线/可订阅/可付费”；任何敏感词命中以 `docs/publish/cws-listing-redlines-policy.md` 为唯一口径并以门禁结果为准
3、一步一步来：交付过程中持续更新 `docs/roadmap.md` 与 `docs/roadmap_status.md`，确保断点续孵化不断档
4、根据需求创建测试用例文档，通过单元测试和自动化测试来确保代码的正确性（本次测试用例文档路径固定为 `docs/test-cases/v1-77.md`）
5、目标任务成功完成之后，写一份简要的汇报材料文档（固定路径：`docs/reports/v1-77-report.md`），说清楚本轮 ASO 迭代改了什么、形成了什么可审计证据、以及与 v1-71（上架同步）/v1-70（发布后取证）的衔接方式

