# V1-37-1 拆分：真实 CWS 发布（含转化入口）+ 可审计发布证据落盘

## 范围（MVP）
在不新增任何扩展权限、不改变既有复制/Prompt/引导/口碑/商业化功能逻辑的前提下：

- 以 `npm run publish:cws`（非 dry-run）完成一次真实 Chrome Web Store 上传并发布到 `default` channel
- 发布门禁必须通过：强制 `bash scripts/test.sh` + `dist/manifest.json` 版本一致性校验 + 基于 `dist/` 重新打包 zip
- 落盘可审计证据到 `docs/reports/v1-37-report.md`（脱敏日志 + CWS 后台/商店页版本号与发布时间取证）

## 子需求清单（按 MVP）
- [x] 版本号递增
  - [x] 更新 `manifest.json` 版本号为 `1.1.19`
- [x] 发布门禁演练（无网络调用）
  - [x] 执行 `npm run publish:cws -- --dry-run`，确认门禁通过并生成 `plugin-1.1.19.zip`
- [ ] 真实上传并发布（必须可复查）
  - [ ] 执行 `npm run publish:cws`（非 dry-run），upload + publish 到 `default` channel
  - [ ] CWS 后台确认版本号与发布时间并截图
  - [ ] 商店页确认版本号与更新时间并截图
- [x] 证据落盘（可审计）
  - [x] `docs/reports/v1-37-report.md` 记录日期/版本号/命令/关键日志片段与阻塞说明

## 当前状态
- 状态：blocked
- 阻塞点：当前环境无法访问 Google API（`ENOTFOUND www.googleapis.com`），导致 `npm run publish:cws` upload/publish 失败；需配置 `HTTPS_PROXY` 代理/VPN 后重试
- 最近更新：2026-03-20

