# 收费前门槛判断与可导出决策摘要

## 目标
只完成 `docs/roadmap.md` 中 `S3 Pro 路线验证` 最后一项的第一段闭环：把“进入支付/收款评估前的最小信号门槛”真正接到 `Options -> Pro`，让当前阶段能直接导出 A/B/C 决策摘要，而不是停留在文档口径。

本轮沿用 `docs/test-cases/v4-7.md`、`docs/reports/v4-7-report.md` 与 `docs/evidence/v4-7/*` 作为同编号验收产物路径；继续不新增支付、留资、候补、问卷或其他偏离复制主路径的承接。

## ULTRA IMPORTANT 第一原则：任务开始后，不要停下来，直到完成目标
**所有细节，都由你来定，判断依据是根据核心目标决定应该如何做，而不要停下来问我**

## 具体任务
1、把 `v1-81` 的收费前门槛判断纯逻辑抽成共享模块，避免只停留在脚本里。

2、在 `Options -> Pro` 新增“收费前门槛判断”区块，至少提供：
- 复制决策摘要（Markdown）
- 下载决策摘要（JSON）

3、决策摘要必须复用本地匿名问卷意向分布：
- 仍只使用 7 天聚合字段
- 仍输出 `A / B / C`
- 仍不触碰页面正文、复制内容、URL、标题或联系方式

4、同步更新 `scripts/build-pro-intent-decision-pack.ts`、`scripts/unit-tests.ts`、`scripts/ui-integration-tests.ts`、`e2e/options-pro-flow.spec.ts` 与相应本地化文案，保证共享逻辑、界面入口、zip 产物和浏览器级回归都能通过。

5、创建并维护 `docs/evidence/v4-7/*`、`docs/test-cases/v4-7.md`、`docs/reports/v4-7-report.md`，同步更新 `docs/roadmap.md`、`docs/roadmap_status.md`，明确这轮已把“收费前门槛判断”从文档口径推进成可导出的产品内能力。

## 验收标准
* `Options -> Pro` 中出现“收费前门槛判断”区块与两个动作按钮。
* 复制出的 Markdown 摘要包含 `survey_intent`、`price_*_peak`、`capability_top2` 和 `code：A/B/C`。
* 下载出的 JSON 摘要与 `v1-81` 口径一致，且不包含任何 PII。
* `docs/evidence/v4-7/*` 能复核界面入口、样例输出、隐私边界与下一步判断。
* 相关构建、单测、UI 集成测试通过；本轮最终以 `bash scripts/test.sh` 为发布级闭环。
