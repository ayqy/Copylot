# 问题修复和功能优化（V1-61 渠道获客效率证据包落盘再降摩擦：新增「下载证据包（JSON）」入口）

## 目标
基于 `docs/roadmap_status.md` 的收入优先 Top3：Top1/Top2 仍受“需要可用代理/VPN + 商店可达”的网络可达性阻塞，本轮顺延选择 Top3「渠道分发/投放跑数与复盘固化 v5」作为 1~3 天最小可交付增量主题。

收入第一：在不引入支付/订阅、不新增权限、不联网发送数据、不采集用户复制内容的前提下，把 v1-60 已交付的「获客效率证据包（JSON，含 CSV + Markdown + Env）」从“复制到剪贴板”进一步升级为**可一键下载、可直接落盘归档、可审计可复核的文件资产**——新增「下载获客效率证据包（JSON）」入口，降低每周渠道跑数后的取证/归档摩擦，让“可转化获客增长 -> 留资 -> 后续 Pro 转化验证”的商业化证据链路更稳定、更可持续。

## ULTRA IMPORTANT 第一原则：任务开始后，不要停下来，直到完成目标
**所有细节，都由你来定，判断依据是根据核心目标决定应该如何做，而不要停下来问我**

## 具体任务
1、新增「下载获客效率证据包（JSON，含 CSV + Markdown + Env）」入口（把“复制”升级为“可落盘文件”，支撑每周证据沉淀）
  - 入口位置：Options -> 隐私与可观测性 -> Pro 面板（与 v1-58/v1-59/v1-60 同区域）
  - 稳定 DOM（必须固定，便于用例/断言/回归）：`#download-pro-acquisition-efficiency-by-campaign-evidence-pack`
  - 下载内容：必须与 v1-60 `#copy-pro-acquisition-efficiency-by-campaign-evidence-pack` 复制出的 evidence pack JSON **同口径、同 schema、可互证复算一致**（字段命名/顺序稳定，便于落盘与 diff）
  - 匿名开关约束（必须与 v1-60 保持一致，避免误读）：
    - 匿名使用数据 OFF：允许下载 evidence pack，但**不得读取/不推断**本地事件；下载文件必须包含明确 OFF 提示与 Env（用于审计）
    - 匿名使用数据 ON：读取本地匿名事件日志并生成完整 evidence pack（含 CSV + Markdown + rows 互证）
  - 文件名规范（必须稳定、可单测、便于按周归档；不得包含用户输入/网页信息/复制内容）：
    - 建议：`copylot-pro-acq-eff-by-campaign-evidence-pack-YYYY-MM-DD.off.json` / `copylot-pro-acq-eff-by-campaign-evidence-pack-YYYY-MM-DD.on.json`
    - `YYYY-MM-DD` 取 `exportedAt` 的本地日期（与既有导出命名风格一致）
  - 实现建议（保持最小改动、不新增依赖/权限）：
    - 复用 v1-60 的构建逻辑与格式化器：`buildProAcquisitionEfficiencyByCampaignEvidencePackForClipboard()` + `formatProAcquisitionEfficiencyByCampaignEvidencePackAsJson(...)`
    - 在 `src/options/options.ts` 增加通用 `downloadTextFile(filename, text, mimeType)`（复用既有 `downloadCsvFile(...)` 的 a 标签下载方式）
    - 将“文件名生成”做成可单测纯函数（建议新增 `src/shared/pro-acquisition-efficiency-by-campaign-evidence-pack-filename.ts` 或扩展既有 shared 模块），并在 `scripts/unit-tests.ts` 覆盖
    - 补齐 i18n：按钮文案 + 下载成功/失败通知（`_locales/en/messages.json`、`_locales/zh/messages.json`）

2、补齐自动化测试 + 可执行用例（保证 1~3 天可交付、可回归、可审计）
  - 自动化测试加固（优先纯函数 + `scripts/unit-tests.ts`）：
    - 文件名纯函数：OFF/ON 后缀正确；日期格式稳定；不得包含 campaign 等用户输入
    - OFF 行为：不得读取 events；下载内容必须包含 OFF 提示 + Env；rows 为空；资产为空占位
    - ON 行为：至少覆盖 2 个 campaign + 空 campaign；排序稳定；`distCopies=0 -> N/A`；小数固定 4 位；与 v1-58/v1-59/v1-60 互证口径一致
    - 等价性断言：同一输入下，“下载 JSON”与“复制 JSON”输出必须一致（允许末尾换行差异，但 JSON parse 后字段值必须一致）
  - 新增测试用例文档（固定路径，必须在 PRD 中明确）：`docs/test-cases/v1-61.md`
    - 覆盖：入口存在（DOM 稳定）；匿名 OFF/ON 两态均可下载；下载文件可被 JSON.parse；并与 v1-60 复制输出互证一致；包含一次回归记录（日期 + PASS）
    - 必须要求 `bash scripts/test.sh` 全量通过

3、证据落盘与进度看板更新（让商业化推进“做完可证明”，并可持续复用）
  - 证据目录（固定路径）：`docs/evidence/v1-61/` + `docs/evidence/v1-61/index.md`
    - 至少落盘（脱敏、可审计、可复核）：
      - `copylot-pro-acq-eff-by-campaign-evidence-pack-YYYY-MM-DD.off.json`：匿名 OFF 下载样例（仅 Env + OFF 提示 + 空资产占位）
      - `copylot-pro-acq-eff-by-campaign-evidence-pack-YYYY-MM-DD.on.json`：匿名 ON 下载样例（含至少 2 个 campaign + 空 campaign 行；含 CSV + Markdown + rows 互证）
      - 截图索引：入口位置 + 点击下载后的文件落地证据（文件名可见）
      - 互证步骤：如何用下载文件中的 CSV/Markdown/rows 复算并核对 leadsPerDistCopy（避免“做完不可证明”）
  - 更新 `docs/roadmap_status.md`：新增 v1-61 条目（或在当前进度中补齐本轮主题），刷新“当前进度/Top3/阻塞”描述（断点续孵化不断档）

## 验收标准
* 修复上述所有问题，并测试通过，达到可发布状态
* 实现以上所有需求，并测试通过，达到可发布状态
* 可验证的商业化推进证据（必须可审计/可复核/可复用）：
  - Options -> 隐私与可观测性 -> Pro 面板存在入口，且稳定 DOM 为 `#download-pro-acquisition-efficiency-by-campaign-evidence-pack`
  - 匿名开关 OFF：可下载 evidence pack JSON，且明确提示“匿名使用数据关闭（不读取/不推断事件）”，并包含 Env（window/版本/时间戳）
  - 匿名开关 ON：可下载 evidence pack JSON，且至少包含（1）v1-58 同口径 7d CSV（2）v1-59 同口径周报 Markdown（3）结构化 rows，可与 CSV/Markdown 互证复算一致（`distCopies=0 -> N/A`）
  - 文件命名稳定可归档：下载文件名符合约定格式，且不包含 campaign/URL/标题/网页内容/复制内容等敏感信息
  - 证据落盘可审计：`docs/evidence/v1-61/index.md` 至少包含（1）OFF/ON 下载样例 JSON（2）截图索引（3）互证步骤，可支撑“按 campaign 的获客效率复盘与审计”（作为真实收入回报路径的可验证证据资产）
* 测试与用例可执行：
  - `docs/test-cases/v1-61.md` 可逐条执行并包含一次回归记录（日期 + PASS），且 `bash scripts/test.sh` 全量通过

## 注意
1、以上任务为“最小可交付增量（1-3 天）”，收入第一：本轮只做“获客效率证据包下载落盘 + 用例/证据/测试闭环”，不引入支付/订阅、不新增权限、不做与商业化取证无关的体验微调
2、保持口径兼容：不得修改 v1-58/v1-59/v1-60 的既有导出字段/文件名/统计口径；本轮只新增“下载入口”与“稳定文件名”，以便每周归档与审计
3、根据需求创建测试用例文档，通过单元测试和自动化测试来确保代码的正确性（本次测试用例文档路径固定为 `docs/test-cases/v1-61.md`）
4、目标任务成功完成之后，写一份简要的汇报材料文档（固定路径：`docs/reports/v1-61-report.md`），说清楚每一个具体任务现在是什么状态（是否按要求完成了），已经实现了什么效果，以及本次对代码的修改范围（涉及哪些目录和文件）
5、隐私与合规红线：不得新增扩展权限；不得上传/采集用户复制内容；OFF 时不读取/不推断本地事件；ON 时仅基于本地匿名事件日志派生（字段白名单）；campaign 仅来源于用户手动输入/本地设置且需通过既有校验（不得包含敏感信息）

