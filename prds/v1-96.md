# 问题修复和功能优化（V1-96 收入回填最小增量：转化入口参数统一与漏斗证据补齐）

## 目标
在 Top1（S0 商店端真实取证）受“CWS 权限 + 代理未启动”阻塞的前提下，顺延执行 `docs/roadmap_status.md` Top2：以 1-3 天最小范围完成官网/CWS/Pro 候补入口参数统一、转化证据索引可导出、意向漏斗可复盘，确保本轮交付可直接推动真实收入回报路径（可转化获客增长与付费意向沉淀）。

## ULTRA IMPORTANT 第一原则：任务开始后，不要停下来，直到完成目标
**所有细节，都由你来定，判断依据是根据核心目标决定应该如何做，而不要停下来问我**

## 具体任务
1、转化入口参数统一（官网 / CWS / Pro 候补）
  - 统一并固化三类入口的 `campaign/source/medium` 参数命名规则，避免同一流量在漏斗中被拆散。
  - 更新并对齐入口资产：`docs/growth/assets/social/xhs/v1-96/cta-links.md`、`docs/growth/assets/social/xhs/v1-96/conversion-evidence-index.json`、`docs/growth/executions/v1-96-growth-regression.md`。
  - 明确“入口 -> 目标页 -> 期望行为（安装/点击升级/提交意向）”映射，保证每个入口都可被复核到商业化漏斗节点。

2、收入证据闭环补齐（可导出 / 可审计）
  - 以不依赖 CWS Dashboard 权限为前提，补齐本地可导出的转化证据索引：至少包含 `channel`、`postUrl`、`targetUrl`、`campaign`、`source`、`intentSignal`、`evidencePath` 字段。
  - 将本轮证据索引与 Pro 意向证据包口径对齐，确保可从 `conversion-evidence-index` 追溯到对应漏斗数据与留资信号。
  - 产出本轮汇报文档：`docs/reports/v1-96-report.md`，明确本轮对“获客可转化性/意向沉淀”的量化推进与证据位置。

3、测试用例与执行门禁（1-3 天可完成）
  - 创建并维护测试用例文档：`docs/test-cases/v1-96.md`。
  - 用例至少包含：
    - 用例 A：三类入口参数一致性校验（官网/CWS/Pro 候补）。
    - 用例 B：`conversion-evidence-index` 字段完整性与可追溯性校验。
    - 用例 C：阻塞分流校验（Top1 阻塞已记录后，Top2 交付仍可独立闭环）。
  - 执行 `bash scripts/test.sh`，并在用例文档记录一次回归结论（PASS/BLOCKED + 证据文件名）。

## 验收标准
* 修复上述所有问题，并测试通过，达到可发布状态
* 实现以上所有需求，并测试通过，达到可发布状态
* 可验证的商业化推进证据（必须可审计/可复盘）：
  - `docs/growth/assets/social/xhs/v1-96/conversion-evidence-index.json` 可导出并包含统一参数口径，且每条记录可追溯到入口链接与证据文件。
  - 官网/CWS/Pro 候补三类入口在证据索引中的 `campaign/source` 命名一致，不存在同源流量多口径拆分。
  - 至少可复核一个“分发入口 -> 转化动作 -> 意向信号”的完整链路（含链接、口径字段、证据路径）。
* 阻塞处理符合顺延规则：
  - `docs/roadmap_status.md` 明确记录 Top1 阻塞原因为“CWS 权限 + 代理未启动（`source ~/.bash_profile && pxy`）”。
  - 本轮主题与交付内容严格对应 Top2，并在阻塞解除后可无缝回切 Top1。
* 测试与用例可执行：
  - `docs/test-cases/v1-96.md` 提供可复现步骤、预期结果、回归记录（PASS/BLOCKED + 证据文件名）。
  - `bash scripts/test.sh` 通过，且本轮不引入与收入主链路无关的功能扩展。

## 注意
1、以上任务规模非常大，每一个都很有挑战性，弄清楚需求后你应该创建新的需求文档来记录每个子需求，例如x-1.md、x-2.md等等（x表示当前的需求代号v几中的几）
2、一步一步来，对于每个阶段，边执行边更新对应文档中的进度
3、根据需求创建测试用例文档，通过单元测试和自动化测试来确保代码的正确性（本轮固定路径：`docs/test-cases/v1-96.md`）
4、目标任务成功完成之后，写一份简要的汇报材料文档（用于口头汇报，所以要简明扼要抓住重点），说清楚每一个具体任务现在是什么状态（是否按要求完成了），已经实现了什么效果，以及本次对代码的修改范围（涉及哪些目录和文件）（本轮固定路径：`docs/reports/v1-96-report.md`）
5、收入第一：本轮仅交付可直接推动“可转化获客增长/意向留资/后续收款链路可信度”的最小增量；若遇到账号、权限或代理阻塞，必须先记录阻塞并按 `docs/roadmap_status.md` 顺延执行下一优先事项
