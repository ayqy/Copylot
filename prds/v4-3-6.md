# v4-3-6 测试、证据、路线图与交付闭环

## 目标

把跨 campaign 复核里程碑的自动化测试、证据、roadmap 与简报全部落盘，形成可交付闭环。

## 状态

- [x] 新增/更新 `scripts/unit-tests.ts`、`scripts/ui-integration-tests.ts`、`e2e/options-pro-flow.spec.ts` 与 `scripts/test.ts`。
- [x] 新增 `docs/test-cases/v4-13.md` 与 `docs/evidence/v4-13/*`。
- [x] 更新 `docs/roadmap.md`、`docs/roadmap_status.md` 与必要阻塞文档。
- [ ] commit / push 结果待本轮验证与发布动作完成后回写。

## 具体任务

1. 覆盖 `supporting / conflicting / thin / no_signals` 四类分支，以及复制、下载、边界与隐私断言。
2. 落盘 `docs/evidence/v4-13/*`、`docs/reports/v4-13-report.md`，说明商业化推进证据与当前阻塞。
3. 通过 `bash scripts/test.sh`，并在测试通过后回写 commit / push 状态。
