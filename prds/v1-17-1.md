# V1-17-1 拆分：Prompt 管理器排序能力（最常用 / 最近使用）+ 修正“最近使用”时间显示

## 范围（MVP）
在不新增权限、不新增第三方依赖、不新增任何联网能力、且不改变 Prompt 数据结构语义的前提下，为 Options 的“私人 Prompt 管理器”补齐可切换排序（默认/最常用/最近使用），并修正卡片底部“最近使用”时间误用 `createdAt` 的问题（改为基于 `lastUsedAt`，未使用则明确占位）。

## 子需求清单（按 MVP）
- [x] Options UI：Prompts Tab 工具条新增排序下拉框（稳定 DOM + i18n）
  - [x] `src/options/options.html`：在 `.toolbar .filter-options` 内新增 `#prompt-sort-select`（紧邻 `#category-filter`）
  - [x] `_locales/en/messages.json` / `_locales/zh/messages.json`：补齐排序控件与选项文案
  - [x] `npm run check-i18n` 通过
- [x] 排序逻辑落地（只影响渲染顺序，不改写存储顺序）
  - [x] `src/shared/prompt-sort.ts`：抽出纯函数 `sortPrompts(prompts, mode)` 与 `parsePromptSortMode(...)`
  - [x] `src/options/options.ts`：新增状态 `promptSortMode`，`#prompt-sort-select` 切换后触发重新渲染；排序仅对过滤结果做拷贝后排序
  - [x] 选择记忆：localStorage（key：`copylot_prompt_sort_mode`）
- [x] 最近使用时间显示修正
  - [x] `src/options/options.ts`：`createPromptCard(...)` 使用 `lastUsedAt`；无值显示 i18n `promptNeverUsed`
- [x] 单测 + 用例文档 + 可发布交付
  - [x] `scripts/unit-tests.ts`：覆盖 `most_used/recent_used/default` 排序规则与非法值兜底
  - [x] `docs/test-cases/v1-17.md`：用例 A-E + `bash scripts/test.sh` 执行记录
  - [x] `docs/reports/v1-17-report.md`：发布简报
  - [x] `bash scripts/test.sh` 全量通过

## 当前状态
- 状态：done
- 最近更新：2026-03-19

