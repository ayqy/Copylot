# V1-10-3 回归资产与用例文档补齐（表格内/外选区）

## 目标
提供“1 分钟可复现 + 可长期维护”的回归资产，并补齐可执行的用例文档，确保修复可验证、可回归、可发布。

## 具体任务
1. 新增可复现页面：`test/cases/table-selection-inside-table.html`
   - 2x2 以上表格
   - 单元格内包含内联元素（`<a>/<code>/<strong>/<em>`）
   - 表格前后包含普通段落（用于验证表格内/外选区差异）
2. 新增快照：`test/snapshots/table-selection-inside-table.expected.md`
   - 供 DOM test runner 以整页 `processContent()` 做回归
3. 更新测试清单：运行 `npm run build:tests` 生成最新 `test/test-manifest.json`
4. 新增用例文档：`docs/test-cases/v1-10.md`（覆盖手工与自动化回归步骤，并记录结论）

## 验收标准
- 新用例与快照落地，且 `npm run build:tests` 已更新 `test/test-manifest.json`
- `bash scripts/test.sh` 全量通过
- `docs/test-cases/v1-10.md` 可执行、可回归，并记录结论

## 进度记录
- 2026-03-19：已完成（新增用例/快照/manifest 更新；测试入口通过；用例文档已补齐）

