# 问题修复和功能优化（V1-97 并行增长循环真实发布回填与收入证据闭环）

## 目标
实现具体任务中要求的所有功能和问题修复（在 Top1「CWS 权限 + `pxy` 代理」阻塞未解除时，顺延执行 `docs/roadmap_status.md` Top2），使“分发 -> 转化 -> 意向”链路具备真实发布回填数据与可审计证据，达到可验证商业化推进的验收标准。

## ULTRA IMPORTANT 第一原则：任务开始后，不要停下来，直到完成目标
**所有细节，都由你来定，判断依据是根据核心目标决定应该如何做，而不要停下来问我**

## 具体任务
1、真实发布回填最小闭环（收入优先，1-3 天内完成）
  - 按 `docs/growth/checklists/manual-posting-xhs-v1-96.md` 完成至少 1 轮真实发布回填，优先回填可直接导向收入路径的三个入口（官网/CWS/Pro 候补）。
  - 更新 `docs/growth/assets/social/xhs/v1-96/conversion-evidence-index.json`：将 `conversionEntries[*].postUrl` 与 `auditChainSamples[*].postUrl` 从 `manual_post_pending` 回填为真实帖子 URL，并补齐对应证据路径。
  - 新增执行记录 `docs/growth/executions/v1-97-growth-backfill.md`，记录本轮真实回填动作、入口映射、证据索引与回填结果。

2、收入证据口径复核（分发 -> 转化 -> 意向）
  - 以 `docs/growth/assets/social/xhs/v1-96/conversion-evidence-index.json` 为主索引，对齐 `docs/evidence/v1-90/pro-intent-run-evidence-pack.json` 的意向口径，形成至少 1 条可复盘链路样例。
  - 在 `docs/evidence/v1-97/` 落盘本轮证据（帖子截图索引、发布回填记录、关键指标快照），并在执行记录中标注每个证据文件路径。
  - 输出汇报材料 `docs/reports/v1-97-report.md`，明确“真实发布回填对可转化获客与意向沉淀”的量化结果与口径说明。

3、测试用例与门禁（必须可执行）
  - 新增测试用例文档 `docs/test-cases/v1-97.md`，至少包含：
    - 用例 A：真实发布回填校验（`postUrl` 非 `manual_post_pending`，入口参数 `campaign/source/medium` 一致）。
    - 用例 B：证据链路校验（可从帖子 URL 追溯到转化入口与 `pro-intent-run-evidence-pack`）。
    - 用例 C：阻塞分流校验（若遇账号/风控阻塞，`docs/growth/blocked.md`、`docs/growth/todo.md`、执行记录三方一致）。
  - 执行 `bash scripts/test.sh` 并将结果写入 `docs/evidence/v1-97/test.sh.log`，在 `docs/test-cases/v1-97.md` 记录 PASS/BLOCKED 与证据文件名。

## 验收标准
* 修复上述所有问题，并测试通过，达到可发布状态
* 实现以上所有需求，并测试通过，达到可发布状态
* 可验证的商业化推进证据（必须可审计/可复盘）：
  - `docs/growth/assets/social/xhs/v1-96/conversion-evidence-index.json` 中至少 1 条记录的 `postUrl` 为真实可访问帖子 URL，且可追溯到对应 `targetUrl` 与 `intentSignal`。
  - `docs/growth/executions/v1-97-growth-backfill.md` 明确记录至少 1 组可量化字段（`clicks`、`installs`、`proIntentSignals`），并附证据路径。
  - 至少 1 条“分发 -> 转化 -> 意向”链路样例可复核到 `docs/evidence/v1-90/pro-intent-run-evidence-pack.json`。
* 顺延机制符合规则：
  - `docs/roadmap_status.md` 明确记录 Top1 阻塞为“CWS 权限 + `source ~/.bash_profile && pxy` 未就绪”，并标明本轮顺延执行 Top2（v1-97）。
* 测试与用例可执行：
  - `docs/test-cases/v1-97.md` 提供可复现步骤、预期结果与回归记录（PASS/BLOCKED + 证据文件名）。
  - `bash scripts/test.sh` 执行完成并有日志落盘，不引入与收入主链路无关的大范围功能改动。

## 注意
1、以上任务规模非常大，每一个都很有挑战性，弄清楚需求后你应该创建新的需求文档来记录每个子需求，例如 `v1-97-1.md`、`v1-97-2.md` 等等（按需拆分，保持 1-3 天可交付）
2、一步一步来，对于每个阶段，边执行边更新对应文档中的进度（至少同步 `docs/roadmap.md`、`docs/roadmap_status.md`、`docs/growth/executions/v1-97-growth-backfill.md`）
3、根据需求创建测试用例文档，通过单元测试和自动化测试来确保代码的正确性（固定路径：`docs/test-cases/v1-97.md`）
4、目标任务成功完成之后，写一份简要的汇报材料文档（用于口头汇报，所以要简明扼要抓住重点），说清楚每一个具体任务现在是什么状态（是否按要求完成了），已经实现了什么效果，以及本次对代码的修改范围（涉及哪些目录和文件）（固定路径：`docs/reports/v1-97-report.md`）
5、收入第一：除阻塞上架/收款/转化漏斗的关键问题外，不处理纯体验细节；若真实发布遇账号/验证码/风控阻塞，必须先写入 `docs/growth/blocked.md` 并执行 `make todo`，随后继续完成可离线复核的证据补齐
