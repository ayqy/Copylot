# 问题修复和功能优化（V1-27 增长内容资产补齐：代码块专业级清理教程 + README 入口 + 用例闭环）

## 目标
在不新增权限、不引入任何联网发送、且不改动核心复制/Prompt/引导主流程的前提下，补齐 `prds/v1.md` 中“增长运营资产与商店转化优化（ASO）-> 示例与教程内容库”的缺口：基于现有“代码块悬停复制 + 保守清理”实现（参考：`src/shared/content-processor.ts`、`src/shared/code-block-cleaner.ts`）产出 1 篇可对外发布且口径真实可验证的教程（代码块专业级清理：保留缩进/去噪/避免误删），并在 `README.md` 增加稳定入口，同时补齐可执行的测试/用例文档与一次自动化回归记录，使之达到验收标准、可发布。

## ULTRA IMPORTANT 第一原则：任务开始后，不要停下来，直到完成目标
**所有细节，都由你来定，判断依据是根据核心目标决定应该如何做，而不要停下来问我**

## 具体任务
1、按教程模板产出“代码块专业级清理”教程（面向开发者激活与口碑传播）
  - 新增并维护文档：`docs/tutorials/code-block-cleaning.md`
  - 教程必须严格按 `docs/tutorials/_tutorial-template.md` 结构编写，并满足：
    - 目标导向：强调“3 分钟内完成一次可直接运行/可直接发给 AI 的干净代码复制”的最短路径（对齐 `prds/v1.md` 的 Activation 目标）
    - 可复现：提供至少 2 个稳定可访问的示例页面（主/备），并写明为何选择（例如：MDN/W3Schools/GitHub 文档页等）
    - 覆盖至少 2 条最短可行路径（都要写清楚前置开关与入口名称，避免用户找不到）：
      - 路径 A（代码块悬停复制）：开启 `Enable Hover Trigger`，在 `<pre>/<code>` 上悬停出现 Copylot 按钮并点击复制
      - 路径 B（块级/页级转换后复制代码）：通过 Magic Copy 提取包含代码块的内容（说明该路径与路径 A 的范围差异与边界）
    - 必须说明“清理能力边界”（不得夸大，且需与实现一致）：
      - Copy 按钮/文案噪音：若此类文案被渲染进代码文本，Copylot 仅做**首/末端整行**的保守移除（参考 `src/shared/code-block-cleaner.ts`）
      - 行号：仅在代码块 DOM 结构可识别为行号/代码分列时才能去除（例如 `ol.hljs-ln` 结构，参考 `src/shared/content-processor.ts`），不承诺覆盖所有站点
      - 缩进与空行：保留所有内部空行与缩进，仅裁剪首尾空行（参考 `extractCodeBlockText()` 的处理）
      - 反转义：对少量 Markdown 转义字符做保守反转义（如 `\\#`、`\\[`），并说明适用范围与限制
    - Expected Results：给出最小可审计示例片段（复制到剪贴板的文本样式），并说明粘贴到至少 2 个目标（如：VS Code/任意纯文本编辑器 + 任意 Chat 输入框）时的预期表现
    - FAQ（至少 5 条）：覆盖“看不到悬浮按钮/复制结果仍包含行号/复制结果仍包含 Copy 文案/站点使用 Shadow DOM 或编辑器导致不可触发/与 Markdown 格式或 Extra 开关冲突”等
    - 隐私口径：必须与 `docs/privacy-policy.md` / `docs/aso/value-prop.md` 一致（本地处理、不上传复制内容；匿名使用数据默认关闭且仅本地记录，不联网发送）
  - 约束：
    - 所有描述必须与仓库当前实现一致（可用 `README.md`、`docs/roadmap.md`、`manifest.json`、`src/shared/content-processor.ts`、`src/shared/code-block-cleaner.ts` 作佐证）
    - 禁止写“命令行提示符自动移除/语义理解清洗/联网分析/云端上传”等未实现能力；禁止任何高打扰引导（强制弹窗/强制评价）

2、对外口径最小纠偏 + README 最小联动：补齐教程入口并确保不夸大
  - 修改：`README.md`
  - 在“【代码块专业级清理】—— 开发者必备”功能介绍段落中新增“教程链接”（指向 `docs/tutorials/code-block-cleaning.md`）
  - 若该段落存在未实现能力描述（例如“命令行提示符自动移除/覆盖所有站点行号清理”），需做**最小必要文案收敛**以与实现一致（参考：`src/shared/content-processor.ts`、`src/shared/code-block-cleaner.ts`）
  - 修改：`docs/aso/value-prop.md`
  - 将“代码块专业清理”差异化卖点描述收敛为可审计口径（不得承诺“提示符自动移除/所有站点去行号”这类不可验证表述）
  - 约束：仅做口径纠偏与新增链接，不改动其它段落结构；链接路径必须在 GitHub 渲染下可直接打开

3、测试/用例与发布闭环（可执行、可回归）
  - 新增并维护测试/用例文档：`docs/test-cases/v1-27.md`
  - 文档必须覆盖（至少）：
    - 用例 A（教程结构可审计）：`docs/tutorials/code-block-cleaning.md` 严格按 `docs/tutorials/_tutorial-template.md` 的结构要求编写（标题/TL;DR/前置条件/步骤/期望结果/FAQ/发布素材/审计清单齐全；一级小节均含 ZH/EN）
    - 用例 B（路径 A 可复现）：按教程在示例页面完成一次“代码块悬停复制”，分别粘贴到纯文本编辑器与任意 Chat 输入框，记录是否符合预期（缩进/空行保留；无多余 Copy 文案；如示例包含行号则应被去除）
    - 用例 C（路径 B 可复现）：按教程通过 Magic Copy 的块级/页级路径完成一次包含代码块的复制，并记录与路径 A 的范围差异是否与教程描述一致
    - 用例 D（口径一致性）：抽检教程中的 3 条对外文案，确认与 `README.md` / `docs/aso/value-prop.md` / `docs/privacy-policy.md` 不冲突且不夸大（尤其“去行号/去 Copy 文案/不联网发送”）
    - 用例 E（口径纠偏生效）：检查 `README.md` 与 `docs/aso/value-prop.md` 中与“代码块清理”相关表述不包含未实现能力（例如“命令行提示符自动移除/所有站点都能去行号”），且与代码实现可佐证
    - 用例 F（链接可用）：`README.md` 中教程链接可打开且路径正确
    - 自动化回归：记录一次 `bash scripts/test.sh` 结论（命令 + 通过/失败）

## 验收标准
* 修复上述所有问题，并测试通过，达到可发布状态
* 实现以上所有需求，并测试通过，达到可发布状态
* 已新增并可对外发布的增长内容资产：`docs/tutorials/code-block-cleaning.md`（按模板结构、可复现、口径真实可验证、含发布素材与隐私口径）
* `README.md` 已新增稳定教程入口链接，GitHub 渲染可直接打开且不影响其它功能描述
* `README.md` / `docs/aso/value-prop.md` 中关于“代码块专业清理”的对外文案已收敛为可审计口径（不夸大、与实现一致）
* 已新增 `docs/test-cases/v1-27.md` 且可执行；按文档完成用例 A-F 并记录结论；`bash scripts/test.sh` 全量通过，可打包发布

## 注意
1、以上任务规模非常大，每一个都很有挑战性，弄清楚需求后你应该创建新的需求文档来记录每个子需求，例如x-1.md、x-2.md等等（x表示当前的需求代号v几中的几）
2、一步一步来，对于每个阶段，边执行边更新对应文档中的进度
3、根据需求创建测试用例文档，通过单元测试和自动化测试来确保代码的正确性
4、目标任务成功完成之后，写一份简要的汇报材料文档（用于口头汇报，所以要简明扼要抓住重点），说清楚每一个具体任务现在是什么状态（是否按要求完成了），已经实现了什么效果，以及本次对代码的修改范围（涉及哪些目录和文件）
5、本次为“最小可交付增量（1-3 天）”：仅新增“代码块专业级清理”教程 + README 入口 + 用例闭环；原则上不新增权限、不新增依赖、不新增任何联网发送、不改动核心复制/Prompt/引导逻辑。测试用例文档路径固定为 `docs/test-cases/v1-27.md`，汇报材料路径建议为 `docs/reports/v1-27-report.md`。
