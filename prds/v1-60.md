# 问题修复和功能优化（V1-60 渠道获客效率取证再降摩擦：一键复制“获客效率证据包”（JSON，含 CSV + Markdown + Env））

## 目标
基于 `docs/roadmap_status.md` 的收入优先 Top3：Top1/Top2 仍受“需要可用代理/VPN + 商店可达”的网络可达性阻塞，本轮顺延选择 Top3「渠道分发/投放跑数与复盘固化 v5」作为 1~3 天最小可交付增量主题。

收入第一：在不引入支付/订阅、不新增权限、不联网发送数据、不采集用户复制内容的前提下，把 v1-58（7d 合并 CSV）+ v1-59（周报 Markdown）进一步升级为**可直接落盘归档、可审计可复核、可复用的“证据包”资产**——新增「获客效率证据包（JSON）」一键复制入口，将 CSV/Markdown/Env/口径说明统一打包，降低“手工归档/丢证据/不可复核”的商业化风险，持续推进真实收入回报路径（可转化获客增长 -> 留资 -> 后续 Pro 转化验证）。

## ULTRA IMPORTANT 第一原则：任务开始后，不要停下来，直到完成目标
**所有细节，都由你来定，判断依据是根据核心目标决定应该如何做，而不要停下来问我**

## 具体任务
1、新增「复制获客效率证据包（JSON，含 CSV + Markdown + Env）」入口（把可审计资产从“零散导出”升级为“一键落盘”）
  - 入口位置：Options -> 隐私与可观测性 -> Pro 面板（与 v1-58/v1-59 同区域）
  - 稳定 DOM（必须固定，便于用例/断言/回归）：`#copy-pro-acquisition-efficiency-by-campaign-evidence-pack`
  - 匿名开关约束（必须与 v1-51/v1-58/v1-59 保持一致，避免误读）：
    - 匿名使用数据 OFF：允许生成证据包，但**不得读取/不推断**本地事件；证据包必须包含明确的 OFF 提示与 Env（用于审计）
    - 匿名使用数据 ON：读取本地匿名事件日志并生成证据包
  - 口径与互证要求（必须“做完可证明”）：
    - 证据包必须内含：v1-58 同口径的 `csv`（7d，按 campaign）+ v1-59 同口径的 `weeklyReportMarkdown`（按 campaign）
    - 必须内含 Env：`extensionVersion/exportedAt/lookbackDays/windowFrom/windowTo/isAnonymousUsageDataEnabled`
    - 必须提供互证字段：表格 rows（或等价结构化字段）可与 CSV/Markdown 表格互证复算；`distCopies=0 -> N/A`；小数格式与 v1-58 一致（4 位）
  - 实现建议（必须可单测的纯函数，避免在 UI 层拼口径）：
    - 新增 `src/shared/pro-acquisition-efficiency-by-campaign-evidence-pack.ts`
      - `buildProAcquisitionEfficiencyByCampaignEvidencePack(...)`（复用 v1-58/v1-59 的 window/rows 口径）
      - `formatProAcquisitionEfficiencyByCampaignEvidencePackAsJson(...)`（输出稳定 JSON，便于落盘归档与 diff）
    - Options 侧新增 `copyProAcquisitionEfficiencyByCampaignEvidencePackToClipboard()`，并接入 i18n 成功/失败提示

2、补齐自动化测试 + 可执行用例（保证 1~3 天可交付、可回归、可审计）
  - 自动化测试加固（优先纯函数 + `scripts/unit-tests.ts`）：
    - 证据包 JSON schema 稳定性：必填字段齐全；字段顺序/命名稳定；csv/markdown 非空时包含稳定 header/title
    - 7d window 边界一致性：与 v1-58/v1-59 在同一 `now` 下 windowFrom/windowTo 必须一致（便于互证）
    - OFF 行为：`enabled=false` 且不得读取 events；证据包必须包含 OFF 提示 + Env
    - ON 行为：至少覆盖 2 个 campaign + 空 campaign；排序稳定；`distCopies=0 -> N/A`；小数格式稳定（4 位）
    - 互证断言：证据包内 rows 与 v1-58 CSV rows、v1-59 Markdown 表格数据可互证复算一致
  - 新增测试用例文档（固定路径，必须在 PRD 中明确）：`docs/test-cases/v1-60.md`
    - 覆盖：匿名 OFF/ON；点击 `#copy-pro-acquisition-efficiency-by-campaign-evidence-pack` 复制成功；把证据包中的 CSV/Markdown 与现有 v1-58/v1-59 入口互证；并包含一次回归记录（日期 + PASS）
    - 必须要求 `bash scripts/test.sh` 全量通过

3、证据落盘与进度看板更新（让商业化推进“做完可证明”，且可持续复用）
  - 证据目录（固定路径）：`docs/evidence/v1-60/` + `docs/evidence/v1-60/index.md`
    - 至少落盘（脱敏、可审计、可复核）：
      - `pro-acq-eff-by-campaign-evidence-pack.off.json`：匿名 OFF 示例（仅 Env + OFF 提示 + 空资产占位）
      - `pro-acq-eff-by-campaign-evidence-pack.on.json`：匿名 ON 示例（含至少 2 个 campaign + 空 campaign 行）
      - 截图索引：按钮入口 + 复制成功通知（可选补齐匿名 OFF/ON 两态）
      - 互证步骤：如何从证据包中的 CSV/Markdown 复算并核对 leadsPerDistCopy（避免“做完不可证明”）
  - 更新 `docs/roadmap_status.md`：新增 v1-60 里程碑条目，并刷新“当前进度/Top3/阻塞”描述（断点续孵化不断档）

## 验收标准
* 修复上述所有问题，并测试通过，达到可发布状态
* 实现以上所有需求，并测试通过，达到可发布状态
* 可验证的商业化推进证据（必须可审计/可复核/可复用）：
  - Options -> 隐私与可观测性 -> Pro 面板存在入口，且稳定 DOM 为 `#copy-pro-acquisition-efficiency-by-campaign-evidence-pack`
  - 匿名开关 OFF：可复制证据包 JSON，且明确提示“匿名使用数据关闭（不读取/不推断事件）”，并包含 Env（window/版本/时间戳）
  - 匿名开关 ON：可复制证据包 JSON，且至少包含（1）v1-58 同口径 7d CSV（2）v1-59 同口径周报 Markdown（3）结构化 rows，可与 CSV/Markdown 互证复算一致（`distCopies=0 -> N/A`）
  - 证据落盘可审计：`docs/evidence/v1-60/index.md` 至少包含（1）OFF/ON 示例 JSON（2）截图索引（3）互证步骤，可支撑“按 campaign 的获客效率复盘与审计”
* 测试与用例可执行：
  - `docs/test-cases/v1-60.md` 可逐条执行并包含一次回归记录（日期 + PASS），且 `bash scripts/test.sh` 全量通过

## 注意
1、以上任务为“最小可交付增量（1-3 天）”，收入第一：本轮只做“获客效率证据包（JSON）一键复制 + 用例/证据/测试闭环”，不引入支付/订阅、不新增权限、不做与商业化取证无关的体验微调
2、保持口径兼容：不得修改 v1-54/v1-57/v1-58/v1-59 的既有导出字段/文件名/统计口径；本轮只新增 evidence pack 作为归档与审计加速器
3、根据需求创建测试用例文档，通过单元测试和自动化测试来确保代码的正确性（本次测试用例文档路径固定为 `docs/test-cases/v1-60.md`）
4、目标任务成功完成之后，写一份简要的汇报材料文档（固定路径：`docs/reports/v1-60-report.md`），说清楚每一个具体任务现在是什么状态（是否按要求完成了），已经实现了什么效果，以及本次对代码的修改范围（涉及哪些目录和文件）
5、隐私与合规红线：不得新增扩展权限；不得上传/采集用户复制内容；OFF 时不读取/不推断本地事件；ON 时仅基于本地匿名事件日志派生（字段白名单）；campaign 仅来源于用户手动输入/本地设置且需通过既有校验（不得包含敏感信息）

