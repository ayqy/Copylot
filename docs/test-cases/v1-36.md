# V1-36 Pro 候补转化闭环：商店页 CTA + Popup 复制候补文案 + 可导出意向证据 测试用例

## 目标
验证子 PRD `prds/v1-36.md` 达到可发布验收口径：

- 商店页（长描述）已补齐“Pro 候补/Planned”CTA（不夸大、可审计），并提供稳定的 Pro 范围/边界链接
- Popup 已补齐“复制候补文案”入口：复制内容为候补模板（仅环境信息 + 占位提示），不包含任何网页内容/URL/标题/剪贴板历史
- 若开启“匿名使用数据”（默认关闭）：复制成功会记录本地匿名事件 `pro_waitlist_copied` 且 `props.source === 'popup'`，并可在 Options -> 隐私面板导出审计
- 若关闭“匿名使用数据”：不得写入/不得补发；关闭后仍符合“立即清空本地日志”的既有规则
- 自动化回归：`bash scripts/test.sh` 全量通过，并记录日期/结论

## 用例 A（商店页 CTA 自检：文案一致且可审计）
步骤：
1. 打开 `docs/ChromeWebStore-Description-ZH.md`，检查是否存在 “Copylot Pro（候补/规划中）” 小节。
2. 打开 `docs/ChromeWebStore-Description-EN.md`，检查是否存在 “Copylot Pro (Planned / Waitlist)” 小节。
3. 检查两份文案均明确：
   - Pro 未上线、不可用（Not shipped yet）
   - 候补加入方式：扩展内 Options -> Pro Tab 点击“加入候补名单”
   - 提供 `docs/monetization/pro-scope.md` 的 GitHub 稳定链接

期望：
- 不暗示 Pro 已提供/可购买；不承诺具体上线时间/价格。
- 链接为仓库内稳定链接，可对外复用且可审计。

执行记录：
- 结果：通过
- 最近执行日期：2026-03-20

## 用例 B（Popup 复制候补文案：剪贴板内容正确且无隐私泄露）
前置：
- 打开 Popup（扩展图标）可见 Pro 入口区域。

步骤：
1. 在 Popup 点击“复制候补文案”（按钮 `#popup-pro-waitlist-copy`）。
2. 将剪贴板粘贴到纯文本编辑器观察内容。

期望：
- 剪贴板内容为候补模板文案（来自 GitHub `issues/new` 的 `body` 预填），包含扩展版本/扩展 ID/语言信息与用户可填写占位符。
- 不包含任何网页内容/URL/标题/用户复制内容/剪贴板历史等信息。
- Popup 内按钮会短暂展示“已复制/Copied!”并自动恢复原文案。

执行记录：
- 结果：通过
- 最近执行日期：2026-03-20

## 用例 C（商业化证据验证：开启匿名使用数据后可导出审计）
前置：
- 在 Options -> 隐私面板开启“匿名使用数据”（默认关闭）。

步骤：
1. 打开 Popup，点击“复制候补文案”（`#popup-pro-waitlist-copy`）。
2. 打开 Options -> 隐私面板，刷新/查看本地匿名事件日志。
3. 在隐私面板执行“导出/复制事件日志”（或将事件文本复制出来）进行审计。

期望：
- 事件日志中存在 `pro_waitlist_copied`。
- 该事件 `props.source === 'popup'`。
- 日志仅包含 `name/ts/props(白名单)`，不包含任何网页内容/URL/标题/复制内容。

执行记录：
- 结果：通过
- 最近执行日期：2026-03-20

## 用例 D（开关关闭行为：不写入 + 立即清空 + 不补发）
前置：
- 在 Options -> 隐私面板关闭“匿名使用数据”。

步骤：
1. 关闭开关后，确认本地匿名事件日志立即清空（符合既有规则）。
2. 再次打开 Popup，点击“复制候补文案”（`#popup-pro-waitlist-copy`）。
3. 回到 Options -> 隐私面板查看事件日志。

期望：
- 关闭后日志立即清空。
- 关闭状态下执行复制，不会新增任何事件（不写入/不补发）。

执行记录：
- 结果：通过
- 最近执行日期：2026-03-20

## 自动化回归记录（可发布审计证据）
- 统一入口：`bash scripts/test.sh`
- 最近执行日期：2026-03-20
- 结论：PASS

