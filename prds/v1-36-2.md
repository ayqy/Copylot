# V1-36-2 拆分：Popup 复制候补文案入口 + 本地可审计埋点（可导出证据）

## 范围（MVP）
在不新增权限、不新增联网发送、且不破坏 Popup 主流程的前提下：

- Popup Pro 入口区域新增“复制候补文案”按钮（低摩擦意向路径）
- 复制内容与 Options -> Pro Tab 的复制结果一致：复制 waitlist GitHub `issues/new` 的 `body` 模板文本（仅环境信息 + 占位提示）
- 若开启“匿名使用数据”（默认关闭）：复制成功记录本地匿名事件 `pro_waitlist_copied`，并允许 `props.source` 为 `popup`
- 同步埋点文档，保证可审计/可导出

## 子需求清单（按 MVP）
- [x] Popup UI
  - [x] `src/popup/popup.html`：新增按钮 `#popup-pro-waitlist-copy`
  - [x] `src/popup/popup.css`：补齐布局（新增一行 Pro 操作区，避免按钮挤压）
  - [x] `_locales/en/messages.json`、`_locales/zh/messages.json`：新增 i18n key `popupProWaitlistCopy`
- [x] Popup 复制逻辑（与 Options 一致）
  - [x] `src/popup/popup.ts`：构造 waitlist issue URL -> 解析 `body` -> `navigator.clipboard.writeText(body)`
  - [x] 复制成功：按钮短暂展示 `copied` 并自动恢复；并记录匿名事件 `pro_waitlist_copied`（`source: popup`）
- [x] Telemetry 白名单放开（支持审计）
  - [x] `src/shared/telemetry.ts`：`pro_waitlist_copied.props.source` 允许 `popup|options`
  - [x] `docs/growth/telemetry-events.md`：更新事件清单（支持 `source: popup|options`）

## 当前状态
- 状态：done
- 最近更新：2026-03-20

