# Roadmap 状态

## 当前阶段

- 阶段名称：S2 / G2 `第二次复用与口碑循环`
- 阶段目标：把第一次成功复制推进成“愿意再次打开 -> 愿意继续复用 -> 愿意分享或评价”的低打扰增长循环。
- 当前判断：`prds/v4.md` 已把本阶段主线收敛为“第二次打开理由 + 高频场景稳定性 + 价值验证后的口碑触发”；本轮已完成 Shift 追加模式与多段收集工作流的状态承接、清理复用、匿名证据导出与统一回归，S2 现已全部收口。
- 当前交付边界：继续不做表单、问卷、候补、支付、收款或其他偏航承接；后续动作仍只服务真实复用质量与口碑样本质量。

## 当前进度

- 当前进度：S0 已完成 `6/6`；S1 已完成 `4/4`；S2 已完成 `4/4`；S3 已完成 `0/4`；全局已完成 `14/18`。
- 已完成事项：
  - 已统一扩展仓可控的官网首页、商店安装页、隐私说明页、Pro 路线页四类固定链接。
  - 已把 Popup 首屏、Chrome Web Store 中英文说明、截图顺序、官网首页承接规范与安装指引统一成“第一次干净复制”叙事。
  - 已补齐 `docs/test-cases/v1-111.md`、`docs/growth/install-first-launch-pack-v1-111.md` 与 `docs/evidence/v1-111/*`，形成首次成功承接的测试与审计基线。
  - 已通过 `bash scripts/test.sh` 完成首次成功路径全量回归，包含 Popup、Options、外链、增长状态与 Playwright 主流程。
  - 已完成 `prds/v4-1.md` 的第 1 项：首次成功后 Popup / Onboarding 已切到快捷 Prompt 槽位复用主路径，并补齐二次成功复制的本地审计摘要。
  - 已补齐第二次打开主路径的发布级收尾：修复 i18n 占位符错误、让默认内置 Prompt 跟随用户设置语言生成，并把本地匿名事件写入改为串行，避免复用事件丢失。
  - 已完成 `prds/v4-2.md`：代码块悬停复制与整页转换补齐了缩进保留、行号剥离、复制按钮噪声清理与 fenced code 回归，并落盘 `docs/evidence/v4-2/*` 作为复用证据。
  - 已完成 WOM 门禁收尾：Popup / Options 的分享与评价入口已绑定到第二次成功复制之后，并补齐 `womQualificationAudit` 本地导出证据。
  - 已完成本轮 `prds/v4-3.md`：Shift 追加模式补齐了 Popup / Options 状态承接、会话清理与匿名审计导出，并通过 `bash scripts/test.sh`。
- 进行中事项：
  - S3 Pro 路线验证尚未开始，仍缺少面向高级页面清洗、批量采集与结构化导出的验证素材。
  - WOM 门禁虽已收口，但仍需要把解锁后的真实分享/评价证据继续回流到承接链路复盘中。
  - 快捷 Prompt 复用路径虽已成立，但还需要继续观察不同场景下的默认 Prompt 命中率与二次成功率。
- 当前经营判断：
  - 当前最小可交付增量已经把“二次成功复制后再推动口碑动作”的门禁立住了，并补齐了多段收集场景的复用证据；下一阶段应把这些复用信号转成 Pro 路线验证素材，而不是继续堆入口。

## 下一步最重要的 3 件事（收入优先）

1. 开始 S3 的高级页面清洗 Pro 路线验证，只保留路线说明、证据口径与可回归素材，不引入支付或留资。
2. 以已完成的追加模式证据为基线，补齐“批量采集与整理”路线说明素材，验证哪些能力最接近真实付费价值。
3. 继续回流 WOM 解锁后的真实分享/评价证据，判断哪些复用场景最值得写进商店说明与后续 Pro 判断。

## 阻塞与需要的人类输入

- 官网真实源码仍不在当前仓库；若后续要把首页承接规范落到真实站点，需要切到对应站点工程，但这不阻塞扩展仓先推进 S2 复用主线。
- 当前 Shift 追加与多段收集回归不依赖新增账号、凭据、支付权限、商店权限或人工审核。
- 当前实现、测试、证据与 roadmap 已完成，且已本地提交 `fa0d287 feat: strengthen append mode workflow`；但 `git push` 仍被当前环境的远端解析失败阻塞。
