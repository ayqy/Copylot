# v1-109 测试用例

## 范围

- 维持无表单安装闭环：Popup 只保留 `#upgrade-pro-entry`，不再出现问卷、候补或显性收集入口。
- 校验 Options Pro 分享工具与外链产物全部指向官网首页、商店安装页或 `/pricing` 路线页。
- 校验路线图、增长投放包和统一测试入口与当前“无表单版”口径一致。

## 用例 A：Popup 不再暴露旧候补节点

步骤：
1. 构建生产包。
2. 检查打包后的 `src/popup/popup.html`。

期望：
- 存在 `#upgrade-pro-entry`。
- 不存在 `#popup-pro-waitlist`。
- 不存在 `#popup-pro-waitlist-copy`、`#popup-pro-waitlist-survey`、`#pro-waitlist-prompt`。

## 用例 B：Options Pro 分享工具只导向安装与路线说明

步骤：
1. 打开 `src/options/options.html#pro`。
2. 填写 `campaign=twitter`。
3. 依次触发路线页、商店页、分享文案与完整投放包复制。

期望：
- 路线页链接包含 `/pricing` 与 `utm_campaign=twitter`。
- 商店链接包含 `chromewebstore.google.com/detail/`。
- 分享文案与投放包不包含 `survey`、`问卷`、`候补`。
- 点击 `#pro-waitlist-button` 打开的仍是 `/pricing` 路线页。

## 用例 C：无表单版增长证据同步

步骤：
1. 检查 `docs/evidence/v1-75/index.md`、`official-links.json`、`pro-distribution-pack.sample.md`。
2. 对比路线图与增长计划文档口径。

期望：
- `official-links.json` 中的 Pro 链接指向 `/pricing`。
- `pro-distribution-pack.sample.md` 只保留官网首页、商店安装页和 Pro 路线页。
- `docs/roadmap.md`、`docs/roadmap_status.md` 的阶段判断与“无表单安装闭环”一致。

## 用例 D：统一测试入口

执行命令：

```bash
bash scripts/test.sh
```

期望：
- `lint`、`type-check`、`check-i18n`、生产构建与 `unit-tests` 全部通过。
- 统一测试入口不再因为旧的 `popup-pro-waitlist` 断言失败。
