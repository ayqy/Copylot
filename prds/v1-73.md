# 问题修复和功能优化（V1-73 并行增长循环启动：官网/商店/Pro 候补转化入口一致化 + 可导出复盘证据）

## 目标
基于 `docs/roadmap_status.md` 的收入优先 Top3：本轮选择 Top1「（v1-73）并行增长循环启动：官网/商店/Pro 候补转化入口一致化 + 可导出复盘证据」作为 1~3 天最小可交付增量主题。

收入第一：插件已上架且开始推广获客后，优先把“真实转化入口”统一为可复盘、可审计、可导出的证据链路——让新增流量能够稳定进入 Pro 候补意向漏斗，并能按 campaign 导出统计与证据包，支撑后续定价/收款链路（S4）决策。

## ULTRA IMPORTANT 第一原则：任务开始后，不要停下来，直到完成目标
**所有细节，都由你来定，判断依据是根据核心目标决定应该如何做，而不要停下来问我**

## 具体任务
1、统一对外转化入口（官网/商店/Pro 候补）的“单一事实来源”（避免推广时口径分裂）
  - 新增/固化统一配置模块（建议：`src/shared/external-links.ts`）：
    - 官网根地址：`https://copy.useai.online/`
    - 商店落地页（用于外部推广的 canonical 链接）：`https://chromewebstore.google.com/detail/ai-copilot-%E2%80%93-magiccopy/ehfglnbhoefcdedpkcdnainiifpflbic`
    - 约定 UTM 口径（不引入时间字段，便于复盘）：`utm_source=copylot-ext`，`utm_medium=<surface>`，`utm_campaign=<campaign>`
  - 提供构建函数（必须可单测、可复核）：
    - `buildOfficialSiteUrl({ medium, campaign? })`
    - `buildProWaitlistUrl({ medium, campaign?, env? })`（仅允许写入可审计的环境信息字段：扩展版本/扩展ID/语言；不得包含 URL/标题/复制内容）
  - 更新所有“跳转/复制链接/分发包”调用方只从该模块取值与构建 URL（避免散落常量）：
    - Popup：Pro 候补提示/入口（`src/popup/popup.ts`）
    - Options：Pro Tab + 渠道分发工具包（`src/options/options.ts` + `src/shared/pro-waitlist-distribution.ts`）

2、把 Pro 候补从“开发者向 GitHub Issues 表单”升级为“官网落地页”（降低留资摩擦，提升真实转化）
  - 将 “加入候补名单 / Join waitlist” 的默认打开目标切换为官网 Pro 候补落地页（仍需保留“不承诺付费/订阅、仅意向验证”的口径）
    - 要求：落地页 URL 必须带 UTM；若设置了 campaign（Options->Pro 可选项），必须带 `utm_campaign=<campaign>`，且可解码复核
  - 保留原有“复制候补文案/复制问卷”能力作为低摩擦留资备选（不依赖联网/账号）
  - 本地可导出商业化证据必须仍成立（不引入任何联网采集）：
    - 匿名开关 ON 时：`pro_entry_opened/pro_waitlist_opened/pro_waitlist_copied/pro_waitlist_survey_copied` 仍可在 Options->隐私面板导出，并按 campaign 聚合复盘
    - 分发工具包的 `pro_distribution_asset_copied` 必须覆盖新增的“官网/候补链接”相关 action（并可按 campaign 统计 distCopies）

3、测试/用例与证据落盘（可执行、可复现、可审计）
  - 新增测试用例文档（固定路径）：`docs/test-cases/v1-73.md`
    - 覆盖：官网/候补 URL 构建（utm + campaign + env 白名单）/ Popup 与 Options 入口行为 / 匿名开关 OFF 与 ON 的导出差异 / campaign 聚合导出
    - 必须包含一次回归记录（PASS/BLOCKED + 证据文件名）
  - 新增证据目录（固定路径）：`docs/evidence/v1-73/`
    - `official-links.json`：官网/商店/候补 URL 样例（含/不含 campaign）与关键字段断言（便于审计）
    - `pro-distribution-pack.sample.md`：完整投放包样例（含官网/商店/候补入口）
  - 自动化测试加固：
    - `scripts/unit-tests.ts`：覆盖 URL 构建函数与 props sanitize 白名单（确保不泄露 URL/标题/复制内容）
    - `bash scripts/test.sh` 全量通过

## 验收标准
* 修复上述所有问题，并测试通过，达到可发布状态
* 实现以上所有需求，并测试通过，达到可发布状态
* 可验证的商业化推进证据（必须可审计/可复核/可复用）：
  - 扩展内所有对外“推广/转化链接”（官网/商店/候补）均来自统一模块（不再散落硬编码）
  - `docs/evidence/v1-73/official-links.json` 存在，且可解码复核：
    - 官网/候补 URL 必须包含 `utm_source=copylot-ext` 与 `utm_medium=<surface>`
    - 当 campaign 非空时，必须包含 `utm_campaign=<campaign>`
    - env 字段仅允许扩展版本/扩展ID/语言（不得出现 URL/标题/复制内容）
  - `docs/evidence/v1-73/pro-distribution-pack.sample.md` 存在，且包含官网/商店/候补三类入口（便于推广与复盘）
  - 匿名开关 ON 时，可在 Options->隐私面板导出 Pro 漏斗与 campaign 聚合数据，并能在导出中看到：
    - `pro_entry_opened/pro_waitlist_opened/pro_waitlist_copied/pro_waitlist_survey_copied`
    - `pro_distribution_asset_copied`（含新增 action）并可用于计算 distCopies
* 测试与用例可执行：
  - `docs/test-cases/v1-73.md` 可逐条执行并包含一次回归记录
  - `bash scripts/test.sh` 全量通过（包含新增单测）

## 注意
1、以上任务为“最小可交付增量（1-3 天）”，收入第一：本轮只做“并行增长循环启动所需的官网/商店/候补入口一致化 + 可导出复盘证据”，不引入支付/订阅、不做与转化证据无关的体验微调
2、隐私红线：不得新增任何联网采集；不得在 URL/query 或证据文件中落盘用户复制内容/网页内容/URL/标题；仅允许扩展版本/扩展ID/语言等可审计环境信息
3、口径红线：任何文案不得暗示“Pro 已上线/可订阅/可付费”；仍需保持“候补/意向验证”表述
4、根据需求创建测试用例文档，通过单元测试和自动化测试来确保代码的正确性（本次测试用例文档路径固定为 `docs/test-cases/v1-73.md`）
5、目标任务成功完成之后，写一份简要的汇报材料文档（固定路径：`docs/reports/v1-73-report.md`），说清楚每一个具体任务现在是什么状态（是否按要求完成了），已经形成了什么商业化证据，以及本次对代码/文档的修改范围（涉及哪些目录和文件）

