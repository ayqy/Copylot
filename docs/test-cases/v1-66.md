# V1-66 CWS Listing 物料证据包一键生成 + 门禁（测试用例）

## 目标
验证子 PRD `prds/v1-66.md` 的验收点：把 Chrome Web Store 上架/获客关键物料（中英文长描述、关键词矩阵、截图顺序/拍摄脚本、更新日志模板）固化为**可一键生成、可审计、可复核、可复用**的结构化证据包，并接入 `bash scripts/test.sh` 门禁，确保长期可重复生成且口径不跑偏。

本轮验收对象（固定路径）：
- 用例文档（本文件）：`docs/test-cases/v1-66.md`
- 生成脚本：`scripts/build-cws-listing-evidence-pack.ts`
- 证据目录与索引：`docs/evidence/v1-66/` + `docs/evidence/v1-66/index.md`
- 样例证据包（JSON）：`docs/evidence/v1-66/cws-listing-evidence-pack-*.json`
- 交付简报：`docs/reports/v1-66-report.md`

---

## 用例 A：生成证据包（固定输出目录，可审计）

前置：
- 已生成生产构建产物（用于版本一致性校验）：`npm run build:prod`（脚本会校验 `manifest.json` 与 `dist/manifest.json` 的 `version` 一致）

步骤：
1. 生成证据包（真实时间）：
   - `node --no-warnings=ExperimentalWarning --loader=ts-node/esm scripts/build-cws-listing-evidence-pack.ts`
2. 检查落盘目录：
   - `docs/evidence/v1-66/index.md`
   - `docs/evidence/v1-66/cws-listing-evidence-pack-<extensionVersion>-YYYY-MM-DD-HHMMSS.json`

期望：
- `docs/evidence/v1-66/` 存在且可被 git 审计。
- 证据包文件命名符合规则（version + UTC 时间戳后缀）。

---

## 用例 B：可解析性与 Schema 稳定（字段齐全 + 顺序稳定）

步骤：
1. 解析 JSON：
   - `node --input-type=module -e "import fs from 'node:fs'; const dir='docs/evidence/v1-66'; const f=fs.readdirSync(dir).find(n=>n.startsWith('cws-listing-evidence-pack-')&&n.endsWith('.json')); JSON.parse(fs.readFileSync(dir+'/'+f,'utf-8')); console.log('OK')"`
2. 检查最小字段齐全（手工/脚本均可）：
   - 顶层字段：`packVersion/exportedAt/extensionVersion/inputs/listing/assertions`
   - `packVersion` 固定为 `v1-66`
   - `inputs[]` 每项包含：`path/bytes/sha256`
   - `listing` 包含：`descriptions.en/zh`、`keywords.en/zh`、`screenshotPlan`、`releaseNotesTemplateSha256`
   - `assertions` 必含：`hasProWaitlistCta/hasTutorialLinks/hasPrivacyClaims/noOverclaimKeywords`

期望：
- `JSON.parse` 成功。
- 字段齐全；字段顺序在长期 diff 中保持稳定（由单测门禁保证）。

---

## 用例 C：断言可复核（当前仓库素材下必须 PASS）

步骤：
1. 打开 `docs/evidence/v1-66/index.md`，核对关键断言：
   - `hasProWaitlistCta: PASS`
   - `hasTutorialLinks: PASS`
   - `hasPrivacyClaims: PASS`
   - `noOverclaimKeywords: PASS`
2. 如需定位断言来源，回溯输入文件（均在 `inputs` 哈希清单内）：
   - `docs/ChromeWebStore-Description-EN.md`
   - `docs/ChromeWebStore-Description-ZH.md`
   - `docs/aso/keywords.md`
   - `docs/aso/store-assets.md`
   - `docs/aso/cws-release-notes-template.md`
   - `docs/aso/value-prop.md`（可选一致性校验输入）

期望：
- 断言全部 PASS；若出现 FAIL，必须在 `docs/evidence/v1-66/index.md` 中出现“FAIL 原因与修复路径”指引（由脚本自动生成）。

---

## 用例 D：sha256 复核（可追溯“本轮物料基线到底是什么”）

步骤：
1. 对任一输入文件复算 sha256（示例）：
   - `shasum -a 256 docs/ChromeWebStore-Description-EN.md`
2. 将结果与 `docs/evidence/v1-66/index.md` 的“输入文件哈希清单”对应行比对。

期望：
- 任意输入文件的 sha256 可复算且与 index 清单一致，可用于发布后追溯物料基线。

---

## 用例 E：敏感信息红线检查（不得落盘任何凭据/Token/环境变量明文）

步骤：
1. 对证据目录做关键字搜索（示例）：
   - `rg -n "CWS_|TOKEN|SECRET|API_KEY|REFRESH_TOKEN" docs/evidence/v1-66`

期望：
- 不出现任何发布凭据/Token/环境变量明文。

---

## 用例 F：可重复性（同一份输入重跑 deterministic）

步骤：
1. 使用稳定 exportedAt 生成（用于门禁与 diff）：
   - `node --no-warnings=ExperimentalWarning --loader=ts-node/esm scripts/build-cws-listing-evidence-pack.ts --stable-exported-at`
2. 记录输出文件哈希（示例）：
   - `shasum -a 256 docs/evidence/v1-66/index.md docs/evidence/v1-66/cws-listing-evidence-pack-*.json`
3. 重复执行步骤 1，并再次计算哈希。

期望：
- 两次输出完全一致（无随机字段/顺序稳定；门禁已接入 `bash scripts/test.sh` 自动校验）。

---

## 自动化回归记录（可发布审计证据）

- 统一入口：`bash scripts/test.sh`
- 最近执行日期：2026-03-21
- 结论：PASS
