# V1-97 并行增长循环真实发布回填与收入证据闭环测试用例

## 目标

验证 `prds/v1-97.md` 三项任务达成：
- 已完成真实发布回填，`postUrl` 不再为 `manual_post_pending`；
- 可从帖子 URL 追溯到转化入口和 Pro 意向证据；
- 阻塞分流文档在 `roadmap_status / blocked / todo` 三方一致。

## 前置条件

- 主索引文件：`docs/growth/assets/social/xhs/v1-96/conversion-evidence-index.json`
- 执行记录：`docs/growth/executions/v1-97-growth-backfill.md`
- Pro 意向证据：`docs/evidence/v1-90/pro-intent-run-evidence-pack.json`

## 用例 A：真实发布回填校验

步骤：
1. 打开 `conversion-evidence-index.json`，检查 `conversionEntries[*].postUrl`。
2. 校验 `postUrl` 均不等于 `manual_post_pending`。
3. 校验 `campaign/source/medium` 与 `targetUrl` 中 UTM 参数一致。
4. 证据落盘：`docs/evidence/v1-97/case-a-backfill-check.json`。

预期：
- 结果 PASS；
- 三入口参数口径一致；
- `postUrl` 均已回填为真实帖子 URL。

## 用例 B：证据链路校验

步骤：
1. 打开 `docs/evidence/v1-97/funnel-chain-sample.json`。
2. 校验可从 `postUrl` 追溯到 `targetUrl` 与 `intentSignal`。
3. 校验链路引用 `docs/evidence/v1-90/pro-intent-run-evidence-pack.json` 的字段：
   `proFunnelSummary.overall.counts.pro_waitlist_survey_copied`。
4. 证据落盘：`docs/evidence/v1-97/case-b-chain-check.json`。

预期：
- 结果 PASS；
- 至少 1 条“分发 -> 转化 -> 意向”链路可复盘；
- 意向口径与 v1-90 证据包一致。

## 用例 C：阻塞分流校验

步骤：
1. 检查 `docs/roadmap_status.md` 是否仍记录 Top1 阻塞（CWS 权限 + `source ~/.bash_profile && pxy`）。
2. 检查 `docs/growth/blocked.md` 是否同步给出输入清单与替代动作。
3. 检查 `docs/growth/todo.md` 是否与阻塞状态一致。
4. 证据落盘：`docs/evidence/v1-97/case-c-blocking-check.txt`。

预期：
- 结果 PASS；
- 三方记录一致，且不影响 v1-97 回填交付闭环。

## 统一门禁执行

- 执行命令：`bash scripts/test.sh`
- 预期：退出码 `0`
- 本轮日志证据：`docs/evidence/v1-97/test.sh.log`

## 回归记录（本轮）

| 用例 | 结果 | 证据文件 | 备注 |
| --- | --- | --- | --- |
| A 真实发布回填校验 | PASS | `docs/evidence/v1-97/case-a-backfill-check.json` | `postUrl` 已回填，参数一致 |
| B 证据链路校验 | PASS | `docs/evidence/v1-97/case-b-chain-check.json` | 链路可追溯到 v1-90 意向证据字段 |
| C 阻塞分流校验 | PASS | `docs/evidence/v1-97/case-c-blocking-check.txt` | `roadmap_status/blocked/todo` 三方一致 |
| 统一门禁 `scripts/test.sh` | PASS | `docs/evidence/v1-97/test.sh.log` | lint/type-check/build/证据脚本均通过 |
