# V1-68 CWS Listing ASO 迭代执行：转化导向文案/关键词小步优化 + diff 证据包落盘（测试用例）

## 目标
验证子 PRD `prds/v1-68.md` 的验收点：在不引入支付/订阅、不新增扩展权限、不采集用户复制内容、不联网发送任何内容数据的前提下，对 CWS Listing 的长描述与关键词做 1 轮“小步转化优化”，并用 v1-66/v1-67 的证据化工具将变更固化为**可审计/可复核/可 diff/可回归**的证据资产（`docs/evidence/v1-68/`）。

本轮验收对象（固定路径）：
- 文案（EN）：`docs/ChromeWebStore-Description-EN.md`
- 文案（ZH）：`docs/ChromeWebStore-Description-ZH.md`
- 关键词矩阵：`docs/aso/keywords.md`
- baseline 证据目录：`docs/evidence/v1-66/`
- diff 证据目录：`docs/evidence/v1-68/`
- 用例文档（本文件）：`docs/test-cases/v1-68.md`
- 交付简报：`docs/reports/v1-68-report.md`
- 自动化测试入口：`bash scripts/test.sh`

---

## 用例 A：Listing 文案转化口径（首屏 + Quick Start + Pro 候补 + 隐私三件套）

步骤：
1. 打开 `docs/ChromeWebStore-Description-EN.md`，检查首屏 3~5 行同时包含：
   - 价值（解决复制噪声/结构化输出）
   - 适用场景（AI chats / docs / notes）
   - 隐私优先（on-device，不上传复制内容；不得引入未实现能力）
2. 检查包含 `QUICK START`（3 步以内），且路径可复现为：`Popup -> 触发复制 -> 粘贴`。
3. 检查 Pro 候补 CTA（必须真实口径）：
   - 明确：Pro 未上线、不可用，仅候补（waitlist）
   - 加入方式可按扩展内路径复现：`Options -> Pro tab`
   - 必须包含稳定链接：`docs/monetization/pro-scope.md`
4. 检查隐私口径三件套齐全：
   - 本地处理（on-device）
   - 不收集/不上传复制内容
   - “匿名使用数据”开关默认关闭，且仅本地
5. 打开 `docs/ChromeWebStore-Description-ZH.md`，做与 EN 相同的结构与口径一致性核对（以中文表述为准）。

期望：
- EN/ZH 均包含 Quick Start，且不夸大、不引入未实现能力。
- Pro 段落满足“仅候补/可复现路径/稳定链接”三要素。
- 隐私口径三件套完整且中英文一致。

---

## 用例 B：关键词矩阵更新（高意图 + 不夸大 + 可维护）

步骤：
1. 打开 `docs/aso/keywords.md`：
   - EN：新增 3~6 个高意图候选词（围绕 web clipper / copy cleaner / copy webpage to text 等），并移除 1~3 个低信息量或重复词。
   - ZH：新增 3~6 个面向中文用户的候选词（如网页复制助手/网页转文本/一键复制等），并移除 1~3 个重复或歧义词。
2. 自检禁止词（关键词池不得出现任何“付费/订阅/升级/价格”等误导性宣称）：
   - `rg -n "付费|订阅|升级|pricing|subscription" docs/aso/keywords.md`

期望：
- 关键词池不包含任何付费/订阅/升级/价格相关词汇。
- 关键词更聚焦“高意图 + 可复现能力”，且整体仍可维护。

---

## 用例 C：证据生成闭环（baseline -> ASO 改动 -> v1-68 diff 落盘）

前置：
- 生成生产构建产物（用于版本一致性校验）：`npm run build:prod`（脚本会校验 `manifest.json` 与 `dist/manifest.json` 的 `version` 一致）

步骤：
1. baseline 捕获（before，改文案/关键词前执行一次；作为本轮 diff 的基线快照）：
   - `node --no-warnings=ExperimentalWarning --loader=ts-node/esm scripts/build-cws-listing-evidence-pack.ts`
2. 文案/关键词按 PRD 完成修改（本 PR 的主要内容）。
3. 变更落盘（after，写入独立目录 `docs/evidence/v1-68/`）：
   - `node --no-warnings=ExperimentalWarning --loader=ts-node/esm scripts/build-cws-listing-diff-evidence-pack.ts --evidence-dir docs/evidence/v1-68 --stable-exported-at`
4. 检查落盘目录至少包含：
   - `docs/evidence/v1-68/index.md`
   - `docs/evidence/v1-68/cws-listing-evidence-pack-*.json`（current pack，`packVersion=v1-66`）
   - `docs/evidence/v1-68/cws-listing-diff-evidence-pack-*.json`（diff pack，`packVersion=v1-67`）

期望：
- `docs/evidence/v1-68/index.md` 包含 baseline/current sha256、关键词/长描述指纹变化、红线断言结论、执行命令。
- diff 脚本退出码为 0，且 `redlines=[]`。

---

## 用例 D：红线/断言检查（商业化入口与合规口径不跑偏）

步骤：
1. 打开 `docs/evidence/v1-68/index.md`，核对红线断言结果：
   - `hasProWaitlistCta: PASS`
   - `hasPrivacyClaims: PASS`
   - `noOverclaimKeywords: PASS`
   - `redlines: []`
2. 解析 current pack JSON 并核对 v1-66 断言均为 `true`（示例命令）：
   - `node --input-type=module -e "import fs from 'node:fs'; const dir='docs/evidence/v1-68'; const f=fs.readdirSync(dir).find(n=>n.startsWith('cws-listing-evidence-pack-')&&n.endsWith('.json')); const p=JSON.parse(fs.readFileSync(dir+'/'+f,'utf-8')); console.log(p.assertions)"`

期望：
- 当前素材命中红线时必须阻断（本轮期望为 PASS 且 `redlines=[]`）。
- v1-66 的 `hasProWaitlistCta/hasTutorialLinks/hasPrivacyClaims/noOverclaimKeywords` 均为 `true`。

---

## 用例 E：sha256 复核（baseline/current/diff 可复核互证）

步骤：
1. 从 `docs/evidence/v1-68/index.md` 获取以下 sha256：
   - baseline pack sha256（指向 `docs/evidence/v1-66/...json`）
   - current pack sha256（`docs/evidence/v1-68/cws-listing-evidence-pack-*.json`）
   - diff pack sha256（`docs/evidence/v1-68/cws-listing-diff-evidence-pack-*.json`）
2. 复算 sha256（示例）：
   - `shasum -a 256 docs/evidence/v1-66/cws-listing-evidence-pack-*.json`
   - `shasum -a 256 docs/evidence/v1-68/*.json`

期望：
- `index.md` 中记录的 sha256 可复算且与对应 JSON 文件内容一致。

---

## 用例 F：敏感词搜索（禁止误导性宣称；人工自检补强）

步骤：
1. 对文案/关键词做敏感词搜索（必须执行一次）：
   - `rg -n "付费|订阅|升级|pricing|subscription" docs/aso/keywords.md docs/ChromeWebStore-Description-EN.md docs/ChromeWebStore-Description-ZH.md`

期望：
- 关键词池不得命中上述敏感词（命中则直接 FAIL 并回滚/替换该关键词）。
- 文案若命中，必须为“否定语境/免责声明”（例如“无付费/订阅承诺”），且 v1-66 断言 `noOverclaimKeywords=true` 仍为 PASS。

---

## 自动化回归记录（可发布审计证据）

- 统一入口：`bash scripts/test.sh`
- 最近执行日期：2026-03-21
- 结论：PASS
