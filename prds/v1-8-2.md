# V1-8-2 自动化单测补齐（code-block-cleaner 覆盖关键场景）

## 目标
补齐针对“代码块保守清理”的自动化单测，并确保 `bash scripts/test.sh` 可在本地/CI 一键全量通过。

## 具体任务
1. 在 `scripts/unit-tests.ts` 增加 `src/shared/code-block-cleaner.ts` 的单测用例：
   - 正文包含 Copy/Clone：标识符/字符串/注释不应被删除
   - 首/末行按钮文案：中英文各 1 个用例应被移除
   - 按钮文案在中间行：不应移除（验证位置受限）
2. 验收前确保 `bash scripts/test.sh` 全量通过（lint/type-check/check-i18n/unit-tests/build:prod）

## 验收标准
- 单测覆盖关键场景且可执行
- `bash scripts/test.sh` 全量通过

## 进度记录
- 2026-03-19：已完成（`scripts/unit-tests.ts` 已补齐用例；`bash scripts/test.sh` 已通过）

