# 问题修复和功能优化（V1-99 收入证据增量：三入口样本持续回填 + 漏斗对比证据导出）

## 目标
实现具体任务中要求的所有功能和问题修复（在 `docs/roadmap_status.md` Top1「CWS 权限 + `source ~/.bash_profile && pxy`」阻塞未解除时，顺延执行 Top3 第 2 项），使“官网/CWS/Pro 三入口”的新增样本与漏斗对比证据在 1-3 天内可持续沉淀并可复盘，直接服务商业化收入路径验证。

## ULTRA IMPORTANT 第一原则：任务开始后，不要停下来，直到完成目标
**所有细节，都由你来定，判断依据是根据核心目标决定应该如何做，而不要停下来问我**

## 具体任务
1、三入口新增样本持续回填（收入优先，1-3 天内完成）
  - 以 `docs/growth/assets/social/xhs/v1-96/conversion-evidence-index.json` 为主索引，新增至少 3 条可追溯样本，且必须覆盖 `official_site`、`chrome_web_store`、`pro_waitlist` 三类入口。
  - 每条新增样本必须补齐：`postUrl`、`targetUrl`、`campaign`、`source`、`medium`、`intentSignal`、`clicks`、`installs`、`proIntentSignals`、`evidencePath`。
  - 新增样本继续复用统一归因口径（`campaign/source/medium`），避免出现不可归因样本。

2、导出可审计的商业化对比证据包（1-3 天内完成）
  - 新增执行记录：`docs/growth/executions/v1-99-growth-backfill.md`，记录样本来源、入口映射、关键指标变化、阻塞分流结果。
  - 新增证据目录：`docs/evidence/v1-99/`，至少产出：
    - `conversion-funnel-v1-99.csv`（新增样本明细）
    - `conversion-funnel-compare-v1-98-v1-99.json`（v1-98 与 v1-99 的 `clicks/installs/proIntentSignals` 对比）
    - `sample-audit-v1-99.json`（样本字段完整性与审计链校验）
  - 更新 `docs/growth/metrics.md`，新增 `run_id: v1-99-xhs-backfill` 条目并与证据包数据一致。

3、测试用例与自动化门禁（必须可执行）
  - 创建测试用例文档：`docs/test-cases/v1-99.md`，至少覆盖：
    - 用例 A：样本完整性校验（样本数、字段完整、`postUrl` 非占位）
    - 用例 B：归因一致性校验（`campaign/source/medium` 与三入口映射一致）
    - 用例 C：商业化对比证据校验（`v1-98` vs `v1-99` 的对比文件可导出且口径一致）
    - 用例 D：阻塞顺延校验（Top1 阻塞时，`docs/roadmap_status.md`、`docs/growth/blocked.md`、执行记录三方一致）
  - 执行 `bash scripts/test.sh`，将日志落盘为 `docs/evidence/v1-99/test.sh.log`，并在 `docs/test-cases/v1-99.md` 记录 PASS/BLOCKED 与证据文件名。

## 验收标准
* 修复上述所有问题，并测试通过，达到可发布状态
* 实现以上所有需求，并测试通过，达到可发布状态
* 可验证的商业化推进证据（必须可审计/可复盘）：
  - `docs/growth/assets/social/xhs/v1-96/conversion-evidence-index.json` 在本轮新增至少 3 条样本，且三入口各至少 1 条。
  - `docs/evidence/v1-99/conversion-funnel-compare-v1-98-v1-99.json` 可直接展示 `clicks`、`installs`、`proIntentSignals` 的增量对比，并与 `docs/growth/metrics.md` 一致。
  - 新增样本中至少 2 条满足 `clicks > 0` 且（`installs > 0` 或 `proIntentSignals > 0`），可证明已推进转化或付费意向链路。
* 顺延策略可验证：
  - `docs/roadmap_status.md` 明确记录 Top1 阻塞与顺延执行 Top3 第 2 项，本轮交付主题与 `prds/v1-99.md` 一致。
* 测试与用例可执行：
  - `docs/test-cases/v1-99.md` 提供可复现步骤、预期结果、回归记录（PASS/BLOCKED + 证据文件名）。
  - `bash scripts/test.sh` 执行完成且日志落盘到 `docs/evidence/v1-99/test.sh.log`。

## 注意
1、以上任务规模非常大，每一个都很有挑战性，弄清楚需求后你应该创建新的需求文档来记录每个子需求，例如 `v1-99-1.md`、`v1-99-2.md`、`v1-99-3.md`（按样本回填/证据对比/测试门禁拆分）
2、一步一步来，对于每个阶段，边执行边更新对应文档中的进度（至少同步 `docs/roadmap.md`、`docs/roadmap_status.md`、`docs/growth/executions/v1-99-growth-backfill.md`）
3、根据需求创建测试用例文档，通过单元测试和自动化测试来确保代码的正确性（固定路径：`docs/test-cases/v1-99.md`）
4、目标任务成功完成之后，写一份简要的汇报材料文档（用于口头汇报，所以要简明扼要抓住重点），说清楚每一个具体任务现在是什么状态（是否按要求完成了），已经实现了什么效果，以及本次对代码的修改范围（涉及哪些目录和文件）（固定路径：`docs/reports/v1-99-report.md`）
5、收入第一：除阻塞上架/收款/转化漏斗的关键问题外，不处理纯体验细节；若账号/风控/权限阻塞导致真实发布受限，必须先更新 `docs/growth/blocked.md` 与 `docs/growth/todo.md`，再继续完成可离线复核的收入证据补齐
