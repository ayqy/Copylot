# V1-14-1 拆分：Options 隐私页“本地增长统计”面板（MVP）

## 范围
在不新增权限、不新增任何联网埋点、且不影响现有复制体验的前提下，为 Options -> 隐私与可观测性补齐 `chrome.storage.local` 的 `copilot_growth_stats` 可视化与一键导出/重置能力。

## 子需求清单（按 MVP）
- [x] UI：在 `src/options/options.html` 的 `#privacy-tab` 内新增折叠面板（details/summary），稳定 DOM：
  - [x] 容器：`#growth-stats-panel`
  - [x] 只读展示区：`#growth-stats-view`
  - [x] 按钮：刷新 `#growth-stats-refresh` / 复制 `#growth-stats-copy` / 重置 `#growth-stats-reset`
- [x] i18n：新增/补齐相关 key，并同步更新 `_locales/en/messages.json` 与 `_locales/zh/messages.json`，确保 `npm run check-i18n` 通过
- [x] 逻辑：在 `src/options/options.ts` 实现读取/规范化/渲染（pretty JSON）/复制（clipboard + fallback）/重置（默认结构 + 立即刷新）
- [x] 约束：展示来源仅允许 `copilot_growth_stats`；展示字段仅允许 `installedAt/successfulCopyCount/ratingPromptShownAt/ratingPromptAction/ratingPromptActionAt`
- [x] 可发布交付：补齐用例文档 `docs/test-cases/v1-14.md` 与简报 `docs/reports/v1-14-report.md`；`bash scripts/test.sh` 全量通过

## 当前状态
- 状态：done
- 最近更新：2026-03-19

