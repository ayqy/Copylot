# 问题修复和功能优化（V1-68 CWS Listing ASO 迭代执行：转化导向文案/关键词小步优化 + diff 证据包落盘）

## 目标
基于 `docs/roadmap_status.md` 的收入优先 Top3：Top1「完成一次真实 CWS 发布 + 商店端取证」仍受“需要可用代理/VPN + 商店可达”的网络可达性阻塞；Top2「发布后回归与 24h/7d 复盘证据补齐」依赖 Top1 同样阻塞；本轮顺延选择 Top3「（v1-68）CWS Listing ASO 迭代执行（离线可推进）」作为 1~3 天最小可交付增量主题。

收入第一：在不引入支付/订阅、不新增扩展权限、不采集用户复制内容、不联网发送任何内容数据的前提下，把“上架=获客入口 + Pro 候补=商业化验证入口”的关键对外素材（长描述/关键词）做 1 轮小步转化优化，并用 v1-66/v1-67 的证据化工具把变更落盘为可审计、可复核、可 diff 的证据资产，确保网络恢复后可直接粘贴上架并能对齐取证口径。

## ULTRA IMPORTANT 第一原则：任务开始后，不要停下来，直到完成目标
**所有细节，都由你来定，判断依据是根据核心目标决定应该如何做，而不要停下来问我**

## 具体任务
1、做 1 轮“转化导向但不夸大”的 Listing 文案小步迭代（可直接用于上架）
  - 更新英文长描述：`docs/ChromeWebStore-Description-EN.md`
    - 优化首屏 3~5 行：把“价值 + 适用场景 + 隐私优先（on-device）”压缩到首屏可读范围（不引入任何未实现能力）。
    - 新增 `QUICK START`（3 步以内）：明确用户如何在 30 秒内完成首次成功复制（Popup -> 触发复制 -> 粘贴到 AI/文档）。
    - 保持并强化 Pro 候补 CTA 的“真实口径”：必须明确 Pro 未上线、仅候补；加入方式必须可按扩展内路径复现；必须包含稳定链接 `docs/monetization/pro-scope.md`。
  - 更新中文长描述：`docs/ChromeWebStore-Description-ZH.md`
    - 同步 EN 的首屏与 QUICK START 结构（中文表述为准），保证中英文口径一致。
    - 强化“隐私口径三件套”：本地处理 / 不上传复制内容 / 匿名使用数据默认关闭（且仅本地）。
  - 更新关键词矩阵（候选池）：`docs/aso/keywords.md`
    - EN：新增 3~6 个“高意图但不夸大”的候选词（例如围绕 web clipper / copy cleaner / copy webpage to text 等），并移除 1~3 个低信息量或重复词，保持整体可维护。
    - ZH：新增 3~6 个面向中文用户的候选词（例如网页复制助手/网页转文本/一键复制等），并移除 1~3 个重复或歧义词。
    - 禁止引入任何“付费/订阅/升级/价格”类词汇（由 v1-66 `noOverclaimKeywords` 断言兜底，但仍需人工自检）。

2、生成并落盘“本轮 ASO 迭代”的可审计证据（可复核、可 diff、可回归）
  - 基线捕获（before）：在改文案/关键词前运行 v1-66 listing pack 生成（要求 `npm run build:prod` 已执行且 `manifest.json` 与 `dist/manifest.json` 版本一致）
    - 命令：`node --no-warnings=ExperimentalWarning --loader=ts-node/esm scripts/build-cws-listing-evidence-pack.ts`
    - 产物：刷新 `docs/evidence/v1-66/index.md` 与对应 `cws-listing-evidence-pack-*.json`（作为本轮 diff 的 baseline）
  - 变更落盘（after）：文案/关键词修改后，运行 v1-67 diff 工具把 current + diff 固化到独立目录 `docs/evidence/v1-68/`
    - 命令：`node --no-warnings=ExperimentalWarning --loader=ts-node/esm scripts/build-cws-listing-diff-evidence-pack.ts --evidence-dir docs/evidence/v1-68 --stable-exported-at`
    - 产物（必须可 `JSON.parse`，字段顺序稳定，便于长期 diff 与审计）：
      - `docs/evidence/v1-68/index.md`
      - `docs/evidence/v1-68/cws-listing-evidence-pack-*.json`（current pack，`packVersion="v1-66"`）
      - `docs/evidence/v1-68/cws-listing-diff-evidence-pack-*.json`（diff pack，`packVersion="v1-67"`）
  - 如发现 `scripts/build-cws-listing-diff-evidence-pack.ts` 的索引模板仍存在硬编码路径（例如写死 `docs/evidence/v1-67/`），必须在本轮修复为基于 `--evidence-dir` 动态输出，避免“证据落盘但索引不可执行”的审计风险。

3、测试/用例与汇报材料闭环（确保“做了就能取证，且能复盘”）
  - 新增测试用例文档（固定路径，必须在 PRD 中明确）：`docs/test-cases/v1-68.md`
    - 覆盖：baseline 生成（v1-66）-> ASO 改动 -> diff 生成（v1-68）-> redlines/断言检查 -> sha256 复核 -> 敏感词搜索（`rg -n "付费|订阅|升级|pricing|subscription"`）
    - 必须包含一次回归记录（日期 + PASS）与可复制命令
  - 新增简报（固定路径建议）：`docs/reports/v1-68-report.md`
    - 用于口头汇报：本轮改动点（首屏/Quick Start/关键词）+ 为什么更利于“安装/候补转化”+ 风险（夸大/隐私口径丢失）是否被红线阻断
  - 更新 `docs/roadmap_status.md`：记录 v1-68 规划状态，并保持 Top1/Top2 网络阻塞信息不断档

## 验收标准
* 修复上述所有问题，并测试通过，达到可发布状态
* 实现以上所有需求，并测试通过，达到可发布状态
* 可验证的商业化推进证据（必须可审计/可复核/可复用）：
  - 长描述与关键词已按口径更新且可直接用于上架：
    - `docs/ChromeWebStore-Description-EN.md` 与 `docs/ChromeWebStore-Description-ZH.md` 均包含 `QUICK START`，且同时满足 v1-66 断言：
      - `hasProWaitlistCta=true`（包含 waitlist 加入方式 + `docs/monetization/pro-scope.md` 稳定链接）
      - `hasPrivacyClaims=true`（本地处理/不上传/匿名默认 OFF 的口径齐全）
      - `hasTutorialLinks=true`（三篇教程链接齐全）
    - `docs/aso/keywords.md` 更新后 v1-66 断言 `noOverclaimKeywords=true`（不得出现“付费/订阅/升级/价格”等误导性宣称）
  - `docs/evidence/v1-68/` 存在且可被 git 审计，至少包含：
    - `index.md`（包含 baseline/current sha256、关键词/长描述指纹变化、红线断言结论、执行命令）
    - 1 个 current pack：`cws-listing-evidence-pack-*.json`
    - 1 个 diff pack：`cws-listing-diff-evidence-pack-*.json` 且 `redlines=[]`
  - 证据可复核：对 `docs/evidence/v1-68/` 内 JSON 运行 `shasum -a 256` 与索引中记录一致；`diff.keywords.*` 可从两份 pack 的 `listing.keywords` 复算。
* 测试与用例可执行：
  - `docs/test-cases/v1-68.md` 可逐条执行并包含一次回归记录（日期 + PASS）
  - `bash scripts/test.sh` 全量通过

## 注意
1、以上任务为“最小可交付增量（1-3 天）”，收入第一：本轮只做“CWS Listing ASO 迭代执行 + 证据落盘闭环”，不引入支付/订阅、不新增权限、不做与上架/获客/转化取证无关的体验微调
2、红线与合规：不得在长描述/关键词/证据索引中暗示“Pro 已上线/可订阅/可付费”；不得出现任何支付承诺或价格信息；不得引入任何用户复制内容、URL/标题/页面内容到证据包
3、与 Top1/Top2 的衔接：本轮只解决“上架素材更利于转化且可审计可复核”，不替代真实发布与商店端截图取证；网络恢复后仍需按 `docs/evidence/v1-62/index.md` 与 `docs/test-cases/v1-45.md` 完成真实发布/回归/复盘取证
4、根据需求创建测试用例文档，通过单元测试和自动化测试来确保代码的正确性（本次测试用例文档路径固定为 `docs/test-cases/v1-68.md`）
5、目标任务成功完成之后，写一份简要的汇报材料文档（固定路径：`docs/reports/v1-68-report.md`），说清楚每一个具体任务现在是什么状态（是否按要求完成了），已经形成了什么商业化证据，以及本次对代码/文档的修改范围（涉及哪些目录和文件）

