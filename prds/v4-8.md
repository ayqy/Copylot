# 三条路线真实打开与复制样本比较

## 目标

只完成 `docs/roadmap.md` 中 `S3 Pro 路线验证` 最后一项的第一段闭环：把高级页面清洗、批量采集与整理、结构化导出与下游工作流三条路线的真实打开/复制样本比较，真正接到 `Options -> Pro`，让当前阶段能直接导出路线比较摘要，而不是停留在文档判断。

## 范围

1. 新增共享纯逻辑，按最近 7 天本地匿名聚合事件比较三条路线的：
   - `pro_waitlist_opened`
   - `validation_route`
   - `validation_brief`
   - `validation_checklist`
2. 在 `Options -> Pro` 新增“三条路线样本比较”区块，至少提供：
   - 复制 Markdown 摘要
   - 下载 JSON 摘要
3. 新增可复用脚本，用样本 telemetry 生成 `docs/evidence/v4-8/comparison-pack/*`。
4. 更新 `scripts/unit-tests.ts`、`scripts/ui-integration-tests.ts`、`e2e/options-pro-flow.spec.ts`，覆盖新入口与摘要输出。
5. 更新 `docs/test-cases/v4-8.md`、`docs/reports/v4-8-report.md`、`docs/evidence/v4-8/*`、`docs/roadmap.md`、`docs/roadmap_status.md`。

## 非目标

- 不进入支付、收款、留资、问卷或候补承接。
- 不新增第四条路线。
- 不回写对外文案，回写留到下一轮。

## 验收

- `Options -> Pro` 中出现“三条路线样本比较”区块与两个动作按钮。
- 摘要能输出三条路线的 `route_opened / validation_route / validation_brief / validation_checklist / total_signals`。
- `docs/evidence/v4-8/comparison-pack/*` 与界面口径一致。
- `bash scripts/test.sh` 通过。
