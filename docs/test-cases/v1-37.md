# V1-37 真实上架验证：CWS 发布含转化入口 + 商店物料一致性核对 + 可审计证据落盘 测试用例

## 目标
验证子 PRD `prds/v1-37.md` 达到可发布验收口径（收入优先）：

- `npm run publish:cws` 发布门禁可通过（`bash scripts/test.sh` + 产物一致性校验 + 重新打包 zip）
- 商店端物料口径一致（EN/ZH 长描述、Pro Planned/候补入口、稳定口径链接、截图顺序、教程链接）
- 从商店安装后转化入口可用（Popup/Options -> Pro Tab）且可导出本地匿名事件证据（开启/关闭行为符合隐私规则）
- 自动化回归：`bash scripts/test.sh` 全量通过，并记录日期/结论

## 用例 A（发布门禁演练：dry-run 必须通过）
步骤：
1. 确认 `manifest.json` 版本号已递增（避免同版本被拒）。
2. 执行 `npm run publish:cws -- --dry-run`。
3. 检查输出包含：
   - 已执行 `bash scripts/test.sh` 且 PASS
   - `dist/manifest.json` 的 `version` 与 `manifest.json` 一致
   - 已重新生成 `plugin-${version}.zip`
   - dry-run 提示“不会进行任何上传/发布网络调用”

期望：
- dry-run 全流程通过，作为真实发布前置门禁。

执行记录：
- 结果：通过
- 最近执行日期：2026-03-20

## 用例 B（真实发布：upload + publish 到 default channel）
步骤：
1. 如网络受限，先配置代理：`HTTPS_PROXY=...`（脚本已支持）。
2. 执行 `npm run publish:cws`（非 `--dry-run`）。
3. 在 CWS Developer Dashboard 检查并截图：
   - 新版本号已可见
   - 发布时间/更新时间已刷新
4. 打开 Chrome Web Store 商店详情页检查并截图：
   - 版本号与更新时间与后台一致

期望：
- 发布成功，且后台/商店页可复查新版本号与发布时间（截图落盘到简报）。

执行记录：
- 结果：阻塞
- 最近执行日期：2026-03-20
- 阻塞原因：`ENOTFOUND www.googleapis.com`（网络不可达导致 upload/publish 失败；需代理/VPN 后重试）

## 用例 C（商店端物料一致性核对：口径可审计）
步骤：
1. 对照 EN 长描述：`docs/ChromeWebStore-Description-EN.md`
2. 对照 ZH 长描述：`docs/ChromeWebStore-Description-ZH.md`
3. 检查两份长描述均满足：
   - 包含 “Copylot Pro（Planned/候补）”小节
   - 明确未上线不可用（Not shipped yet）
   - 引导路径一致：商店页 -> 扩展内 Options -> Pro Tab -> 加入候补/复制候补文案
   - 包含稳定对外链接：`docs/monetization/pro-scope.md`
4. 对照截图顺序与标题口径：`docs/aso/store-assets.md`（01-07）
5. 检查长描述中引用的教程链接指向仓库内存在的文档（`docs/tutorials/*`）

期望：
- 仓库侧口径一致、真实可验证、不夸大、不承诺未实现能力。

执行记录：
- 结果：通过（仓库侧核对）
- 最近执行日期：2026-03-20

## 用例 D（从 CWS 安装回归：转化入口可用）
前置：
- 必须从 Chrome Web Store 安装本次发布版本（禁止 unpacked / 本地加载替代验收）。

步骤：
1. 从 CWS 安装扩展（目标版本号与简报一致）。
2. 打开 Popup，验证存在 Pro 入口：
   - `#upgrade-pro-entry`
   - `#popup-pro-waitlist`
   - `#popup-pro-waitlist-copy`
3. 打开 Options，验证 Pro Tab：
   - Pro Tab 可打开（`#pro-tab`）
   - 候补入口可打开（`#pro-waitlist-button`）
   - “复制候补文案”可写入剪贴板（`#pro-waitlist-copy`）

期望：
- Popup/Options 的候补入口可见且可用；复制内容为候补模板（仅环境信息 + 占位提示），不包含任何网页内容/URL/标题/复制内容。

执行记录：
- 结果：阻塞（依赖用例 B 发布成功）
- 最近执行日期：2026-03-20

## 用例 E（可导出审计证据：事件导出与开关清理）
前置：
- 从 CWS 安装该版本（同用例 D）。

步骤：
1. Options -> 隐私与可观测性，开启“匿名使用数据”（默认关闭）。
2. 触发事件并记录来源：
   - Popup 点击升级 Pro（触发 `pro_entry_opened`，`props.source=popup`）
   - Options 打开 Pro Tab（触发 `pro_entry_opened`，`props.source=options`）
   - Popup/Options 打开候补（触发 `pro_waitlist_opened`）
   - Popup/Options 复制候补文案成功（触发 `pro_waitlist_copied`）
3. 在隐私面板导出/复制本地匿名事件日志，检查包含以上事件，且 `props.source` 仅为 `popup|options`。
4. 关闭“匿名使用数据”，确认事件日志立即清空；随后重复触发入口，确认不会新增事件（不写入/不补发）。

期望：
- 开关开启：可导出 `pro_entry_opened/pro_waitlist_opened/pro_waitlist_copied`，且字段白名单与枚举值正确。
- 开关关闭：立刻清空且不补发。

执行记录：
- 结果：阻塞（依赖用例 B 发布成功）
- 最近执行日期：2026-03-20

## 自动化回归记录（可发布审计证据）
- 统一入口：`bash scripts/test.sh`
- 最近执行日期：2026-03-20
- 结论：PASS

