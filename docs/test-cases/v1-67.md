# V1-67 CWS Listing ASO 迭代证据化 v2：证据包 diff + 变更摘要一键生成（测试用例）

## 目标
验证子 PRD `prds/v1-67.md` 的验收点：基于 v1-66 的 Listing 物料证据包基线，新增「diff 证据包 + 变更摘要 + 红线门禁 + 可重复性」一键生成能力，确保每次 CWS Listing ASO 迭代都**可审计/可复核/可复用**，且不会丢失“获客入口/Pro 候补 CTA/隐私合规口径”。

本轮验收对象（固定路径）：
- 用例文档（本文件）：`docs/test-cases/v1-67.md`
- 生成脚本：`scripts/build-cws-listing-diff-evidence-pack.ts`
- 基线证据目录：`docs/evidence/v1-66/` + `docs/evidence/v1-66/index.md`
- diff 证据目录与索引：`docs/evidence/v1-67/` + `docs/evidence/v1-67/index.md`
- 样例 diff 证据包（JSON）：`docs/evidence/v1-67/cws-listing-diff-evidence-pack-*.json`
- 交付简报：`docs/reports/v1-67-report.md`

---

## 用例 A：生成 baseline（v1-66）与 diff（v1-67）证据（可审计落盘）

前置：
- 已生成生产构建产物（用于版本一致性校验）：`npm run build:prod`（脚本会校验 `manifest.json` 与 `dist/manifest.json` 的 `version` 一致）

步骤：
1. 生成 baseline pack（v1-66，稳定 exportedAt，用于 diff 基线）：
   - `node --no-warnings=ExperimentalWarning --loader=ts-node/esm scripts/build-cws-listing-evidence-pack.ts --stable-exported-at`
2. 生成 diff pack（v1-67，稳定 exportedAt，用于门禁与可重复性）：
   - `node --no-warnings=ExperimentalWarning --loader=ts-node/esm scripts/build-cws-listing-diff-evidence-pack.ts --stable-exported-at`
3. 检查落盘目录：
   - `docs/evidence/v1-67/index.md`
   - `docs/evidence/v1-67/cws-listing-diff-evidence-pack-<extensionVersion>-YYYY-MM-DD-HHMMSS.json`

期望：
- `docs/evidence/v1-67/` 存在且可被 git 审计。
- `docs/evidence/v1-67/index.md` 包含 baseline/current sha256、关键变更摘要、红线断言结果、执行命令。

---

## 用例 B：可解析性与 Schema 稳定（字段齐全 + 顺序稳定）

步骤：
1. 解析 diff JSON：
   - `node --input-type=module -e "import fs from 'node:fs'; const dir='docs/evidence/v1-67'; const f=fs.readdirSync(dir).find(n=>n.startsWith('cws-listing-diff-evidence-pack-')&&n.endsWith('.json')); JSON.parse(fs.readFileSync(dir+'/'+f,'utf-8')); console.log('OK')"`
2. 检查最小字段齐全（手工/脚本均可）：
   - 顶层字段：`packVersion/exportedAt/extensionVersion/baseline/current/diff/redlines`
   - `packVersion` 固定为 `v1-67`
   - `baseline/current` 至少包含：`packPath/sha256/extensionVersion/exportedAt/assertions`
   - `diff` 至少包含：
     - `keywords.enAdded/enRemoved/zhAdded/zhRemoved`
     - `descriptions.enSha256From/enSha256To/zhSha256From/zhSha256To`
     - `screenshotPlan.changedIds`
     - `assertions.changedKeys`
   - `redlines`：数组（门禁用）

期望：
- `JSON.parse` 成功。
- 字段齐全；字段顺序在长期 diff 中保持稳定（由单测门禁保证）。

---

## 用例 C：红线门禁（商业化入口/合规口径不跑偏）

步骤：
1. 打开 `docs/evidence/v1-67/index.md`，核对红线断言结果：
   - `hasProWaitlistCta: PASS`
   - `hasPrivacyClaims: PASS`
   - `noOverclaimKeywords: PASS`
   - `redlines: []`

期望：
- 当前仓库素材下 `redlines=[]` 且脚本退出码为 0。
- 若未来任一断言 FAIL（例如 `hasProWaitlistCta=false`），则：
  - `redlines` 必须非空（至少包含 `lostProWaitlistCta/lostPrivacyClaims/overclaimDetected` 的对应枚举）
  - 脚本以非 0 退出码阻断门禁（但仍会落盘证据，便于审计定位）。

---

## 用例 D：sha256 复核（baseline/current/diff 可复核互证）

步骤：
1. 从 `docs/evidence/v1-67/index.md` 获取以下 sha256：
   - baseline pack sha256（指向 `docs/evidence/v1-66/...json`）
   - current pack sha256（`docs/evidence/v1-67/...json`）
   - diff pack sha256（`docs/evidence/v1-67/cws-listing-diff-evidence-pack-*.json`）
2. 复算 sha256（示例）：
   - `shasum -a 256 docs/evidence/v1-66/cws-listing-evidence-pack-*.json`
   - `shasum -a 256 docs/evidence/v1-67/*.json`

期望：
- `index.md` 中记录的 sha256 可复算且与对应 JSON 文件内容一致。

---

## 用例 E：敏感信息红线检查（不得落盘任何凭据/Token/环境变量明文）

步骤：
1. 对证据目录做关键字搜索（示例）：
   - `rg -n "CWS_|TOKEN|SECRET|API_KEY|REFRESH_TOKEN" docs/evidence/v1-67`

期望：
- 不出现任何发布凭据/Token/环境变量明文。

---

## 用例 F：可重复性（同一份输入重跑 deterministic）

步骤：
1. 使用稳定 exportedAt 连续执行两次：
   - `node --no-warnings=ExperimentalWarning --loader=ts-node/esm scripts/build-cws-listing-diff-evidence-pack.ts --stable-exported-at`
2. 分别计算输出哈希（示例）：
   - `shasum -a 256 docs/evidence/v1-67/index.md docs/evidence/v1-67/cws-listing-diff-evidence-pack-*.json`

期望：
- 两次输出完全一致（无随机字段/顺序稳定；门禁已接入 `bash scripts/test.sh` 自动校验）。

---

## 自动化回归记录（可发布审计证据）

- 统一入口：`bash scripts/test.sh`
- 最近执行日期：2026-03-21
- 结论：PASS

