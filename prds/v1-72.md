# 问题修复和功能优化（V1-72 CWS Listing 敏感词/夸大口径门禁定稿 + 自动化扫描取证：解除 v1-71/v1-69 的口径阻塞）

## 目标
基于 `docs/roadmap_status.md` 的收入优先 Top3：Top1「（v1-69）真实 CWS 发布 + 商店端取证闭环」仍受网络/代理阻塞；因此本轮选择 Top2「（v1-72）敏感词/夸大口径门禁口径定稿 + 自动化扫描/证据落盘」作为 1~3 天最小可交付增量主题。

收入第一：把“上架获客入口”的审核风险与临场争议，固化为可执行的自动化门禁与可审计证据；在不改动 v1-68 Listing 口径的前提下，解除 v1-71 的 BLOCKED-2（否定语境免责声明命中关键词即阻断），确保网络恢复后可以直接执行 v1-71 Listing 同步与 v1-69 真实发布，而不会因为口径不一致导致返工或审核拒绝。

## ULTRA IMPORTANT 第一原则：任务开始后，不要停下来，直到完成目标
**所有细节，都由你来定，判断依据是根据核心目标决定应该如何做，而不要停下来问我**

## 具体任务
1、定稿“敏感词/夸大口径”门禁口径（文档化，可复用、可审计）
  - 新增规范文档：`docs/publish/cws-listing-redlines-policy.md`（本轮唯一口径）
  - 必须明确（写成可执行规则，而不是原则性描述）：
    - Keywords：零容忍（出现 `subscribe/subscription/pricing/paid/payment/upgrade/premium/付费/订阅/收费/购买/价格/会员/内购/高级版` 等即 BLOCK）
    - 长描述（EN/ZH）：只拦截“误导性宣称”（例如“Pro 已上线/可订阅/可付费/Subscribe now/Upgrade to Pro”），允许“否定语境免责声明”（例如“Pro 尚未上线，不支持订阅/付费”）存在
    - 证据化要求：门禁必须输出“命中清单（含文件/行号/命中词/归因规则）”，并落盘到 `docs/evidence/v1-72/`，保证审计可复核

2、落地自动化扫描与证据输出（替代手工 `rg` 争议，收入优先）
  - 新增脚本：`scripts/scan-cws-listing-redlines.ts`
    - 输入：`docs/ChromeWebStore-Description-EN.md`、`docs/ChromeWebStore-Description-ZH.md`、`docs/aso/keywords.md`
    - 输出目录：`docs/evidence/v1-72/`（固定）
    - 输出文件（文件名固定，便于审计与复用）：
      - `docs/evidence/v1-72/cws-listing-redlines-scan.json`：命中明细（file/line/term/rule/result=allowed|blocked）
      - `docs/evidence/v1-72/index.md`：扫描摘要（PASS/BLOCKED + 修复建议 + 引用源 sha256）
    - 断言：
      - 若存在 blocked 命中：脚本退出码非 0（阻断 `bash scripts/test.sh`）
      - 若仅有 allowed 命中：脚本退出码 0（允许免责声明出现，但必须在 index.md 中记录 allowed 命中的口径归因）
  - 接入门禁：在 `scripts/test.sh` 增加 1 步执行扫描脚本（保证 CI 与本地回归一致）
  - 单元测试：在 `scripts/unit-tests.ts` 增加用例，覆盖：
    - “否定语境免责声明命中 payment/subscription/付费/订阅”应为 allowed
    - “误导性宣称命中（Pro 已上线 / subscribe now / upgrade to pro）”应为 blocked
    - keywords 命中任意敏感词应为 blocked

3、解除 v1-71 的 BLOCKED-2（把“口径阻塞”变成“网络阻塞”，为上架做准备）
  - 更新 `docs/evidence/v1-71/index.md`：
    - 将 BLOCKED-2 改为 PASS（引用 `docs/publish/cws-listing-redlines-policy.md` 与 `docs/evidence/v1-72/cws-listing-redlines-scan.json` 的结论）
    - 保留 BLOCKED-1（网络/商店不可达）不变

4、测试/用例与汇报材料闭环（可执行、可复现、可追责）
  - 新增测试用例文档（固定路径，必须可逐条执行）：`docs/test-cases/v1-72.md`
    - 覆盖：口径文档 -> 扫描脚本 -> 证据落盘 -> `bash scripts/test.sh` 门禁 -> v1-71 索引状态更新
    - 必须包含一次回归记录（日期 + PASS/BLOCKED + 证据文件名）
  - 新增简报（固定路径建议）：`docs/reports/v1-72-report.md`
    - 只写结论：本轮门禁口径是什么、解除的阻塞是什么、形成的商业化证据是什么、对 v1-69/v1-71 的衔接说明
  - 更新 `docs/roadmap_status.md`：刷新 Top3 与阻塞（保持收入优先 + 阻塞不断档）

## 验收标准
* 修复上述所有问题，并测试通过，达到可发布状态
* 实现以上所有需求，并测试通过，达到可发布状态
* 可验证的商业化推进证据（必须可审计/可复核/可复用）：
  - `docs/publish/cws-listing-redlines-policy.md` 存在，且明确“keywords 零容忍 + 长描述允许否定语境免责声明”的可执行口径
  - `docs/evidence/v1-72/index.md` 存在，且给出 PASS/BLOCKED 结论，并引用：
    - `docs/evidence/v1-72/cws-listing-redlines-scan.json`
    - Listing 来源文件 sha256（与 v1-68 `inputs.sha256` 可互证）
  - `docs/evidence/v1-71/index.md` 的 BLOCKED-2 已解除（改为 PASS），仅保留网络相关 BLOCKED-1（若仍不可达）
* 测试与用例可执行：
  - `docs/test-cases/v1-72.md` 可逐条执行并包含一次回归记录（日期 + PASS/BLOCKED + 证据文件名）
  - `bash scripts/test.sh` 全量通过（包含新增的扫描门禁）

## 注意
1、以上任务为“最小可交付增量（1-3 天）”，收入第一：本轮只做“CWS Listing 合规门禁口径定稿 + 自动化扫描取证 + 解除口径阻塞”，不引入支付/订阅、不做与上架/获客/转化取证无关的体验微调
2、隐私与合规红线：任何证据/索引不得包含 token/secret；不得落盘用户复制内容/网页内容/URL/标题；不得在文案中暗示“Pro 已上线/可订阅/可付费”
3、门禁策略边界：keywords 继续零容忍；长描述允许出现“否定语境免责声明”（用于降低误导与投诉），但必须可被自动化规则识别并在证据索引中解释归因
4、根据需求创建测试用例文档，通过单元测试和自动化测试来确保代码的正确性（本次测试用例文档路径固定为 `docs/test-cases/v1-72.md`）
5、目标任务成功完成之后，写一份简要的汇报材料文档（固定路径：`docs/reports/v1-72-report.md`），说清楚每一个具体任务现在是什么状态（是否按要求完成了），已经形成了什么商业化证据，以及本次对代码/文档的修改范围（涉及哪些目录和文件）

