# V1-20-1 拆分：反馈模板补齐漏斗摘要（Issue 模板附带 GrowthFunnelSummary）

## 范围（MVP）
在不新增权限、不新增任何联网发送、且不包含任何网页/复制内容的前提下，扩展 Popup/Options 的“反馈与建议”Issue 模板：在现有 `copilot_settings` 快照 + `copilot_growth_stats` +（last N）`copilot_telemetry_events` 的基础上，追加一段可审计且隐私合规的“本地漏斗摘要”（`copilot_growth_funnel_summary`，含“首次打开 Popup 后 3 分钟内首次成功复制”判断），并补齐单测、用例文档与可发布交付物。

## 子需求清单（按 MVP）
- [x] 反馈模板生成能力扩展：注入并渲染 `GrowthFunnelSummary`（严格隐私合规、仅读取不落盘）
  - [x] `src/shared/word-of-mouth.ts`：类型扩展（`BuildFeedbackIssueParams.growthFunnelSummarySnapshot?: GrowthFunnelSummary`）
  - [x] `src/shared/word-of-mouth.ts`：序列化为 pretty JSON，缺失/失败兜底 `{}`；作为新占位符注入 `feedbackIssueBodyTemplate`
  - [x] 约束：仅允许 number/boolean 标量输出（白名单字段 + 类型过滤）；不新增任何 storage 写入；URL 超长仅截断 telemetry events
- [x] Popup 与 Options 反馈入口接入漏斗摘要（不新增事件名、不改动现有入口行为）
  - [x] `src/popup/popup.ts`：读取 `growthStatsSnapshot` 后构造 `growthFunnelSummarySnapshot` 并注入 `buildFeedbackIssueUrl(...)`
  - [x] `src/options/options.ts`：`buildWomFeedbackUrl()` 同步注入 `growthFunnelSummarySnapshot`
  - [x] 失败兜底：任意读取/构造失败不阻断打开反馈页（仅 `console.warn`）
- [x] i18n 模板更新（中英文一致、可通过 check-i18n）
  - [x] `_locales/en/messages.json` / `_locales/zh/messages.json`：`feedbackIssueBodyTemplate` 新增 `copilot_growth_funnel_summary` 段落（JSON code block）
  - [x] 新增占位符 `GROWTH_FUNNEL_JSON`，并确保占位符序号与 substitutions 顺序一致
- [x] 单测与测试用例文档（可执行、可回归、可发布）
  - [x] `scripts/unit-tests.ts`：更新 `getMessage('feedbackIssueBodyTemplate', substitutions)` stub 覆盖新增占位符；补齐关键断言
  - [x] `docs/test-cases/v1-20.md`：补齐用例 A-D + `bash scripts/test.sh` 执行记录
  - [x] `docs/reports/v1-20-report.md`：发布简报
  - [x] `bash scripts/test.sh` 全量通过

## 当前状态
- 状态：done
- 最近更新：2026-03-19

