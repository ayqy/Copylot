# V1-28 对外口径一致性修复：CWS 长描述/ASO 素材/关键词矩阵收敛 + 教程入口补齐 测试用例

## 目标
验证 V1-28 达到可发布（不新增权限、不引入任何联网发送、且不改动核心复制/Prompt/引导主流程的前提下）：
- `docs/ChromeWebStore-Description-EN.md` / `docs/ChromeWebStore-Description-ZH.md` 已完成口径收敛：不包含“提示符自动移除/remove prompts”等未实现能力；“代码块清理”描述与实现/教程一致；并包含 3 个可复用教程入口 URL
- `docs/aso/store-assets.md`（截图 05）与 `docs/aso/keywords.md` 已完成口径纠偏：不再宣称“去提示符”，且与 `docs/aso/value-prop.md` 一致
- 用例闭环可执行：按文档完成用例 A-D 并记录结论
- `bash scripts/test.sh` 全量通过

## 预置条件
- 仓库已包含并可读取：
  - `docs/ChromeWebStore-Description-EN.md`
  - `docs/ChromeWebStore-Description-ZH.md`
  - `docs/aso/store-assets.md`
  - `docs/aso/keywords.md`
  - `docs/aso/value-prop.md`
  - `docs/tutorials/table-to-csv-markdown.md`
  - `docs/tutorials/prompt-workflow.md`
  - `docs/tutorials/code-block-cleaning.md`
  - 代码佐证：`src/shared/code-block-cleaner.ts`、`src/shared/content-processor.ts`
- 可执行 `bash scripts/test.sh`

## 用例 A（CWS 长描述口径：不夸大 + 可佐证）
检查项：
1. `docs/ChromeWebStore-Description-EN.md` / `docs/ChromeWebStore-Description-ZH.md` 不包含以下未实现或夸大表述：
   - “提示符自动移除/命令行提示符清理”
   - “remove prompts”
2. 两份长描述中“代码块清理”相关表述必须与实现/教程一致（可审计）：
   - Copy/复制按钮文案：仅首/末端整行保守移除（不做行内子串替换，见 `src/shared/code-block-cleaner.ts`）
   - 行号：仅对可识别结构有限支持（不承诺覆盖所有站点，见 `src/shared/content-processor.ts` 的 `ol.hljs-ln` 分支）
   - 缩进与空行：保留内部缩进与空行，仅裁剪首尾空行（见 `extractCodeBlockText()`）
   - 对外口径需与 `docs/tutorials/code-block-cleaning.md` 一致（保守去噪边界清晰）

执行记录：
- 结果：通过
- 备注：两份长描述未出现“提示符自动移除/remove prompts”等表述；代码块清理口径与教程/实现一致（保留缩进与空行、仅裁剪首尾空行、Copy 文案仅首尾整行保守移除、行号仅可识别结构有限支持）。

## 用例 B（教程入口可用：3 个 URL 可直接复用）
检查项：
1. `docs/ChromeWebStore-Description-EN.md` / `docs/ChromeWebStore-Description-ZH.md` 均包含 “Learn more / Tutorials” 或 “了解更多 / 教程”段落。
2. 段落内包含以下 3 个纯文本 URL（逐行列出，非 Markdown 链接语法也可）：
   - `https://github.com/ayqy/copy/blob/main/docs/tutorials/table-to-csv-markdown.md`
   - `https://github.com/ayqy/copy/blob/main/docs/tutorials/prompt-workflow.md`
   - `https://github.com/ayqy/copy/blob/main/docs/tutorials/code-block-cleaning.md`
3. 本地侧校验：URL 的路径对应的仓库文件真实存在（可被 GitHub 渲染打开）。

执行记录：
- 结果：通过
- 备注：两份长描述均包含 3 条教程入口 URL；对应文件在仓库 `docs/tutorials/` 中存在，发布到 GitHub 后可直接访问渲染页。

## 用例 C（ASO 素材口径：截图 05 不再宣称“去提示符”）
检查项（`docs/aso/store-assets.md` 截图 05）：
1. 标题（ZH/EN）不包含“去提示符/remove prompts”等不实表述。
2. 拍摄步骤口径与 `docs/aso/value-prop.md` 一致：
   - 保留缩进与空行，仅裁剪首尾空行
   - Copy 文案仅首/末端整行保守移除（不做行内替换）
   - 行号仅在结构可识别时去除，不承诺覆盖所有站点
   - 保留“悬停复制”入口

执行记录：
- 结果：通过
- 备注：截图 05 标题与步骤已收敛为可审计口径，且与 `docs/aso/value-prop.md` 差异化卖点 2 描述一致。

## 用例 D（关键词矩阵口径：不包含“去提示符”，并记录替换清单）
检查项（`docs/aso/keywords.md`）：
1. 不包含“去提示符”等会造成能力夸大的关键词。
2. 记录本次替换后的关键词清单（用于回归与审计）。

替换记录（本次）：
- 移除：`去提示符`
- 新增替代：`代码块去噪`、`去 Copy 文案（保守）`、`保留缩进`

执行记录：
- 结果：通过
- 备注：关键词矩阵不再包含“去提示符”；替换词汇均可由实现/教程佐证（保守去噪、保留缩进/空行、Copy 文案首尾整行移除）。

## 自动化回归：统一入口（验收前必须填写）
步骤：
- 执行：`bash scripts/test.sh`

期望：
- lint/type-check/check-i18n/unit-tests/build:prod 全量通过。

执行记录：
- 命令：`bash scripts/test.sh`
- 结果：通过
- 最近执行日期：2026-03-19
