# V1-98 xhs 可转化样本扩展与收入证据批量回填测试用例

## 目标

验证 `prds/v1-98.md` 的三项任务达成：
- 新增至少 3 条真实可追溯 xhs 样本并完成批量回填；
- 证据包可导出且可复核 `clicks -> installs -> proIntentSignals`；
- 阻塞顺延记录在 `roadmap_status / blocked / todo / execution` 一致。

## 前置条件

- 主索引：`docs/growth/assets/social/xhs/v1-96/conversion-evidence-index.json`
- 执行记录：`docs/growth/executions/v1-98-growth-backfill.md`
- 证据目录：`docs/evidence/v1-98/`

## 用例 A：样本回填完整性

步骤：
1. 检查 `conversionEntries` 新增样本数量是否 >= 3。
2. 检查新增样本 `postUrl` 非占位值，且 `clicks/installs/proIntentSignals` 字段完整。
3. 检查 `auditChainSamples` 的新增链路 `postUrl` 已回填。
4. 证据落盘：`docs/evidence/v1-98/case-a-backfill-completeness.json`。

预期：
- 结果 PASS；
- 新增样本数 >= 3；
- 至少 2 条新增样本具备完整三项量化字段。

## 用例 B：归因一致性

步骤：
1. 核验新增样本 `campaign/source/medium` 均为 `v1_96_growth_regression/xhs/organic_social`。
2. 核验样本与目标入口映射关系：官网/CWS/Pro 候补。
3. 对照执行记录复核入口映射表。
4. 证据落盘：`docs/evidence/v1-98/case-b-attribution-consistency.json`。

预期：
- 结果 PASS；
- 三类入口映射正确；
- 归因参数无漂移。

## 用例 C：商业化证据可导出

步骤：
1. 校验证据索引可定位到 `conversion-funnel-v1-98.csv`、`sample-audit-v1-98.json`、截图索引。
2. 复核导出文件中的 `clicks/installs/proIntentSignals` 与执行记录一致。
3. 证据落盘：`docs/evidence/v1-98/case-c-export-evidence-check.json`。

预期：
- 结果 PASS；
- 可从索引快速定位证据文件；
- 漏斗量化字段可复盘。

## 用例 D：阻塞分流

步骤：
1. 核验 `docs/roadmap_status.md` 是否记录 Top1 阻塞：`CWS 权限 + source ~/.bash_profile && pxy 未就绪`。
2. 核验 `docs/growth/blocked.md` 与 `docs/growth/todo.md` 是否同步所需人类输入与替代动作。
3. 核验 `docs/growth/executions/v1-98-growth-backfill.md` 是否记录顺延执行 Top2。
4. 证据落盘：`docs/evidence/v1-98/case-d-blocked-diversion-check.txt`。

预期：
- 结果 PASS 或 BLOCKED（若阻塞信息缺失）；
- 本轮阻塞分流记录四方一致。

## 统一门禁执行

- 执行命令：`bash scripts/test.sh`
- 预期：退出码 `0`
- 日志证据：`docs/evidence/v1-98/test.sh.log`

## 回归记录（本轮）

| 用例 | 结果 | 证据文件 | 备注 |
| --- | --- | --- | --- |
| A 样本回填完整性 | PASS | `docs/evidence/v1-98/case-a-backfill-completeness.json` | 新增样本 3 条，字段完整 |
| B 归因一致性 | PASS | `docs/evidence/v1-98/case-b-attribution-consistency.json` | `campaign/source/medium` 与入口映射一致 |
| C 商业化证据可导出 | PASS | `docs/evidence/v1-98/case-c-export-evidence-check.json` | 可导出 CSV/JSON/截图索引并复核量化字段 |
| D 阻塞分流 | PASS | `docs/evidence/v1-98/case-d-blocked-diversion-check.txt` | `roadmap_status/blocked/todo/execution` 一致 |
| 统一门禁 `scripts/test.sh` | PASS | `docs/evidence/v1-98/test.sh.log` | lint/type-check/build/证据脚本通过 |
