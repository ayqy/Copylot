Outside paragraph (A): select text here to verify non-table selection still uses precise selection. **Bold** and [link](https://example.com/outside).

| Col A | Col B |
| --- | --- |
| Cell A1 with [link](https://example.com/a1) and inline `code`. | Cell B1 with **bold** and *em*. |
| Cell A2 plain text. | Cell B2 with nested inline elements: [`code-link`](https://example.com/b2). |

Outside paragraph (B): used to verify conversion behavior when selection is outside the table.
