# 问题修复和功能优化（V1-58 渠道分发/投放效率复盘一键化：按 campaign 合并导出 leads + distCopies + leads_per_dist_copy（7d））

## 目标
基于 `docs/roadmap_status.md` 的收入优先 Top3：由于 Top1/Top2 仍受“需要可用代理/VPN + 商店可达”的网络可达性阻塞，本轮顺延选择 Top3「渠道分发/投放跑数与复盘固化 v4」作为 1~3 天最小可交付增量主题。

收入第一：在不引入支付/订阅、不新增权限、不联网发送数据、不采集用户复制内容的前提下，把渠道投放复盘从“导出两张表手工对齐”升级为**一键可导出、可审计、可复核**的商业化获客效率证据资产——新增「按 campaign 合并导出 7d 获客效率（leads + distCopies + leads_per_dist_copy）」入口，用于快速判断“分发动作 -> Pro 留资（付费意向）”的渠道效率差异，并为真实发布后的投放优化与转化验证提供可量化证据。

## ULTRA IMPORTANT 第一原则：任务开始后，不要停下来，直到完成目标
**所有细节，都由你来定，判断依据是根据核心目标决定应该如何做，而不要停下来问我**

## 具体任务
1、新增「按 campaign 合并导出 Pro 获客效率（7d CSV）」入口（让渠道效率对比可一键复核）
  - 入口位置：Options -> 隐私与可观测性 -> Pro 面板（与 v1-54/v1-57 的导出入口同区域）
  - 稳定 DOM（必须固定，便于用例/断言/回归）：`#export-pro-acquisition-efficiency-by-campaign-7d-csv`
  - 开关约束：匿名使用数据 OFF 时入口置灰且不读取/不推断本地 events；ON 时允许导出
  - 时间窗：过去 7 天（window 口径必须与 v1-51/v1-54/v1-57 保持一致，便于互证）
  - 数据口径（必须可复核）：
    - `leads`：复用 v1-54 定义（`pro_waitlist_copied + pro_waitlist_survey_copied`，按 campaign 聚合）
    - `distCopies`：复用 v1-57 定义（`pro_distribution_asset_copied` 按 action 聚合求和，按 campaign 聚合）
    - `leads_per_dist_copy`：`leads / distCopies`（当 `distCopies = 0` 时输出 `N/A`，避免除零与误读）
    - campaign 空值：必须与 v1-54/v1-57 一致，归入“空 campaign”桶（用于发现分发时漏填）
  - CSV 固定字段建议（字段顺序必须稳定，便于审计/对比/单测）：
    - `exportedAt/extensionVersion/windowFrom/windowTo/lookbackDays/campaign/leads/distCopies/leadsPerDistCopy`
  - 实现建议（必须可单测的纯函数，避免在 UI 层拼口径）：
    - 新增 `src/shared/pro-acquisition-efficiency-by-campaign-csv.ts`：单次 sanitize/trim 后同时派生 leads 与 distCopies 聚合，并输出合并 rows + CSV
    - 导出文件名规范：`copylot-pro-acquisition-efficiency-by-campaign-7d-YYYY-MM-DD.csv`
  - 兼容性红线：不得修改 v1-51/v1-54/v1-57 既有导出字段/文件名/统计口径；本需求仅新增入口与新增导出

2、补齐自动化测试 + 可执行用例（保证 1~3 天可交付、可回归）
  - 自动化测试加固（优先纯函数 + `scripts/unit-tests.ts`）：
    - 7d window 边界一致性（与 v1-51/v1-54/v1-57 互证）
    - campaign 聚合与空桶规则（至少 2 个 campaign + 空 campaign）
    - 合并对齐规则：同一 campaign 下 `leads/distCopies` 必须与 v1-54/v1-57 各自导出结果可复算一致
    - `leads_per_dist_copy` 的 N/A/小数格式（建议保留 4 位小数或原始浮点字符串，但必须稳定且可测试）
    - 匿名开关行为：OFF 入口置灰且导出为空表头；ON 可导出且行数正确
  - 新增测试用例文档（固定路径）：`docs/test-cases/v1-58.md`
    - 覆盖：匿名 OFF/ON；导出 v1-54 leads、v1-57 distCopies、本次 v1-58 合并导出三方互证；在表格中复算 `leads_per_dist_copy` 与导出一致
    - 必须包含一次回归记录（日期 + PASS），并要求 `bash scripts/test.sh` 全量通过

3、证据落盘与复盘口径固化（让商业化推进“做完可证明”）
  - 证据目录（固定路径）：`docs/evidence/v1-58/` + `docs/evidence/v1-58/index.md`
    - 至少落盘（脱敏、可审计、可复核）：
      - `pro-acquisition-efficiency-by-campaign-7d.sample.csv`：合并导出样例（含至少 2 个 campaign + 空 campaign 行）
      - 截图索引：导出入口（匿名 OFF 置灰/匿名 ON 可点击）+ 导出文件（下载成功）
      - 复盘口径：说明如何用 v1-54 `leads` 与 v1-57 `distCopies` 复算并校验 v1-58 的合并结果（避免“做完不可证明”）
  - 更新 `docs/roadmap_status.md`：在阶段里程碑中增加 v1-58 条目，并刷新“当前进度/Top3/阻塞”描述（断点续孵化不断档）

## 验收标准
* 修复上述所有问题，并测试通过，达到可发布状态
* 实现以上所有需求，并测试通过，达到可发布状态
* 可验证的商业化推进证据（必须可审计/可导出/可复用）：
  - Options -> 隐私与可观测性 -> Pro 面板存在合并导出入口，且稳定 DOM 为 `#export-pro-acquisition-efficiency-by-campaign-7d-csv`
  - 匿名开关 OFF：入口置灰且不读取/不推断本地 events；匿名开关 ON：可成功导出 7d 合并 CSV
  - 合并 CSV 可复核：任意 campaign 行的 `leads` 与 v1-54 导出一致、`distCopies` 与 v1-57 导出一致，且 `leads_per_dist_copy` 可由表格按公式复算一致（`distCopies=0` 输出 N/A）
  - 证据落盘可审计：`docs/evidence/v1-58/index.md` 至少包含（1）样例 CSV（2）截图索引（3）复盘口径与互证步骤，能支撑“按 campaign 的获客效率对比”复盘与审计
* 测试与用例可执行：
  - `docs/test-cases/v1-58.md` 可逐条执行并包含一次回归记录（日期 + PASS），且 `bash scripts/test.sh` 全量通过

## 注意
1、以上任务为“最小可交付增量（1-3 天）”，收入第一：本轮只做“按 campaign 的获客效率合并导出 + 用例/证据/测试闭环”，不引入支付/订阅、不新增权限、不做与商业化取证无关的体验微调
2、保持口径兼容：不得修改 v1-50/v1-51/v1-54/v1-57 的既有导出字段与统计口径；本轮只新增合并导出作为复盘加速器
3、根据需求创建测试用例文档，通过单元测试和自动化测试来确保代码的正确性（本次测试用例文档路径固定为 `docs/test-cases/v1-58.md`）
4、目标任务成功完成之后，写一份简要的汇报材料文档（固定路径：`docs/reports/v1-58-report.md`），说清楚每一个具体任务现在是什么状态（是否按要求完成了），已经实现了什么效果，以及本次对代码的修改范围（涉及哪些目录和文件）
5、隐私与合规红线：不得新增扩展权限；不得上传/采集用户复制内容；导出仅基于本地匿名事件日志派生（字段白名单）；campaign 仅来源于用户手动输入/本地设置且需通过既有校验（不得包含敏感信息）；匿名使用数据默认 OFF，OFF 时不记录、不缓存、不补发

