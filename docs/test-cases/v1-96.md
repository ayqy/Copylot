# V1-96 收入回填最小增量（转化入口参数统一与漏斗证据补齐）测试用例

## 目标

验证 `prds/v1-96.md` 的三项核心要求：
- 官网 / CWS / Pro 候补三类入口参数口径一致；
- `conversion-evidence-index` 可导出且字段完整、可追溯；
- Top1 阻塞被明确记录后，Top2 交付仍可独立闭环。

## 前置条件

- 使用仓库内现有 V1-96 资产与执行记录。
- CWS 实操仍受阻塞：缺少 Dashboard 权限且代理未启动（需 `source ~/.bash_profile && pxy`）。

## 用例 A：三类入口参数一致性校验（官网/CWS/Pro 候补）

步骤：
1. 执行参数一致性检查并生成证据：`docs/evidence/v1-96/case-a-params-check.json`。
2. 校验 `conversionEntries` 的 `campaign/source/medium` 是否全部等于命名规则。
3. 校验 `targetUrl` 是否都包含 `utm_campaign/utm_source/utm_medium`。

预期：
- 结果为 PASS；
- 不存在参数口径漂移或缺失 UTM 参数。

## 用例 B：`conversion-evidence-index` 字段完整性与可追溯性校验

步骤：
1. 执行字段完整性检查并生成证据：`docs/evidence/v1-96/case-b-field-check.json`。
2. 校验每条记录包含：`channel/postUrl/targetUrl/campaign/source/intentSignal/evidencePath`。
3. 校验 `evidencePath` 指向文件在仓库中存在。

预期：
- 结果为 PASS；
- 所有记录都可追溯到入口或意向证据文件。

## 用例 C：阻塞分流校验（Top1 阻塞记录 + Top2 独立闭环）

步骤：
1. 检查 `docs/roadmap_status.md` 是否记录 Top1 阻塞原因为 `CWS 权限 + 代理未启动`，并包含修复命令 `source ~/.bash_profile && pxy`。
2. 检查 `docs/growth/blocked.md` 是否同步记录所需人类输入与无输入替代动作。
3. 将检查结果落盘：`docs/evidence/v1-96/case-c-blocking-check.txt`。

预期：
- 结果为 PASS；
- Top1 阻塞已记录，且 Top2 交付与替代动作可独立执行。

## 统一门禁执行

- 执行命令：`bash scripts/test.sh`
- 预期：退出码 `0`
- 本轮日志证据：`docs/evidence/v1-96/test.sh.log`

## 回归记录（本轮）

| 用例 | 结果 | 证据文件 | 备注 |
| --- | --- | --- | --- |
| A 入口参数一致性 | PASS | `docs/evidence/v1-96/case-a-params-check.json` | 三类入口 `campaign/source/medium` 一致且 URL 参数完整 |
| B 字段完整与追溯 | PASS | `docs/evidence/v1-96/case-b-field-check.json` | 必填字段齐全，`evidencePath` 可落到实际文件 |
| C 阻塞分流校验 | PASS | `docs/evidence/v1-96/case-c-blocking-check.txt` | Top1 阻塞已记录，Top2 闭环与替代动作可执行 |
| 统一门禁 `scripts/test.sh` | PASS | `docs/evidence/v1-96/test.sh.log` | 全量测试通过（含 lint/type-check/build/证据脚本回归） |
