# V1-29 商业化边界收敛：Pro 功能包/免费功能清单固化 + Pro Tab 口径补齐 + 候补模板强化 测试用例

## 目标
验证 V1-29 达到可发布（不新增权限、不引入任何联网发送、且不影响现有免费核心功能的前提下）：

- 已新增并可审计的 Pro 边界文档 `docs/monetization/pro-scope.md`：明确 Free（永久免费）与 Pro（规划/候补）清单与边界原则；口径与隐私政策一致，不暗示 Pro 已上线
- Options -> Pro Tab 口径补齐：清晰展示“永久免费能力清单”与“Pro 规划提示”；文案可 i18n；新增“了解更多/查看 Pro 边界文档”入口；DOM 稳定（用于用例与未来自动化）
- 候补登记 Issue 模板已强化：包含 Pro scope 段落与可审计文档入口；仍不预填任何网页内容/复制内容/URL/标题，仅包含环境信息与用户自填段落
- 用例闭环可执行：按文档完成用例 A-D 并记录结论
- `bash scripts/test.sh` 全量通过

## 预置条件
- 仓库已包含并可读取：
  - `docs/monetization/pro-scope.md`
  - `docs/privacy-policy.md`
  - `docs/aso/value-prop.md`
  - `src/options/options.html`
  - `src/options/options.css`
  - `src/options/options.ts`
  - `_locales/en/messages.json`
  - `_locales/zh/messages.json`
- 可执行 `bash scripts/test.sh`

## 用例 A（文档可审计：Pro scope 文档存在且不暗示已上线）
检查项：
1. `docs/monetization/pro-scope.md` 文件存在。
2. 文档至少包含四个明确部分：
   - Free（永久免费，不做付费墙）
   - Pro（规划/候补，不代表已实现）
   - 边界与原则
   - 非目标（Non-goals）
3. 文案不暗示 Pro 已上线/已提供（例如避免“已支持/已提供 Pro 功能”等表述），并明确“规划中/未实现/未上线”。
4. 隐私口径与 `docs/privacy-policy.md`、`docs/aso/value-prop.md` 一致（不收集/不上传复制内容；匿名使用数据默认关闭且仅本地记录、不联网发送）。

执行记录：
- 结果：通过
- 备注：`docs/monetization/pro-scope.md` 已包含 Free/Pro/边界与原则/非目标四部分，并在 Pro 清单中明确“规划中/未实现/未上线”；隐私口径与隐私政策/ASO 话术一致。

## 用例 B（Pro Tab 口径：Free vs Pro Planned 清晰，DOM 稳定）
检查项：
1. Options -> Pro Tab 可见“永久免费（Free）能力清单”区块，且容器 DOM ID 为：
   - `#pro-free-scope`
2. Pro 规划区块可见明确提示“规划中/未上线/Not shipped yet”，且容器 DOM ID 为：
   - `#pro-planned-scope`
3. Pro Tab 包含“了解更多/查看 Pro 边界文档”入口，DOM ID 为：
   - `#pro-scope-learn-more`
4. 入口打开链接固定为：
   - `https://github.com/ayqy/copy/blob/main/docs/monetization/pro-scope.md`
5. Pro Tab 文案不出现“已提供/已支持”等暗示 Pro 已实现的表述（Pro 能力清单需统一为规划/候补口径）。
6. Free/Pro 相关文案可 i18n（EN/ZH messages 均有对应 key）。

执行记录：
- 结果：通过
- 备注：Pro Tab 已新增 Free 清单与 Pro 规划提示，并提供固定文档入口；新增稳定 DOM：`#pro-free-scope` / `#pro-planned-scope` / `#pro-scope-learn-more`；Pro 能力清单以“规划中/未上线”口径展示。

## 用例 C（候补模板口径：包含 Pro scope 段落 + 文档入口，且满足隐私约束）
检查项：
1. 打开“加入候补名单”（GitHub `issues/new`）后，Issue body 中包含 Pro scope 段落：
   - 明确“现有免费核心功能不会被收费（不做付费墙）”
   - 明确“Pro 仅为可选增强，处于规划/候补阶段（未上线）”
2. Pro scope 段落包含 1 条可审计文档入口（固定为）：
   - `https://github.com/ayqy/copy/blob/main/docs/monetization/pro-scope.md`
3. 保持严格隐私：Issue body 不预填任何网页内容/复制内容/用户访问 URL/标题，仅包含扩展版本/扩展 ID/语言信息与用户自填段落。
4. `npm run check-i18n` 通过（包含对 `_locales/en/messages.json` 与 `_locales/zh/messages.json` 的改动回归）。

执行记录：
- 结果：通过
- 备注：候补 Issue 模板已新增 Pro scope 段落与固定文档入口；模板内容仍不包含网页/复制内容/用户访问 URL/标题，仅保留环境信息与用户自填段落；`npm run check-i18n` 已通过。

## 用例 D（回归：不影响 WOM 区块与匿名使用数据开关行为）
检查项：
1. Pro Tab WOM 区块仍存在（分享/评价/反馈入口不受影响），且 DOM ID 不变：
   - `#wom-actions-panel`
2. “匿名使用数据”开关默认关闭、行为不变（仅本地记录、不联网发送；关闭会清空本地事件）。

执行记录：
- 结果：通过
- 备注：本次仅收敛 Pro 口径与文档/模板，不涉及 WOM 区块与匿名使用数据逻辑修改。

## 自动化回归：统一入口（验收前必须填写）
步骤：
- 执行：`bash scripts/test.sh`

期望：
- lint/type-check/check-i18n/unit-tests/build:prod 全量通过。

执行记录：
- 命令：`bash scripts/test.sh`
- 结果：通过
- 最近执行日期：2026-03-19

