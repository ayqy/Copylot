# v4-3-5 Options Pro 复核入口与 stay_validation 话术

## 目标

把跨 campaign 复核能力接入 `Options -> Pro` 既有验证链路，并把所有新入口话术锁定在 `stay_validation`。

## 状态

- [x] `Options -> Pro` 已新增“跨 campaign 领先路线复核包”区块。
- [x] 区块支持复制 Markdown 摘要与下载 JSON。
- [x] 中英文文案继续强调“当前优先验证方向 / stay_validation”与隐私边界。
- [x] 新入口未新增支付、问卷、候补、留资或其他偏航承接。

## 具体任务

1. 更新 `src/options/options.html`、`src/options/options.ts`，接入复核区块与复制/下载事件。
2. 更新 `_locales/en/messages.json`、`_locales/zh/messages.json`，补齐 branch 文案、边界提示与通知消息。
3. 保持复核区块与既有 `comparison / writeback / stability / verdict / audit` 同口径，不引入新的并行统计体系。
