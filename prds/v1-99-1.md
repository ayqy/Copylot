# v1-99-1 三入口新增样本持续回填（官网/CWS/Pro 候补）

## 子目标
在统一归因口径下持续回填 3 条可追溯新增样本，覆盖 `official_site`、`chrome_web_store`、`pro_waitlist` 三类入口，提升收入漏斗样本密度。

## 具体任务
- 更新 `docs/growth/assets/social/xhs/v1-96/conversion-evidence-index.json`：
  - 追加 3 条 `conversionEntries` 新样本；
  - 追加 3 条 `auditChainSamples` 链路样本；
  - 更新 `backfillMeta` 为 v1-99 轮次并补齐汇总指标；
  - 保持 `campaign/source/medium` 统一口径。
- 每条新增样本补齐字段：
  - `postUrl`、`targetUrl`、`campaign`、`source`、`medium`、`intentSignal`
  - `clicks`、`installs`、`proIntentSignals`、`evidencePath`

## 验收标准
- 新增样本数 >= 3，且三入口各至少 1 条。
- 新增样本归因口径一致（`v1_96_growth_regression / xhs / organic_social`）。
- 至少 2 条新增样本满足 `clicks > 0` 且（`installs > 0` 或 `proIntentSignals > 0`）。
