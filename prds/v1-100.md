# 问题修复和功能优化（V1-100 Pro 意向转化最小增量：三入口 CTA 对齐 + 问卷最短路径 + 可导出漏斗证据）

## 目标
实现具体任务中要求的所有功能和问题修复（在 `docs/roadmap_status.md` Top1「CWS 权限 + `source ~/.bash_profile && pxy`」阻塞未解除时，顺延执行 Top3 第 2 项），使“升级入口点击 -> 意向留资提交 -> 证据导出复盘”链路在 1-3 天内形成可验证、可审计、可复盘的最小商业化增量。

## ULTRA IMPORTANT 第一原则：任务开始后，不要停下来，直到完成目标
**所有细节，都由你来定，判断依据是根据核心目标决定应该如何做，而不要停下来问我**

## 具体任务
1、三入口升级 CTA 与归因口径对齐（收入优先，1-3 天内完成）
  - 统一 Popup / Options / 官网落地页（或商店描述中的导流入口）的升级文案与目标链接，确保全部落到同一套可归因参数口径（`campaign/source/medium/content`）。
  - 对齐事件命名与字段：至少固化 `upgradeEntryClick`、`proIntentFormStart`、`proIntentFormSubmit` 三类事件及其必填字段。
  - 更新执行口径文档：`docs/growth/metrics.md`，新增 v1-100 指标定义与取值口径，确保后续导出文件可直接复盘。

2、问卷最短路径与意向样本增量（收入优先，1-3 天内完成）
  - 在不打扰免费核心体验前提下，完成“1 分钟问卷”最短路径（入口可达、字段最小化、提交后可确认）的最小可交付增量。
  - 产出并落盘 `docs/evidence/v1-100/` 证据包，至少包含：
    - `intent-funnel-v1-100.csv`（按入口统计 `upgradeEntryClicks/formStarts/formSubmits`）
    - `intent-funnel-summary-v1-100.json`（转化率口径与聚合结果）
    - `intent-sample-audit-v1-100.json`（样本完整性与审计链）
  - 至少新增 3 条可追溯意向样本，覆盖官网、商店、扩展内入口三类来源。

3、测试用例、自动化门禁与汇报材料（必须可执行）
  - 创建测试用例文档：`docs/test-cases/v1-100.md`，至少覆盖：
    - 用例 A：三入口 CTA 可达与参数一致性校验。
    - 用例 B：问卷最短路径完整性（进入、填写、提交、确认）校验。
    - 用例 C：商业化漏斗证据导出校验（CSV/JSON 与 `metrics` 口径一致）。
    - 用例 D：顺延策略校验（Top1 阻塞记录与 Top2 交付主题一致）。
  - 执行 `bash scripts/test.sh`，将日志落盘为 `docs/evidence/v1-100/test.sh.log`，并在 `docs/test-cases/v1-100.md` 记录 PASS/BLOCKED 与证据文件名。
  - 新增执行记录与简报：`docs/growth/executions/v1-100-pro-intent.md`、`docs/reports/v1-100-report.md`。

## 验收标准
* 修复上述所有问题，并测试通过，达到可发布状态
* 实现以上所有需求，并测试通过，达到可发布状态
* 可验证的商业化推进证据（必须可审计/可复盘）：
  - `docs/evidence/v1-100/intent-funnel-v1-100.csv` 与 `docs/evidence/v1-100/intent-funnel-summary-v1-100.json` 可直接展示三入口 `upgradeEntryClicks -> formStarts -> formSubmits` 漏斗。
  - `docs/evidence/v1-100/intent-sample-audit-v1-100.json` 至少包含 3 条新增样本，且三类来源（官网/商店/扩展内）各至少 1 条。
  - `docs/growth/metrics.md` 与证据包指标一致，能计算 `intentSubmitRate = formSubmits / upgradeEntryClicks`。
* 顺延策略可验证：
  - `docs/roadmap_status.md` 明确记录 Top1 阻塞原因与本轮顺延执行 Top2（`prds/v1-100.md`）的一致性。
* 测试与用例可执行：
  - `docs/test-cases/v1-100.md` 提供可复现步骤、预期结果、回归记录（PASS/BLOCKED + 证据文件名）。
  - `bash scripts/test.sh` 执行完成且日志落盘到 `docs/evidence/v1-100/test.sh.log`。

## 注意
1、以上任务规模非常大，每一个都很有挑战性，弄清楚需求后你应该创建新的需求文档来记录每个子需求，例如 `v1-100-1.md`、`v1-100-2.md`、`v1-100-3.md`（按入口对齐/漏斗证据/测试门禁拆分）
2、一步一步来，对于每个阶段，边执行边更新对应文档中的进度（至少同步 `docs/roadmap.md`、`docs/roadmap_status.md`、`docs/growth/executions/v1-100-pro-intent.md`）
3、根据需求创建测试用例文档，通过单元测试和自动化测试来确保代码的正确性（固定路径：`docs/test-cases/v1-100.md`）
4、目标任务成功完成之后，写一份简要的汇报材料文档（用于口头汇报，所以要简明扼要抓住重点），说清楚每一个具体任务现在是什么状态（是否按要求完成了），已经实现了什么效果，以及本次对代码的修改范围（涉及哪些目录和文件）（固定路径：`docs/reports/v1-100-report.md`）
5、收入第一：除阻塞上架/收款/转化漏斗的关键问题外，不处理纯体验细节；若 Top1 权限/代理阻塞仍未解除，必须先更新 `docs/growth/blocked.md` 与 `docs/growth/todo.md`，再继续推进可离线复核的收入证据闭环
